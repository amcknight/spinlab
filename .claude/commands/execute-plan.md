Find the plan file in docs/superpowers/plans/ whose filename or title best matches: $ARGUMENTS

If no match or ambiguous, prefer the most recent by date prefix. If no arguments, use the most recent plan.

Once you have the plan file path, read and follow .claude/skills/execute-plan.md to execute it.
