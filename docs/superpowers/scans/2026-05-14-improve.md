---
date: 2026-05-14
focus: "full sweep"
git_head: 0e8905e
lenses_run: [architect, control-inversion, test-skeptic, dead-code, types, observability]
critiques_run: [skeptic, convergence]  # inline — async critique agents vanished from task queue mid-run
findings_count: ~95 raw lens → ~25 after dedup → 9 after critique/verify
status: full_scan
---

## Top wins

### must-fix
- *(none — no broken-now or correctness-risk items)*

### high-leverage
- **CF1' — type the event boundary end-to-end** — carry-over from 2026-05-13; plan was never written. `protocol.py:34,43,49,55,69` `conditions: dict` × 5 + `poller.py:38` `on_event: Callable[[Any]]` + `movies.py:51` `on_event: Callable[[object]]` + `condition_registry.py:159` `decode -> dict[str, Any]` + `recorder.py:36` `PendingStart.raw_conditions: dict`. Unlocks downstream `# type: ignore` removal in `db/model_state.py:69,80,95` and `db/attempts.py:96`. size: **medium**
- **CF-NEW-1 — wire `transition_mode` into the only mode-flip seam** — `models.py:33-37` `transition_mode()` defined but VERIFIED never called (grep clean across `python/spinlab/`); `session_manager.py:358` `_apply_result` sets `mode = result.new_mode` without validating; `session_manager.py:396-416` `finalize_run` and `save_and_finish_run` set `self.mode` directly, bypassing `_apply_result` entirely. Closes the "Mode mutated freely" pattern that 3 lenses surfaced. size: **medium**
- **CF8' — state-machine logging + diagrams** — carry-over from 2026-05-13. `DIAGRAMS.md` has only the high-level flow; no `stateDiagram-v2` for TransitionDetector / PracticeTiming / movie playback. `retroarch/predicates.py` (TransitionDetector `step()`) emits zero state-transition log lines. size: **medium**

### convergent win
- **CF-NEW-3 — dead/gravestone cleanup batch** — absorbs: D1-D4 (`ra_game_basename` field at `config.py:24` + 3 references at `config.py:64,85` / `wiring.py:49` / `cold_fill.py:53`), D6 (`poller.py:9-10` `mark_state_loaded` gravestone), D7 (`raclient.py:197` same gravestone), D9 (`recorder.py:159` `_compute_is_primary` single-call inline). Note: last scan PICKED `ra_game_basename` for deletion but commit 738d4cb didn't include it — actually-dead still. size: **trivial**
- **CF-NEW-4 — trivial test deletion batch** — absorbs 9 tautology tests: `test_errors.py:45-51` (parametrized exception attributes), `test_errors.py:53-55` (raise/catch ceremony), `test_romid.py:24-28` (Python hash determinism), `test_transition_state.py:5-18` (dataclass field reset), `test_segment_variants.py:43-49` (mocked CRUD), `test_db_capture_sessions.py:16-28` (basic CRUD field-existence — borderline, VERIFY tagged low), `test_nci.py:15-17` (issubclass tautology), `test_model_output.py:15-40` (dataclass field-assignment), `test_snapshot.py:6-23` (frozen dataclass shape). C12 (test_models JSON key-order) kept per skeptic — real algorithmic property. size: **trivial**

### nearby cleanup
- `routes/system.py:75` — wrap private `session._clear_ref_and_idle()` in a public method. size: trivial
- `capture/recorder.py:76-100` — `pending_start` staleness/timeout check (3rd-time carry-over). size: trivial
- `session_manager.py:213-217` — `_notify_sse` exception log lacks state-builder context. size: trivial
- `retroarch/poller.py:84-89` — condition-recovery log info-only, no count of missed frames. size: trivial
- `retroarch/raclient.py:284-323` — save_state retry count not logged. size: trivial
- A5 (`cold_fill` ↔ `fill_gap` spawn duplication) — premature DRY for single-game codebase; revisit if a third capture controller appears
- A7 (`start_reference` ↔ `resume_reference`) — small duplication; extract only if touched again
- A6 / A11 / B3 / B4 (parallel session hierarchies) — YAGNI until a third session type lands
- A9 (`api_schemas.Estimate` / `models.Estimate` duplication) — VERIFY says intentional (codegen pipeline); leave

## Picked this session
- **CF1' (type event boundary)** → handed off to: writing-plans → execution; branch `improve/type-event-boundary`
- **CF-NEW-3 (dead-code batch)** → handed off to: inline edit on the same branch
- **CF-NEW-4 (trivial test deletion)** → handed off to: inline edit on the same branch

## Dropped during critique
- A3 alone (`transition_mode` unused) — folded into CF-NEW-1 as part of the same fix.
- B6 (state-gated routing race) — asyncio single-threaded; handler runs to completion before next event. Same debunk as last scan's B5.
- B7 (PracticeSession.handle_death calls emu directly) — by design; PracticeSession owns reload-on-death.
- B8 (poller optional detector defaults) — Bundle 2 added DI; the optional default is intentional convenience for tests.
- B12 (on_poller_event push-splits) — passive timing observers is the right design.
- B13 (handler crash not re-queued) — log+continue is correct at 60Hz tick; re-queueing a poison event would loop.
- C12 (test_models JSON key-order) — lens misread; real canonical-JSON property test.
- D5 (`_drop_legacy_allocator_config_key`) — still needed; lens itself said keep.
- D8 (`Attempt.__post_init__` duplicates DB CHECK) — defense-in-depth; keep.
- A13 (state_builder `get_scheduler()` eager fetch) — DEBUNKED by VERIFY: result is cached on Session, not per-snapshot.
- A6/A11/B3/B4 OVERSTATED: real duplication but YAGNI for single-game solo-dev codebase.
- A8 (_notify_sse swallows exceptions) OVERSTATED: SSE broadcast must not crash on bad subscriber; the fix is adding context, not stopping the catch.
- CF-NEW-2 (eliminate placeholder lambdas in wiring) — VERIFY downgraded to "working as designed"; not worth the disruption.

## New finds from VERIFY pass
- `models.py:33-37` `transition_mode` is definitely uncalled across `python/spinlab/` (raises confidence of CF-NEW-1 to high).
- `session_manager.py:159-171` `get_scheduler` IS memoized — A13 lens claim debunked.
- `condition_registry.read_all` failure is caught at `poller.py:84-88` and degrades to an unchanged event — silent skip is by design, not a missing handler.
- `test_db_capture_sessions.py:16-28` is closer to a real schema-contract test than tautology; VERIFY tagged low confidence on the "delete" call.

## Full merged list (collapsed)

### Architectural (Lens 1) — A1-A13
A1 mode validation gap (FOLDED into CF-NEW-1)
A2 routes call private (nearby)
A3 transition_mode dead (FOLDED into CF-NEW-1)
A5 cold_fill/fill_gap dup (nearby, YAGNI)
A6 parallel sessions (nearby, YAGNI)
A7 start/resume dup (nearby)
A8 _notify_sse swallows (nearby — log context only)
A9 ModelOutput dup (nearby — intentional)
A10 callers bypass _apply_result (FOLDED into CF-NEW-1)
A11 receive_result asymmetric (nearby, YAGNI)
A12 lambda rebound (DROPPED, working as designed)
A13 unconditional get_scheduler (DEBUNKED, V: memoized)

### Coupling (Lens 2) — B1-B13
B1/B2 placeholder lambdas (DROPPED — A12 family)
B3/B4 SSE lambdas (nearby — A6 family)
B5 3-way ConditionRegistry (nearby trivial)
B6 routing race (DEBUNKED — asyncio)
B7 handle_death direct (DEBUNKED — by design)
B8 optional detector defaults (DEBUNKED — convenience)
B9 SystemState not injected (low priority)
B10 build gets full SessionManager (low priority)
B11 pending_start no timeout (nearby — carry-over)
B12 on_poller_event splits (DEBUNKED)
B13 handler crash logged-not-requeued (DEBUNKED)

### Tests (Lens 3) — C1-C15
C1-C6, C9, C13, C14 (PICKED into CF-NEW-4)
C7 AsyncMock vs FakeEmuBackend (deferred — needs test-by-test judgement)
C8 asyncio.sleep timing (debunked by last scan, not actually flaky)
C10/C11 subprocess/health-check patched (deferred)
C12 JSON key-order (DROPPED — real property)
C15 monkeypatch registry loader (deferred)

### Dead code (Lens 4) — D1-D9
D1-D4 ra_game_basename family (PICKED into CF-NEW-3)
D5 drop_legacy_allocator_config_key (DROPPED — still needed)
D6/D7 mark_state_loaded gravestones (PICKED into CF-NEW-3)
D8 Attempt.__post_init__ (DROPPED — defense-in-depth)
D9 _compute_is_primary inline (PICKED into CF-NEW-3)

### Types (Lens 5) — E1-E15
E1-E5 conditions: dict in 5 events (FOLDED into CF1')
E6/E7 Callable[[Any]] callbacks (FOLDED into CF1')
E8/E9 Callable | None callbacks (FOLDED into CF1' as satellite)
E10/E11 estimator priors/params (could fold or stand alone — deferred)
E12 _launch_emulator -> dict (nearby)
E13 get_paused_state -> dict | None (nearby)
E14 PendingStart.raw_conditions: dict (FOLDED into CF1')
E15 decode -> dict[str, Any] (FOLDED into CF1')

### Observability (Lens 6) — F1-F16
F1 event loop no event context (nearby trivial)
F2 unhandled event no payload (nearby trivial)
F3 read_all silent (DEBUNKED — caught at poller)
F4 poller recovery info-only (nearby trivial)
F5 first load-timeout silent (DEBUNKED — V: logged)
F6 pending_start collision dropped (folds into B11)
F7 ReplayErrorEvent bare exc (DEBUNKED — message included)
F8 save_state timeout (nearby trivial)
F11 cold_fill reset no state dump (nearby trivial)
F12 estimator fallback silent (defer)
F14 state machine transitions don't log (FOLDED into CF8')
F16 missing stateDiagram-v2 (FOLDED into CF8')

## Critique reports

### Skeptic (inline — async agent vanished)
KILL: A3 standalone (folds into CF-NEW-1), B6 (asyncio single-threaded), D8 (defense-in-depth), D5 (still needed), B8 (intentional default), C12 (real property), B13 (correct 60Hz pattern).
OVERSTATED: A6/A11/B3/B4 (YAGNI single-game), A13 (memoized), B7 (by design), A8 (SSE must catch), B12 (passive observer correct), F14/F16 (already named CF8').
MISSED: condition_registry.decode return type (CF1' covers); pending_start staleness still uncommitted; cli.py `db reset` status.

### Convergence (inline)
- CF1' = CF1 reloaded (carry-over, never executed). Absorbs E1-E15. medium.
- CF8' = CF8 reloaded (carry-over, partial). Absorbs F4, F11, F14, F16. medium.
- CF-NEW-1 mode-validation gate. Absorbs A1, A3, A10. medium.
- CF-NEW-3 dead/gravestone batch. Absorbs D1-D4, D6, D7, D9. trivial.
- CF-NEW-4 test deletion batch. Absorbs C1-C6, C9, C13, C14. trivial.

### Verify
See inline summary above; key debunks: A13 memoized, B6/B7/B8/B12/B13 by-design, F3/F5/F7 working-as-intended. High-confidence: CF1', CF-NEW-1 (transition_mode VERIFIED uncalled), CF-NEW-3 (ra_game_basename VERIFIED unread), A2, A9, CF8', F2.
