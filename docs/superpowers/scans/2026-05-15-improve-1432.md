---
date: 2026-05-15
focus: "full sweep (no focus arg)"
git_head: dd0ba100cc07550c9627c7443f8cd116723e8dfc
lenses_run: [architect, control-inversion, test-skeptic, dead-code, types, observability]
critiques_run: [skeptic, convergence]
findings_count: 70+
status: full_scan
---

## Top wins

### must-fix
*(none — nothing broken-now; the StateBuilder atomicity claim was DEBUNKED by VERIFY: `build()` is sync, snapshot pattern at lines 33-37 is intentional)*

### high-leverage

- **HL-A — Practice loop observability gaps**. Absorbs L6-O1, L6-O2, L6-O7, L6-O8, L6-O9. VERIFY confirmed all 5 anchors high. Andrew's daily-use path. `pick_next()` returns None for 3 distinct reasons all logged the same generic message; practice task crash log lacks `session_id`/`game_id`/`segments_attempted`; segment-load timeout logs only first occurrence; missing-result path is fully silent (segment "attempted" but never recorded). size: **medium** (touches `practice.py:161-229`, `scheduler.py:104-115`, `session_manager.py:459-466`).
- **HL-B — `_snapshot_expected_times` silent shrinkage** at `practice.py:74-104`. VERIFY confirmed `os.path.exists` check silently drops segments without logging — UI saved-time panel can lie. size: **trivial** → SHIPPED this session.
- **HL-C — Poller silences detector/cold_fill exceptions** at `retroarch/poller.py:138-169`. VERIFY confirmed the lens misframed slightly: exceptions ARE logged via `log.error`, but at 60Hz a persistent fault floods the log. size: **trivial** → SHIPPED this session (transition-log pattern applied).

### convergent win

- **CF-4 — Literal/Enum types end-to-end across protocol + api_schemas**. Absorbs **9 findings**: L5-Y1 (`ActionResponse.status: str`), L5-Y2 (`SetConditionsCmd.definitions: list[dict]`), L5-Y3 (`LevelExitEvent.goal: str = "abort"`), L5-Y4 (`cp_type`/`end_type`/`checkpoints` bare strings in protocol.py), L5-Y5 (Status enum mismatch with ActionResponse), L5-Y20 (`condition_registry.replace_with_read_specs(list[dict])`), L1-A10 (ModelOutput duplicated in api_schemas + models), L5-Y17 (EndpointType StrEnum vs Literal duplicate), L5-Y18 (Mode duplicated). VERIFY confirmed all anchors high. Codegen pipeline (`scripts/dump_openapi.py` → openapi-typescript) propagates Literal → TS string-union, so this also tightens frontend types. — size: **medium** → handed off to `writing-plans`.
- **CF-2 — Routes-through-SessionManager write facade**. Absorbs **6 findings**: L1-A3 (practice_invalidate constructs internal event), L2-I6 (`_resolve_replay_path` direct DB), L2-I7 (`/segments` GET+PATCH direct), L2-I8 (`/attempts/{id}` PATCH no SSE notify), L2-I9 (`/reset` sets `session.scheduler = None` directly), L2-I10 (`/sessions` direct DB read). VERIFY confirmed all six anchors. — size: **medium-big** (not picked this session).

### nearby cleanup

- **Cluster H** dead artifacts: `models.py:46-60` `EventType` (zero usages), `session_manager.py:76` `self.invalidate_combo` (never read). `NetworkConfig.port` DEBUNKED — actually used in `cli.py`. size: **trivial** → SHIPPED this session.
- **M4** Enable `--strict-markers` in pyproject.toml so marker typos fail loudly. size: **trivial** → SHIPPED this session.
- `session_manager.py:179-181` `_clear_ref_and_idle()` helper, 3 callers, asymmetric — VERIFY confirms. size: **trivial**.
- **Cluster J** Estimator param dict shapes (`get_priors -> dict`, `init_state(priors: dict)`, `_resolve_window`, `_resolve_params`). All verified high. Replace with `EstimatorParams`/`Priors` dataclasses. size: **trivial-medium**.
- **Cluster I** Frontend test debt: `model-logic.test.ts:160-198` `canStartSpeedRun` duplicate tests (verified high), `tuning-sync.test.ts:45-70` `setTimeout(r, 0)` racy, `format.test.ts:84-94` timing-window flake, `model-logic.test.ts:102-157` + `api-contract.test.ts:122-141` tautology asserts. size: **trivial**.
- **Cluster K** DB row → typed: `db/attempts.py:96,106,123` `dict(zip(cols, row))`, `db/model_state.py:56-59` manual dict, `db/segments.py:42` vs `db/core.py:117` `_row_to_segment` Callable vs staticmethod. All verified. size: **medium**.
- **L2-I4 / L2-I3** Dual scheduler at `practice.py:47` + `session_manager.py:436-457`. VERIFY confirmed: PracticeSession constructs its OWN Scheduler, independent of SessionManager's. Not a correctness bug today (scheduler.pick_next syncs config from DB each call), but extra wiring + an asymmetry worth resolving. size: **medium** when picked.
- `retroarch/poller.py:42-44` gravestone comment (known carry-over). size: **trivial**.
- `condition_registry.py:87-100` YAML errors lack file context. size: **trivial**.
- `routes/system.py:99-114` RA-already-running heuristic treats unreachable as not-running (skeptic defended; lower priority).

## Picked this session

- **HL-B, HL-C, Cluster H, M4** — 4 trivials → inline edits on branch `improve/literal-enum-types-and-cleanups`, 4 commits (`e9bbe01`, `21d97cf`, `d407dd2`, `dd0ba10`). Fast suite 880 passed, full suite 892 passed (12 emulator tests actually ran).
- **CF-4 — Literal/Enum types end-to-end** (medium, convergent win) → handed off to `superpowers:writing-plans`.

## Dropped during critique

- L1-A1 "576-line God Object" — KILLED by skeptic. Project orchestrator scope is normal; recent encapsulation pass already extracted FillGapController/MovieController/build_orchestrator.
- L1-A5 `_LEGAL_TRANSITIONS` bare dict — KILLED by skeptic + DEBUNKED by VERIFY: `dict[Mode, set[Mode]]` keyed on Enum has natural type safety from the Enum.
- L1-A14 dashboard.create_app couples to wiring — KILLED. Only one backend (RA); backend-agnostic abstraction is post-migration cargo-culting.
- L2-I12 late-binding lambda — KILLED. Known accepted smell from prior audit.
- L2-I14 StateBuilder no atomic snapshot — DEBUNKED by VERIFY. `build()` is sync, no `await` inside, snapshot pattern at lines 33-37 is intentional and documented.
- L2-I15 AppConfig static (no hot-reload) — KILLED. Restart-on-config-change is fine for a solo tool.
- L2-I17 SSE always full snapshot — KILLED. Payload is tiny on localhost.
- L2-I20 "no precondition validation in routes" — KILLED. Validation IS in SessionManager.transition; route signature would just duplicate.
- L2-I22 Database mixin no facade — KILLED. Bundle 1 already added the facade.
- L3-T4 frontend api.test mocks fetch — KILLED. Defended in prior scan.
- L3-T7 test_transitions bare asserts — KILLED. Defended in prior scan.
- L3-T15 test_harness_isolation overhead — KILLED. Intentional regression guard (project_ra_sram_isolation memory).
- L3-T17 "no property-based tests" — KILLED. Wishlist; conflicts with PoC-first principle.
- L4-D4 `NetworkConfig.port` — DEBUNKED by VERIFY: actually used in `cli.py:130,144,204` to write `.spinlab-ports` AND passed to `launch_emulator`.
- L4-D5 poller gravestone comment — KILLED as known carry-over (low pri but kept on the list).
- L5-Y22 `fetchJSON<T = unknown>` default — KILLED. Codegen pipeline supplies explicit types at call sites.
- L6-O5 state_builder exceptions silenced — LOW confidence per VERIFY: no try/except blocks observed in `build()` or helpers.
- L6-O11 event loop broad `except Exception` — KILLED. Top-level handlers should be broad.
- L6-O15 no "still alive" log in practice loop — KILLED. SSE state stream already provides liveness.
- L6-O19 RA-already-running heuristic — KILLED. Solo machine; alternative is more brittle.

## New finds from VERIFY pass

- **N1** `[python/spinlab/models.py:46-60](python/spinlab/models.py#L46)` — `EventType` class confirmed zero usages. → SHIPPED.
- **N2** `[python/spinlab/practice.py:47](python/spinlab/practice.py#L47)` vs `[python/spinlab/session_manager.py:79](python/spinlab/session_manager.py#L79)` — Dual Scheduler creates asymmetry. Each construction reads DB state independently; the practice loop's scheduler is independent of SessionManager's. Noted for future medium pick.
- **N3** `[python/spinlab/routes/attempts.py:14-24](python/spinlab/routes/attempts.py#L14)` — PATCH mutates without SSE notify. Frontend won't see invalidation until next poll (already in CF-2).
- **N4** `[python/spinlab/routes/segments.py:45-55](python/spinlab/routes/segments.py#L45)` — Segment PATCH not guarded by SessionManager during recording. Already in CF-2.

VERIFY also added (from skeptic's MISSED list, not separately verified):
- **M2** No `test_config_example_loads` test (asserts `config.example.yaml` parses cleanly as AppConfig).
- **M3** No assertion that `tests/integration/addresses.py::ADDR_MAP` contains expected key set.
- **M4** `--strict-markers` was not enabled in pyproject. → SHIPPED.
- **M9** No exhaustiveness test for `_LEGAL_TRANSITIONS` (every Mode has an entry).
- **M11** Codegen pipeline does propagate Python `Literal` types to TS string-unions, so CF-4 doubles as a frontend-typing improvement.

## Synergies

- **HL-A + CF-2 share session_manager.py + routes.** A `practice_event(kind, **fields)` structured-log helper added during CF-2's facade work is the natural place to land HL-A's structured logging.
- **HL-B sits adjacent to HL-A in practice.py** — natural co-edit (HL-B shipped this session, HL-A waits).
- **Cluster H + L1-A2 (`_clear_ref_and_idle`)** are both small `session_manager.py` edits — batch in a future cleanup commit.
- **CF-4 is independent of everything else** — pure type-substitution pass, no architectural reshuffle. Safe to ship in parallel with HL-A / CF-2.

## Full merged list (collapsed by lens)

70+ findings across 6 lenses (architect / control-inversion / test-skeptic / dead-code / types / observability). Key clusters:

- **Architect (L1-A1..A16)**: God-object, bare-dict transitions, current_session_id property, scattered mode mutations, StateBuilder reach-in, _clear_ref_and_idle asymmetry, scheduler.record_attempt non-atomic, SegmentWithModel over-fetch, Pydantic extra="allow", _snapshot_expected_times file-check, ModelOutput duplication, lazy scheduler DCL, current_session_id, condition_registry dual-mode, dashboard create_app coupling, protocol empty-string defaults, frontend _currentGameId.
- **Control inversion (L2-I1..I22)**: orchestrator construction, lazy DCL, dual scheduler (start_practice creates PracticeSession creates Scheduler), routes pulling DB direct (replay_path, segments GET+PATCH, attempts PATCH no SSE, /reset direct mutation, /sessions DB direct), condition registry not resent on reconnect, late-binding lambda, scattered mode mutations, StateBuilder atomicity (DEBUNKED), AppConfig static, controller eager construction, SSE full snapshot, on_attempt callback, start_reference no rollback, route precondition validation, pick_next config sync per call, db mixin facade.
- **Test skeptic (L3-T1..T17)**: canStartSpeedRun duplicate tests, tuning-sync racy, elapsedStr timing window, api.test mocks fetch, model-logic tautologies, segment-detail Chart.js mocking, test_transitions bare asserts, time.sleep NCI test, function-local conftest import, allocator ordinal helper, sent_commands no length check, api-contract tautologies, savings-panel className, manage.test confirm bleed, harness_isolation overhead, estimator_sanity helper semantics, no property-based tests.
- **Dead code (L4-D1..D5)**: EventType class (SHIPPED), invalidate_combo field (SHIPPED), config.yaml stale Mesen keys (skeptic defended), NetworkConfig.port (DEBUNKED — used), poller gravestone comment.
- **Types (L5-Y1..Y22)**: ActionResponse.status: str, SetConditionsCmd.definitions list[dict], LevelExitEvent.goal: str, cp_type/end_type/checkpoints bare strings, ActionResponse vs Status mismatch, state_builder bare dict (J3 carry-over), capture/reference get_paused_state, capture/cold_fill get_state, estimator dict signatures (get_priors, init_state, _resolve_window, _resolve_params), speed_run checkpoints: list[dict], speed_run SegmentRow ad-hoc, dict(zip) in db/attempts, manual dict in db/model_state, EndpointType StrEnum vs Literal duplicate, Mode duplicate, _row_to_segment Callable vs staticmethod, condition_registry list[dict], _load_estimator_params, fetchJSON<T = unknown>.
- **Observability (L6-O1..O26)**: practice loop None pick reason, scheduler.pick_next 3 silent failure modes, ROM dir errors, practice exceptions no context, state_builder exceptions (LOW), condition YAML no file context, practice task crash no session_id, segment load timeout, missing-result path silent, SSE broadcast failure, event loop except broad (KILLED), poller detector/cold_fill exceptions (SHIPPED), routes/model rebuild not atomic, rebuild_all_states unobservable, no heartbeat (KILLED), segment history curve sync, install_condition_registry no try/except, poller condition read recovery, routes/system RA-already-running heuristic (KILLED), db/ layer-level log, config.py missing-key, speed_run aggregate missing-state, Pydantic 422 not logged, errors.py ActionError details, nci.py protocol parse errors, NotRunningError no mode.

## Critique reports

Full skeptic + convergence-hunter + verifier outputs are in the conversation transcript for this run. Key verdicts surfaced above in `Dropped during critique` and `New finds from VERIFY`.
