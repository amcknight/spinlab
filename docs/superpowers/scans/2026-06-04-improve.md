---
date: 2026-06-04
focus: "full sweep"
git_head: 26454e4ea3318ea5de070a43899c4de0d9dfbee9
lenses_run: [architect, control-inversion, test-skeptic, dead-code, types, observability]
critiques_run: [skeptic, convergence]
raw_findings: ~50
merged_findings: 27
picked: 3 (2 inline + 1 to writing-plans)
status: full_scan
---

Branch: `improve/model-inputs-facade-and-cleanups`. Baseline 1161 passed, 1 v07 warning. Post-edit identical.

## Top wins

### must-fix
*(empty — C17a "mutable default" was a critique hallucination; the line is a local-variable assignment inside `def evaluate()`, not a default arg. See "Dropped during critique / verify" below.)*

### high-leverage
- **C10 — `NetworkConfig.port` truly dead** — `python/spinlab/config.py:13`. Verifier confirmed `scripts/spinlab.ahk:11-23` reads only `dashboard_port`, never `tcp_port`. Three-scan carry-over (2026-05-15, 2026-05-19, 2026-05-27). — size: **trivial** — SHIPPED (909122f).
- **C20 — silent conditions-registry empty load** — `python/spinlab/condition_registry.py:198-204` returned `ConditionRegistry(definitions=[])` without logging when no per-game YAML existed; every event downstream carried `conditions={}` with no breadcrumb. — size: **trivial-small** — SHIPPED (854548e).
- **C19 — practice `asyncio.TimeoutError` loop has no escalation or metric** — `python/spinlab/practice.py:283-289` logs once at INFO and loops forever; no counter, no escalation level. Skeptic downgraded to "OVERSTATED" because long idle on a segment is by design; the actual fix is a metric, not a timer. Deferred to nearby cleanup.

### convergent win
- **Fix A — public `model.inputs` facade on Scheduler/EmSuiteSamplerEstimator** — promote `_attempts_from_rows`, `_events_from_rows`, `_load_all_sampler_states` to public; expose cached `SamplerState` map; collapse `routes/model.py:103-104` inline `EmSuiteSamplerEstimator()` to a scheduler lookup. Absorbs C1 (4 call sites reaching past underscore helpers), C2 (inline estimator), C4 (`session_manager.py:550-568` snapshot replays events instead of using cache), and naturally typed-up C15 (`estimators/session_snapshot.py:146,166` bare-dict diff returns). All four verified high-confidence. — size: **medium** — PICKED → writing-plans.

### nearby cleanup (verified real, low value — do when in the area)
- **C13** `python/spinlab/routes/model.py:63` `body: dict[str, int]` → `AllocatorWeightsRequest` Pydantic model.
- **C16** `python/spinlab/condition_registry.py:141` `read_all(...) -> dict[str, int]` → `TypedDict` `ConditionValues`.
- **C11** `python/spinlab/practice.py:50` / `python/spinlab/hyper_play.py:62` `Callable | None` → parameterised signatures (`Callable[[Attempt], None]`, `Callable[[HyperPlayEvent | None], None]`).
- **C17b** `python/spinlab/routes/practice_engine.py:168` `ctx = dict(body.objective_ctx)` — defensive copy that doesn't currently lose typing, but if `objective_ctx` is ever tightened to a TypedDict, this would silently degrade it.
- **C3 / M2** `python/spinlab/routes/model.py:323-325` "while the three call sites stabilize" comment — Fix A's facade satisfies the stabilization condition; delete the route-side copy.
- **C19** `python/spinlab/practice.py:283-289` add a `practice_load_timeouts` metric / a WARN at N≥5 instead of forever-INFO at N=1.
- **C25** `python/spinlab/session_manager.py:312-316` death-event mode mismatch — log carries the event but not "why we landed in this mode."
- **C27** `python/spinlab/allocators/__init__.py:68-85` model-output JSON failure logs without segment context.
- **C24** `python/spinlab/retroarch/movies.py:147-160` `MoviePlaybackError` vs `RAClientError` collapsed into one `ReplayErrorEvent`.
- **C23** `python/spinlab/session_manager.py:328-329` `except (ConnectionError, OSError)` classifies a permission-denied OSError as "backend gone."
- **C18 capture-side** `python/spinlab/capture/reference.py:465-476` silently replaces `state_path=None` on save_state failure; nullable propagates downstream untested (verifier M7 confirmed no test pins consumer behavior).
- **C19 metric/M10** `python/spinlab/practice.py:283-289` stuck-waiter is invisible to ops (no counter / no SSE signal). Bundle with the C19 escalation when revisiting.
- **M3** No drift test for `frontend/src/api-types.ts` vs `scripts/dump_openapi.py` output — a contributor editing `api_schemas.py` without `npm run gen-types` would silently drift FE types.
- **M6** Audit `except OSError` / `except (ConnectionError, OSError)` patterns elsewhere in `retroarch/*.py`.

## Picked this session
- **C10** `NetworkConfig.port` removal → inline → commit 909122f on branch `improve/model-inputs-facade-and-cleanups`.
- **C20** conditions-registry empty-load warning → inline → commit 854548e on branch `improve/model-inputs-facade-and-cleanups`.
- **Fix A** public `model.inputs` facade → handed off to `superpowers:writing-plans` (medium).

## Dropped during critique / verify

- **C17a (mutable default)** — DEBUNKED by self-verify. The line `threshold_kwargs: dict = {}` at `routes/practice_engine.py:156` is indented INSIDE `def evaluate()` — a local variable, not a default argument. Critic and verifier both misread the indentation. Both `routes/practice_engine.py:156` and `:168` are not bugs.
- **C3 (running_min_clean duplication)** — killed by skeptic K1 as Andrew's own deliberate stabilization-window decision (`routes/model.py:323-325` comment). Reopens automatically once Fix A lands (the call-site count will collapse).
- **C5 (`practice.py:226-271` re-extracts SegmentWithModel fields)** — killed by skeptic K2: that IS the dispatch boundary, translating a model-bearing DTO into a backend-bound command.
- **C6 (SSE `on_attempt`/`on_event` callbacks)** — killed by skeptic K3: textbook DI'd seam, not a leak. Subsumes any rationale for a "listener bus" refactor (Convergence Fix D dropped).
- **C7 partial kills** — `tests/unit/retroarch/test_poller.py:118-122` (explicit Windows-asyncio comment), `tests/unit/test_db/test_db_capture_sessions.py:96` (created_at tick), `tests/unit/test_system_route.py:94` (mtime tick) all confirmed as load-bearing workarounds, not laziness.
- **C8 (FakeState below-gate tests)** — killed by skeptic K4: the n=0 short-circuit IS a logically-distinct branch worth a guard; the real test at `:142-222` complements rather than replaces these.
- **C12 (`StateBuilder.build(...) -> dict`)** — debunked by verifier: `response_model=AppState` at `routes/system.py:35` enforces the boundary type; internal `dict` flow is allowed. (Carry-over from 2026-06-03's M4(a) finding.)
- **C14 partial (`sse.py:40 state: Any`)** — killed by skeptic K5: SSE channel is multi-payload by design.
- **C18 partial (`practice.py:117-125`)** — killed by skeptic K6: lens admits an INFO log exists; "user-facing visibility" is a UX request, not a defect.
- **C26 (SSE broadcast swallow)** — killed by skeptic K7: broken consumer must not kill the producing action; that's correct isolation.

## New finds from VERIFY pass

- **C10 fully confirmed**: AHK launcher (`scripts/spinlab.ahk:27, 97`) reads only `dashboard_port`. Both Python and AHK are dead consumers — three-scan carry-over now safely removable.
- **M4 (.spinlab-ports half-flag)**: verifier confirmed `tcp_port=` is unreferenced; the C10 removal also drops the write-side, not just the field.
- **M6 partial**: `except OSError` appears in `raclient.py`, `movie_io.py`, `nci.py` but not specifically in reset-classification context; the C23 finding is isolated to `session_manager.py:328-329`.
- **M7 confirmed**: no test pins consumer behavior for `state_path=None` propagation from `capture/reference.py:465-476`. If the nullable contract is policy-compliant, it's hoped-for, not enforced.
- **C17 both sides hallucinated by both critique agents and the verifier**: the actual code is a local-variable initialization. Verification of file:line claims by directly reading at scan-orchestrator level — not just trusting the verifier — is the lesson.

## Patterns observed by lenses

**Lens 1 (Architect):** Three modules (scheduler, em_suite_sampler, session_manager) all expose underscore-prefixed conversion helpers that get imported across module boundaries — the "no public surface, so everyone reaches into privates" pattern. Drove convergence Fix A. Otherwise codebase remains in the clean shape inherited from the recent session-lifecycle-facade work.

**Lens 2 (Control-inversion):** No new architectural rifts. The recent CTOR-injection wave (Scheduler→PracticeSession, ConditionRegistry install-once) has paid off: routes use facades, callbacks are properly DI'd, the call graph stays acyclic. Lens flagged the same private-helper leak as Lens 1.

**Lens 3 (Test-skeptic):** Eight sleep-margin sites total; verifier confirmed the load-bearing ones (Windows asyncio granularity, sqlite created_at tick, mtime tick) have honest comments. The remaining four sleep-margin tests (`test_practice.py:30-31, 75-76, 184` and `test_session_manager.py:514-537`) deserve a flake-ledger audit before any refactor — per `feedback_isolate_flake_trigger_with_controlled_experiment`.

**Lens 4 (Dead-code):** Only one true find — `NetworkConfig.port` — which had been carrying through three prior scans pending AHK confirmation. This scan closed the loop and the field is now removed. No new gravestones from recent commits.

**Lens 5 (Types):** Pattern repeats from prior scans — bare `dict[str, ...]` returns at internal builder boundaries (`state_builder.py`, `session_snapshot.py`, `condition_registry.py`) where a TypedDict would name the contract. Mostly nearby-cleanup; one of them (`session_snapshot.py:146, 166`) collapses naturally under Fix A.

**Lens 6 (Observability):** Two clusters that recur — silent log-and-continue around save_state/load_state error paths, and silent log-and-continue around YAML/structured-input failures. The C20 fix this session covers the conditions-YAML half of the second cluster; the rest are nearby cleanup. The skeptic's "single I/O failure policy for NCI calls" (Fix E) is real but too big for a drive-by — deferred until a 2am incident actually forces the discussion.

## Critique reports

(Skeptic and Convergence-hunter reports, plus the Verify pass, are recorded in the conversation transcript. The merged + ranked digest above represents the orchestrator's final synthesis.)

## Lens-finding cross-reference

For archival — full merged list with cluster numbers, lens origin, and final verdict:

- **C1** private helpers leaked (architect + control-inversion + types) — verified high → Fix A.
- **C2** inline `EmSuiteSamplerEstimator()` at `routes/model.py:103-104` (architect + control-inversion) — verified high (overstated severity) → Fix A.
- **C3** `_running_min_clean` duplication (architect + dead-code) — killed (Andrew's intentional split) → reopened by Fix A.
- **C4** snapshot rebuilds events (architect + control-inversion) — verified high (not hot-path) → Fix A.
- **C5** `practice.py:226-271` re-extracts fields — killed (dispatch boundary).
- **C6** SSE callbacks — killed (textbook DI).
- **C7** sleep-margin tests (8 sites) — 3 killed as load-bearing; 5 deferred to flake-ledger audit.
- **C8** FakeState below-gate tests — killed.
- **C9** YAML monkeypatch — verified, deferred (low value).
- **C10** dead `NetworkConfig.port` — verified high → SHIPPED.
- **C11** `Callable | None` → typed (2 sites) — trivial, deferred.
- **C12** StateBuilder bare dict — debunked (route response_model).
- **C13** bare-dict request body — trivial, deferred.
- **C14** misc `Any` (5 sites) — `sse.py:40` killed (multi-payload by design); others deferred.
- **C15** session_snapshot diff bare dict returns — verified → collapses under Fix A.
- **C16** ConditionRegistry.read_all bare dict — trivial, deferred.
- **C17** mutable-default + dict copy — both DEBUNKED by self-verify.
- **C18** silent save/load_state errors (5 sites) — `practice.py:117-125` killed; rest deferred to capture-observability audit.
- **C19** async timeout no escalation — overstated (loop is correct; metric is the real gap), deferred.
- **C20** conditions YAML silent empty load — verified high → SHIPPED.
- **C21** finalizer per-step logging — deferred.
- **C22** orchestrator unknown-command silent return — deferred.
- **C23** OSError mis-caught as backend gone — deferred.
- **C24** replay error type collapse — deferred.
- **C25** death-event mode-mismatch silent log — deferred.
- **C26** SSE broadcast swallow — killed (correct isolation).
- **C27** allocator model-output deserialization silent — deferred.
