---
date: 2026-06-21
focus: "full sweep"
git_head: 5c042f2
lenses_run: [architect, control-inversion, test-skeptic, dead-code, types, observability]
critiques_run: [skeptic, convergence]
raw_findings: 21
merged_findings: 9
picked: 3
status: full_scan
---

## Top wins

### must-fix
*(none — nothing broken-now)*

### high-leverage
- **EndpointType enum** — `api_schemas.py:64,66,82,84` `start_type/end_type: str` → existing `EndpointType` StrEnum (`models.py:47`). Verified high; codegen note at `api_schemas.py:38-50` confirms StrEnum emits the *identical* TS string-union wire format, so the change is wire-safe — pure end-to-end type-safety gain. Continuation of the prior scan's typed-routes theme. — size: **medium**

### convergent win
- **Type StateBuilder's bare-dict returns** — absorbs: [item-1a `cold_fill.get_state()→ColdFillState`, item-1b `reference.get_paused_state()→PausedRunState`, item-1c `state_builder.build()→AppState`] — the two getters are clean; `build()→AppState` is entangled with the in-place `base: dict` mutation in `_build_practice_state` (design call to settle). Also defuses item-2 (StateBuilder reading controller state — VERIFIED those are all *public* interfaces, so not a real leak). — size: **medium**

### nearby cleanup
- `movie_io.py:233-239` — silent `os.unlink` retry exhaustion; log on failure to match `_cleanup_staged:320`. — size: **trivial** — SHIPPED (`2b8c78f`).
- `test_protocol_menu.py:19-20` — drop `assert PracticePauseCmd() is not None` trivial smoke + unused import. — size: **trivial** — SHIPPED (`2b8c78f`).
- `db/segment_fits.py:29,59,79,92` — `dict[str,Any]` fit payloads; real but v07 pre-MVP, lower priority. — size: medium (deferred)
- **SessionManager start/done duplication** (`session_manager.py:680-848`) — 40+ verified duplicated lines. — size: **medium** — PICKED this session.

## Synergies
- The convergent "type StateBuilder returns" absorbs three lens findings (the two getters + `build()`) and defuses the item-2 "encapsulation leak" (verified public, not a leak).

## Picked this session
- **SessionManager start/done duplication** (medium) → handed off to: `superpowers:writing-plans` → branch `improve/session-lifecycle-dedup-and-cleanups`.
- movie_io stranded-source log (trivial) → inline → SHIPPED `2b8c78f`.
- Drop pause-cmd smoke test (trivial) → inline → SHIPPED `2b8c78f`.

## Dropped during critique / verify
- **Item 5 — Hyper-play emits per-level not per-attempt** (Lens 1) — **DEBUNKED** by verify. `segments_recorded` increments per-segment inside `_record_attempt` (`hyper_play.py:326`, called at the checkpoint/complete handlers `:250-256`/`:299-304`), independent of `on_event`. The `on_event(None)` at `hyper_play.py:309-310` is a level-boundary SSE sentinel bound to `_notify_sse` (`session_manager.py:807`), not the counter driver. Counter is NOT stalled.
- **Item 9 — `_segments_v07` unused imports (~11)** (Lens 4) — **KILLED** by skeptic. The module is a vendored/parked prototype the maintainer has explicitly said NOT to "fix" for cosmetic noise (~110 expected pyright errors from a sys.path shim; see memory `project_segments_v07_known_issues`). Churning a frozen prototype for lint hygiene is net-negative.
- **MISSED-D — exception chaining asymmetry** (skeptic) — **DEBUNKED** by verify. Both `start_practice` (`session_manager.py:732`) and `start_hyper_play` (`:825`) already `raise SnapshotFailedError() from exc`. No asymmetry.
- **Item 2 severity (StateBuilder "encapsulation leak")** — DOWNGRADED. Verify confirmed all five accesses (`is_recording`, `cold_fill.get_state()`, `emu.is_connected`, `rec_path`, `get_paused_state()`) are public `@property`/methods — a deliberate thin-coordinator interface, not a leak. Folded into the convergent typed-returns win rather than standing alone.
- **Item 7 (test cleanups) — 3 of 4 survived.** Only `test_protocol_menu.py:20` was a clean drop. `test_practice.py:102-107` is a legit init-state test; `gamepad/test_loop.py:81` (`_detector._commands`) is the *only* coverage of the button-inversion mapping; `test_dashboard_boot.py:20`'s 2s `done.wait` is a real sync point. Left them.

## New finds from VERIFY pass
- (bonus) `gamepad/test_loop.py:81` pins a double-private field (`_detector._commands`) as the sole inversion coverage — fragile but currently the only signal; rewrite to public-API assertion is a future nicety, not flagged this session.

## Notable deferred (bigger/real — future sessions)
- **Type StateBuilder's bare-dict returns** (convergent above) — strong follow-on; the two clean getters are low-risk, `build()→AppState` needs a design call on the dict-mutation builder pattern.
- **db/segment_fits.py `dict[str,Any]` fit payloads** — real undertyping but v07 pre-MVP; harden when v07 graduates.
- **Late-binding callback wiring** (Lens 2) / **SegmentRecorder testability** (Lens 2) / **timing observe_event vs tick push/pull** (Lens 2) — known-deferred design tensions; an explicit `OrchestrationDeps` would help. Big.

## Patterns observed by lenses (archival)
- **Architect:** backend is well-organized (DB mixins, thin coordinators, clear protocol boundaries). Real structural finds are localized duplication in the practice/hyper-play session layer; capture/cold_fill/fill_gap controllers have asymmetric start/skip/abort APIs with no shared interface.
- **Control-inversion:** widespread callback-binding DI (Poller, MovieController, gamepad, timing) with late rebinding after construction; stateful components expose mutable-state methods rather than pure event-in/state-out — efficient but couples caller control flow to internal state machines.
- **Test-skeptic:** suite is generally healthy — no mock-over-integration sins (FakeEmuBackend + real DB), no skips/xfails, focused reused factories. Only modest waste (one trivial smoke test).
- **Dead-code:** app code (routes/capture/retroarch) is clean; recent Mesen→RA and WRAM→gamepad migrations were well-cleaned. Dead imports are confined to the parked `_segments_v07` tree.
- **Types:** repeated "bare dict matching an existing schema" smell (state_builder, cold_fill, reference getters, fit payloads); `EndpointType` exists but API schemas use bare `str`; DB layer already uses TypedDicts elsewhere — extending that pattern would catch field-drift.
- **Observability:** structured-logging discipline is good (`log.warn(logger, ..., k=v)`); cold-fill detector has excellent trace diagnostics. One real asymmetry: movie record cleanup swallowed unlink failure while playback cleanup logged it (now fixed).

## Full merged list (collapsed)
1. Typed bare-dict returns — `state_builder.py:31` build()→AppState, `cold_fill.py:168` get_state()→ColdFillState, `reference.py:181` get_paused_state()→PausedRunState (Lens 5). VERIFIED high.
2. StateBuilder reads controller state — `state_builder.py:41-42,68,121,125,130` (Lens 1). VERIFIED but all public → downgraded.
3. Stringly-typed EndpointType — `api_schemas.py:64,66,82,84` vs `models.py:47` (Lens 5). VERIFIED high, wire-safe.
4. Fit payload `dict[str,Any]` — `db/segment_fits.py:29,59,79,92` (Lens 5). VERIFIED, v07 pre-MVP.
5. Hyper-play per-level on_event — `hyper_play.py:309-310` (Lens 1). DEBUNKED.
6. SessionManager lifecycle duplication — `session_manager.py:680-848` (Lens 1). VERIFIED 40+ dup lines. PICKED.
7. Test cleanups ×4 — `test_protocol_menu.py:20`, `test_practice.py:102-107`, `gamepad/test_loop.py:81`, `test_dashboard_boot.py:20` (Lens 3). 1 dropped, 3 kept.
8. movie_io silent unlink — `movie_io.py:233-239` (Lens 6). VERIFIED. SHIPPED.
9. `_segments_v07` unused imports ×11 (Lens 4). KILLED.
