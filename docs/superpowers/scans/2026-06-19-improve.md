# /improve scan — 2026-06-19

Focus arg: non-UI backend architecture; prefer simple, self-contained cleanups.
6 lenses → merge → verify (1 verifier; skeptic/convergence applied inline by orchestrator).
Branch: `improve/typed-routes-and-cleanups`.

## Top wins

### must-fix
*(none)*

### high-leverage / convergent win
- **Typed route outputs** — `routes/attempts.py:18` (`surgery_rows` → `SurgeryAttempt`), `routes/segments.py:39` (→ `ApiSegment`), `routes/reference.py:84` (→ `Reference`). Routes built untyped `list[dict]` (via `dict(row)` + key mutation) whose fields duplicate Pydantic schemas already in `api_schemas.py`. Verified high (field sets match; all three routes declare `response_model=` so output was already schema-constrained — construction is behavior-identical, the win is pyright safety + no field-drift). — size: **medium** — SHIPPED (`ccaf659`).

### nearby cleanup (trivial, verified high)
- `practice.py:71-72` — `on_attempt`/`on_segment_load: Callable|None` → `Callable[[Attempt],None]` / `Callable[[str],None]`. — SHIPPED (`86eeda3`).
- `hyper_play.py:62` — `on_event: Callable|None` → `Callable[[HyperPlayEvent|None],None]` (annotation lied — `on_event(None)` is passed at ~`:310`). — SHIPPED (`86eeda3`).
- `routes/model.py:65` — `body: dict[str,int]` → `AllocatorWeightsRequest` (a `RootModel[dict[str,int]]`, to keep the flat wire format `{"alloc": N}` rather than nesting under `weights`). 3rd-scan carry-over. — SHIPPED (`86eeda3`).
- `tests/unit/test_errors.py:80-102` — 3 near-identical per-error handler tests collapsed into ONE parametrized handler-integration test. NOTE: the verifier mis-called them "redundant with `test_action_error_attributes`" — but that test only asserts exception *attributes*, not the handler wiring through a real request. Naive deletion (commit `1470316`) orphaned `_build_app_with_raising_route` and dropped real coverage; caught at review and restored as a parametrized test (`12bbb24`).
- `tests/unit/test_dashboard_boot.py:39` — removed a dead `time.sleep(0.1)` (thread already joined via `done.wait`). — SHIPPED (`1470316`).

## Synergies
- The "typed route outputs" medium absorbs three separate lens findings (attempts/segments/reference) under one theme: routes constructing existing schemas instead of bare dicts. Lens 1 (architect) independently flagged `state_builder.py`'s dict-returning as the same smell (NOT done this session — bigger, ties into the SessionManager/StateBuilder boundary).

## Picked this session
- Typed route outputs (medium) + the 5 trivials — all SHIPPED inline on `improve/typed-routes-and-cleanups` (`86eeda3`, `1470316`, `ccaf659`, `12bbb24`).

## Dropped during critique / verify
- **addresses.py `ADDR_CONTROLLER_HELD`/`_1` "gravestone"** (Lens 1 pattern + Lens 4 explicit, orchestrator's own prior assumption) — **DEBUNKED**. Verifier grepped: both constants are LIVE in `tests/integration/addresses.py` `ADDR_MAP`, consumed by the RA-poke harness (`ra_poke_engine.py`, `poke_parser.py`). Not orphaned. The comment referencing the deleted `menu_detector.py` is mildly stale but the constants are load-bearing — leave them.
- **db/core.py:111 `except Exception: pass` on connection close, no log** (Lens 6 F1) — verified the no-log claim but it's a *documented* best-effort teardown ("one bad close mustn't strand the rest"); CLAUDE.md discourages unearned log noise. Not a defect.
- **raclient.py state-load validation masks NCI errors with generic StateLoadError** (Lens 6 F8/F9) — real, but verifier sized it **big** (needs error-context preservation through the validation polling loop). Deferred — not a "simple" win.

## Notable deferred (bigger, real — for a future session)
- **PracticeSession / HyperPlaySession lifecycle duplication** (Lens 1) — parallel session_id/started_at/is_running/run_loop scaffolding; a `SessionBase`/protocol would de-asymmetrize. Big.
- **SessionManager (903 lines) event-routing growth** (Lens 1) — now coordinates capture/practice/hyper-play/cold-fill/replay/gamepad; two-level dispatch to mode coordinators. Big.
- **Late-binding callback wiring** (Lens 2) — poller/movies/gamepad/timing all get `on_event` rebound after construction; an explicit `OrchestrationDeps` would make the call graph legible. Medium-big.
- **Async sleep-based test brittleness** (Lens 3) — ~53 `asyncio.sleep` calls drive background-task timing in tests; event-based signalling would de-flake. Medium, risky (touch carefully).
- **state_builder.py returns bare dicts matching AppState/SessionInfo/CurrentSegment** (Lens 5) — same typed-output smell as the routes, but tangled with the StateBuilder/SessionManager boundary. Medium.

## Patterns noticed (archival)
- Backend is well-organized: DB layer uses mixins (not a god object), routes stay thin, protocol boundaries are clear. Most real findings are 1-2 line type/annotation fixes, not refactors.
- Dict-building anti-pattern in routes (`out: list[dict]` → `dict(row)` → mutate → append) is the most repeated smell; this session converted the three response routes, `state_builder` remains.
- Lens 6 (observability) over-produced (10 findings) — most were "add a log to an intentional best-effort path," which the project's own rules discourage. Verify/skeptic correctly cut them to ~1 real (the big raclient one).
- Process note: the verifier's "redundant test" call was a coverage trap (attribute-test ≠ handler-integration test). Reinforces: re-read what a test actually exercises before deleting it.
