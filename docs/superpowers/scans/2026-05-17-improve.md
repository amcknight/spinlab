---
date: 2026-05-17
focus: "full sweep"
git_head: 812b6797776be77f33969680c92a62885462d6d9
lenses_run: [architect, control-inversion, test-skeptic, dead-code, types, observability]
critiques_run: [skeptic, convergence]
raw_findings: 31
merged_findings: 31
picked: 7
status: full_scan
---

## Top wins

### must-fix
*(none)*

### high-leverage
- **CF-β — Typed AppState + typed DB rows** — `state_builder.py:30` returns bare `dict`; `db/segments.py:100,132` + `db/model_state.py:69` + `db/attempts.py:96` use `dict(zip(...)) # type: ignore`; `capture/cold_fill.py:127` + `capture/reference.py:183` return `dict|None`. One typed-row helper + AppState assembly drops the ignores and gives the SSE/REST boundary real type safety. — size: **medium** — absorbs T1+T2+T3+A1(partial).
- **A3 — SessionLifecycle spawn helper** — `session_manager.py:445-456` and `:494-508` are the same 11-line pattern (`create_task(run_loop) + add_done_callback(_on_X_done)`); `_on_practice_done`/`_on_speed_run_done` at `:458-465`/`:510-517` are structurally identical. — size: **medium** — NOT picked this session; queued.

### convergent win
- **CF-ε — Dead-config + CLI stub trim** — absorbs D1 (`cli.py:157,180-182` `stats` stub) + D2 (`config.py:35-37,47,66-69` orphaned `PracticeConfig.invalidate_combo` — parser + test exist but grep shows zero runtime consumers; reader was deleted in the 2026-05-15 scan). — size: **trivial**
- **CF-θ — Frontend error-surface helper** — absorbs O9 (`frontend/src/api.ts:59-75` SSE `catch (_)` silently drops JSON parse) + O10 (`frontend/src/app.ts:52-68` `String(err)` shows "[object Object]"). — size: **trivial**

### nearby cleanup
- **TS2** `tests/unit/test_session_manager.py:518` — `asyncio.sleep(10)` 10× longer than necessary. — size: trivial
- **A4** `session_manager.py:449,498` — fire-and-forget `_notify_sse` callbacks; VERIFY confirmed `_notify_sse` already self-logs at `:204-216`, so this is cosmetic only. — size: trivial (defer)
- **O6** `capture/cold_fill.py:98-102` — VERIFY DEBUNKED at edit-time: `spinlab.log.warn(exc=exc)` calls `level_fn(msg, exc_info=exc)` which preserves the traceback. Not a defect.

### Carry-overs not surfaced (already backlogged)
- **M1 / CF-2** routes-through-SessionManager facade — known pending
- **M2** Dual Scheduler at `practice.py:47` vs `session_manager.py:169` — known pending
- **M4** `transition_mode()` dead validator at `models.py:36-40` — known pending
- **M5** `EndpointType` propagation across schemas/db/allocators — known pending
- **M7 / Cluster J** Estimator dict-param shapes — known pending
- **HL-A** Practice loop observability gaps — known pending
- **CF3** in-flight exception-context-helpers plan (already absorbs O2 orchestrator.py:304-305, partial O3 sse.py)
- **poller.py:42-44** gravestone (user declined repeatedly)

## Picked this session
- **CF-ε** (trivial, convergent) → inline edit on branch `improve/typed-boundaries-and-cleanups`: delete `PracticeConfig` + `practice` field + the orphaned test; remove the `stats` CLI subparser + handler.
- **CF-θ** (trivial, convergent) → inline edit: add `formatClientError` helper + log SSE parse failures; replace `String(err)` in `app.ts`.
- **TS2** (trivial, nearby) → inline edit: `tests/unit/test_session_manager.py:518` reduce sleep from 10s to 1s.
- **CF-β** (medium, high-leverage) → handed off to `superpowers:writing-plans` on the same branch.

## Dropped during critique
- **A6** "two ConditionRegistry instances, switch_game leaves poller stale" — VERIFY DEBUNKED: `wiring.py:91` constructs one shared `ConditionRegistry` for orchestrator+poller; `install_condition_registry()` at `session_manager.py:244-254` syncs the orchestrator's copy via `SetConditionsCmd` → `_on_set_conditions` → `replace_with_read_specs`. Capture's registry is intentionally separate (decode-capable, YAML-loaded) since capture needs the full ConditionDef shape, not just read specs.
- **A1** StateBuilder "private reach-in" — fields `session.practice_session`, `.capture`, etc. are PUBLIC attributes; this is normal composition.
- **A5** `orchestrator.py:285` push+pull "tangle" — skeptic right; on_poller_event correctly multicasts events to two consumers (timing modules + event queue).
- **A7** `practice.py:78` `_current_state_path` armed/cleared by different paths — works because event loop serializes; framing was speculative.
- **D3** `raclient.py:482` "stale gravestone" — comment is a legitimate why-comment explaining `SPEED_UNCAPPED` history.
- **O2, O3** subset of in-flight CF3 plan.
- **O4** `poller.py:118-127` `_read_failing` "never cleared" — DEBUNKED, cleared at `:125-127` on recovery (same for `_detector_failing`, `_cold_fill_failing`).
- **O5** `raclient.py:319-323` `StateSaveTimeoutError` "missing diagnostics" — message already names pattern + RA basename; deeper retry-budget telemetry is a wishlist not a defect.
- **O7** `db/migrations/__init__.py:91-99` silent rollback — already logs before re-raise.
- **T6** `condition_registry.py:91-117` `from_yaml` schema validation — already partly addressed in recent commit `c932e66`.
- **T7** `scheduler.py:78-80` `all_weights` matches `AllocatorWeightsResponse.weights` but unlinked — zero churn unless shapes diverge.
- **TS1** `test_log_context.py:81-86` `asyncio.sleep` → lambda — standard async-test idiom, not a defect.
- **TS3** `test_state_builder.py:18-139` mock_emu ceremony — taste, not a regression risk.

## New finds from VERIFY pass
- **V1** `session_manager.py:204-216` `_notify_sse` already includes `exc=` + `subscriber_count=` context — partially addresses O3; the "what state was broadcast" addition would balloon the log payload (AppState is large). Skeptic right to push back.
- **V2** `poller.py:118-188` has three independent failing-flag streams (`_read_failing`, `_detector_failing`, `_cold_fill_failing`) all properly cleared on success with a "recovered" log line. Pattern is consistent and correct.
- **V3** `cli.py:180-182` `stats` IS plumbed (prints "Stats coming in a future step." + exits) — so "dead code" framing was wrong; CF-ε pick is "rip the stub" rather than "wire up the dead branch."
- **V4** `config.py` `PracticeConfig.invalidate_combo` confirmed orphan: grep returns only config.py + test_config.py — zero runtime consumers. Migration history (2026-05-15 scan) shipped the consumer-side deletion; this scan finishes the schema end.
- **V5** At edit-time, **O6 dropped**: `spinlab.log.warn(exc=exc)` (at log.py:30-31) calls `level_fn(msg, exc_info=exc)`, which preserves traceback via Python's standard `exc_info` path. The lens claim "loses traceback" was wrong. Counts as a lens hallucination — add to the pattern.

## Patterns observed by lenses
- **Architect**: Asymmetries between PracticeSession and SpeedRunSession lifecycle (A3) are the same as the 2026-05-16 lens's "near-identical done-callbacks" — the duplication has now been confirmed across two consecutive scans and is the right next medium after CF-β.
- **Control-inversion**: Push+pull is consistent across orchestrator → timing → queue; the two-fire-paths concern (A5) is a topology choice, not a defect. Real coupling lives in the SessionManager god-object area (carry-over).
- **Test-skeptic**: Codebase has largely absorbed the mock/sleep critiques from prior scans. Remaining items (TS1-TS3) are small enough that only TS2 earned a pick.
- **Dead-code**: Two genuine orphans found (D1 `stats` stub-with-handler, D2 `PracticeConfig.invalidate_combo` orphan). D3 was a false positive — gravestones are not the same as why-comments.
- **Types**: The pattern is the same across 4 sessions: ad-hoc dicts at SQLite ↔ controller ↔ API boundaries; AppState exists but is bypassed. CF-β is the natural convergent answer.
- **Observability**: Most exception-context gaps already in CF3 scope; remaining items are small (O6 traceback fix) or speculative (O5 retry telemetry).

## Full merged list (collapsed)

Architect (7): A1 StateBuilder private reach (DEBUNKED — public attrs), A2 SessionManager god object (carry-over framing), A3 spawn-task lifecycle dup, A4 fire-and-forget _notify_sse (cosmetic), A5 push+pull (DEBUNKED), A6 two registries (DEBUNKED), A7 reload-on-death state split (DEBUNKED).

Control-inversion (7): I1 lambda SSE coupling (= A4), I2 same for speed-run (= A4), I3 push/pull (= A5), I4 PracticeSession self-Scheduler (carry-over M2), I5 _current_state_path split (= A7), I6 routes call private (carry-over M1), I7 two registries (= A6, DEBUNKED).

Test-skeptic (7): TS1 lambda-sleep (DEBUNKED), TS2 asyncio.sleep(10), TS3 mock_emu ceremony, plus known-sound carry-overs (test_mix_allocator σ, test_db_capture_sessions sleep, conftest 10s uvicorn, format.test.ts drift).

Dead-code (4): D1 cli.py stats stub, D2 config.py PracticeConfig orphan, D3 raclient.py:482 gravestone (DEBUNKED), plus M4 transition_mode (carry-over).

Types (9): T1 StateBuilder.build dict, T2 ColdFill/PausedRun dict|None, T3 db dict(zip), T4 EstimatorParamsRequest, T5 RawConditions vs ConditionMap, T6 from_yaml validation (already partly fixed), T7 all_weights link, plus M5 EndpointType (carry-over), M7 estimator params (carry-over).

Observability (10): O1 reference.py save_state, O2 tick_loop context (CF3), O3 _notify_sse state (CF3-adjacent, V1), O4 poller flag (DEBUNKED), O5 StateSaveTimeoutError diag, O6 cold_fill traceback, O7 migration rollback (DEBUNKED), O8 60Hz heartbeat, O9 SSE parse, O10 fetchSegments error string.

## Critique reports

**Skeptic verdict**: KILL D3 (legit why-comment), TS3 (taste), T7 (zero churn), O3 (payload-size footgun), O9 (defensible JSON-parse skip), A4 (already self-logs), TS1 (standard idiom). OVERSTATE A2 (vibe without seam), A6 (verify-or-drop), A1 (public attrs), T1 (one-line trivial), T3 (standard idiom), O8 (medium too high). MISSED 13 small items (dep file hygiene, multiple CLI stubs, migration transaction model, conftest diagnostic block, SSE reconnect backoff, raclient magic numbers, _notify_sse gather, db connection lifecycle, .gitattributes hook, poller _read_failing lifecycle, migration test coverage) — most of these are speculative without verification.

**Convergence-hunter verdict**: 8 candidate fixes — CF-α single registry (DEBUNKED by VERIFY — not a fix, the two registries are by design), **CF-β typed AppState+rows (PICKED)**, CF-γ SessionLifecycle base (big, deferred — A3 medium is the trim-down version), CF-δ typed-conditions vocabulary (deferred), **CF-ε dead-config audit (PICKED)**, CF-ζ flaky tests (rolled into nearby), CF-η heartbeat (overlap-with-CF3 risk + skeptic downgrade), **CF-θ frontend error helper (PICKED)**.

**VERIFY verdict**: A6 highest-impact claim DEBUNKED through direct re-read of wiring.py + session_manager.install_condition_registry + orchestrator._on_set_conditions. T1+T2+T3 confirmed. D1 plumbed-but-stub. D2 confirmed orphan (zero runtime grep hits). O4+O7+D3 DEBUNKED through re-read. _notify_sse self-logs (mitigates A4+O3 concerns).
