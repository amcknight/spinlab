---
date: 2026-05-17
focus: "flaky, skipped, slow tests"
git_head: 0843fe4 (at scan start)
lenses_run: [architect, control-inversion, test-skeptic, dead-code, types, observability]
critiques_run: [skeptic, convergence]
raw_findings: ~50
merged_findings: 18
picked: 4 (3 absorbed into 1 cleanup commit + 1 helper-extraction commit)
status: full_scan
---

## Top wins

### must-fix
*(none)*

### high-leverage
*(none — the lens swarm proposed several mediums but VERIFY debunked or downgraded all of them)*

### convergent win
- **CF-cleanup-batch** — absorbs K3 (orphan `_hard_kill`) + L (stale `--rom-key default` docstring) + M7 (dead skipif). Three trivial dead-code/stale-doc fixes shipped as one commit on `improve/test-infra-trivials`. — size: **trivial**

### nearby cleanup
- **A** `tests/integration/conftest.py:274-291` + `:543-579` — extracted `_wait_for_dashboard_state(base_url, *, check)` helper; absorbs M9 (mid-function `import time as _time`). Three near-identical 40×0.25s polling loops collapsed to predicate-based helper. — size: **trivial** (shipped)
- **M3** `tests/integration/conftest.py:716-721` — diagnostic hook duck-types tuples on `t[0].startswith("http")`, so `(Page, list[str])` from `test_frontend_smoke.py:99` silently bypasses the diagnostic dump. — size: trivial (deferred; small footprint until a frontend-smoke failure actually loses diagnostics)

### Carry-overs not surfaced (already backlogged)
- **CF-β** AppState/typed DB rows — in flight on prior branch
- **CV2** typed `IntegrationTestContext` dataclass — proposed 2026-05-14, declined as deferrable; verifier reconfirmed
- **M15-family async sleeps** in `test_practice*.py` / `test_speed_run_mode.py` / `test_poller*.py` — VERIFY confirmed these are intentional event-delivery delays (the deliver coroutine has to land after `run_one()` parks on `_result_event.wait()`); not bugs, not flakes
- **M16** `test_nci.py:105-106, 214-215` sleeps — verifier confirmed TOCTOU-in-theory only; localhost UDP processed in microseconds; no observed flake
- **poller.py:42-44** gravestone — user declined repeatedly

## Picked this session
- **CF-cleanup-batch** (trivial, convergent) → inline edit on branch `improve/test-infra-trivials`, commit `0e57cf2` "tests/integration: dead-code sweep — orphan helper, stale docstring, dead skipif"
- **A + M9** (trivial, nearby) → inline edit on same branch, commit `c7f5629` "tests/integration/conftest: extract _wait_for_dashboard_state polling helper"

## Dropped during critique
- **Cluster B** (`ra_poke_engine.py:90` 100ms band-aid) — Skeptic right that NCI has no request-reply primitive that would serialize the FRAMEADVANCE queue; sleep is the only available sync point. Comment is rationale, not apology.
- **Cluster C** (~17 `asyncio.sleep(0.05)` sites in practice/poller tests) — VERIFY confirmed `run_one()` parks on `_result_event.wait()` at `practice.py:228`; the sleep is the deliberate "let the deliver task run before awaiting the result" pattern, not a bug. Bare `await asyncio.sleep(0)` does NOT suffice.
- **Cluster D** (`test_nci.py:105-106, 214-215` UDP TOCTOU) — Skeptic right that fake server runs in-process on localhost; datagrams processed in microseconds; 50ms is safe headroom. No observed flake.
- **Cluster H** (`conftest.py:728, 744, 755-761` `<unavailable: {exc}>`) — diagnostic code MUST be defensive (its job is to dump state without itself crashing the report). Swallow-with-context is correct design.
- **Cluster M** (`test_replay_fixture.py:49-52, 102-106` "shows one snapshot") — `_wait_for_idle_with_progress` already logs trajectory to the spinlab logger which the diagnostic hook tails. Observability is fine.
- **Cluster O** (`ra_harness.py:310-312` `except: pass`) — covered by next-pass `get_status()` in the same loop; persistent NCI wedge surfaces via the `else` branch.
- **Cluster R** (practice tests use AsyncMock vs FakeEmuBackend) — solve different problems. AsyncMock is for argument-shape assertions; FakeEmuBackend is for state-bearing integration. Both appropriate.
- **Cluster S** (Windows-only skips) — confirmed acceptable; `skipif sys.platform != "win32"` is a real platform gate.
- **Cluster X** (session-wide `_RingHandler` not test-scoped) — `pytest_runtest_setup` calls `_ring.clear()` per test; no leak.
- **Cluster Y** (three poller test files) — distinct concerns (core mechanics / condition-registry integration / state_path resolver); consolidation would create a god-file.
- **Cluster Q+U+V** — already covered by in-flight CF-β plan.
- **Cluster W** (`_HarnessFactory` hides reset mode) — cache key is `(rom_key, use_fresh_state)`; both axes are visible.

## New finds from VERIFY pass
- **M1** `conftest.py:498` `pytest.skip("replay_ra_dashboard: emulator.savestate_dir not configured")` — looked like a CLAUDE.md violation at first glance, but it's a conditional skip on missing user config, not on missing infra. Appropriate.
- **M2** `scripts/coverage.sh` Windows-portability gap — documentation lists `./scripts/coverage.sh` but there's no PowerShell sibling. Tooling note, not a code issue.
- **M3** Frontend-smoke fixture silently missing from diagnostic dump — real but small; deferred until it bites.
- **M4–M6** Magic constants in `make_fresh_boot_state.py` (SETTLE_FRAMES=600, SAVE_DRAIN_SEC=1.0) and `ra_poke_engine.py` (POST_LOAD_SETTLE_FRAMES=30) — all have justifying comments per project_model_principles. Not violations.
- **M7** `test_replay_fixture.py:109-111` skipif — fixture files committed and git-tracked, condition always false; promoted into the cleanup batch.
- **M8** Eight more `asyncio.sleep` sites in `test_speed_run_mode.py:172-275` — same pattern as M15-family; same conclusion (intentional event-delivery delays).
- **M9** `conftest.py:540` mid-function `import time as _time` — folded into helper-extraction commit.
- **M10** Nested timeouts in `test_replay_fixture.py:34-52` — `_api(..., timeout=5)` inside a 15s outer wait; `requests.RequestException` not caught. Verifier judged correct (a timeout IS a real failure that should propagate).

## Patterns observed by lenses
- **Wall-clock as synchronisation primitive** (Architect, Control-Inversion, Test-Skeptic agreed). True at the boundary (UDP fire-and-forget) where no event is available; less true inside the codebase, where the lens swarm conflated "intentional fixture delay before delivering an event" with "polling-with-sleep."
- **Untyped fixture returns + duck-typed diagnostic hook** (Architect, Types) — real but small; the `startswith("http")` check is robust enough that the typing gap doesn't actually hurt today.
- **CF3 stopped one level short** (Convergence-hunter, Observability) — true but in production code, not flaky-test scope; orchestrator `_tick_loop` doesn't name which timing module raised. Out of scope for this scan; backlog candidate.

## Full merged list (collapsed)
(18 clusters A–Y as enumerated in PRESENT phase; see Picked + Dropped + New-finds above for disposition.)

## Critique reports
(Skeptic: 6 KILLs, 11 OVERSTATEMENTS, 11 MISSED items. Convergence: 6 proposed fixes; VERIFY accepted only Fix 5 (wait-for-url) and Fix 6 (dead-code sweep) as actionable in this scan; Fix 1 (drive_until helper) was debunked because the underlying "sleep cluster" is intentional delivery delay; Fix 2-4 deferred or out of scope. Full verbatim retained in chat transcript.)
