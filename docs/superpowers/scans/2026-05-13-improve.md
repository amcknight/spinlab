---
date: 2026-05-13
focus: "full sweep"
git_head: 8b80c9a29b681dde512a1d8b5b338ac5d26ee3c2
lenses_run: [architect, control-inversion, test-skeptic, dead-code, types, observability]
critiques_run: [skeptic, dreamer, convergence]
findings_count: 86 (raw lens) → ~30 after dedup/critique
status: full_scan
---

## Top wins

### must-fix
- **F23 — `_on_practice_done` swallows task exceptions** (`session_manager.py:454-457`) — practice task crashes silently flip mode to IDLE with no log. One-line fix: call `task.exception()` and log if non-None. size: **trivial**

### high-leverage
- **CF1 — Type the event boundary end-to-end** — `poller.py:34` `on_event: Callable[[Any], None]` is the upstream root; downstream symptoms are `conditions: dict` on every protocol event (`protocol.py:34,43,49,69`), `_stamp_*: Any → Any` (`poller.py:62,72`), `condition_registry.decode → dict[str, Any]` (`condition_registry.py:159`), `state_builder.build → dict` (`state_builder.py:28`). Replace callback with `Callable[[PollerEvent], None]` (sealed union of frozen event dataclasses), `conditions: dict[str, str | bool]`, typed stampers, typed `build()` return. Absorbs B11, E2, E3, E8, E9, E10. size: **medium**
- **CF4 — Structured error context + sweep silent-except sites** — `errors.py:17-19` (`ActionError` no-args) is the upstream cause. Symptoms: `poller.py:101-103` silent 60Hz read failures, `state_builder.py:173-174` silent JSON parse, `practice.py:251-254` swallowed ConnectionError, `movies.py:146-150` swallowed RAClientError, RAClient slot/move/socket-drain failures. Define `class SpinLabError(Exception)` with `context: dict[str, Any]` + a `log_with_context(logger, exc, **extras)` helper. Sweep with grep. size: **medium**

### convergent win
- **CF8 — Observability charter** — DIAGRAMS.md missing 5 diagrams + state-transition decision points lack log lines. Absorbs F4, F9, F10, F16, F17, F19, F23, F26 (with overlap into CF4). Add `stateDiagram-v2` for ReferenceController IDLE/RECORDING/PAUSED, save-state file lifecycle, cold-fill spawn detection. size: **medium**

### nearby cleanup
- Delete dead config knobs `ra_game_basename` + `ra_core_subdir` (`config.py:24,26`). `invalidate_combo` deferred per Andrew. size: **trivial**
- Delete trivial tests: `test_protocol.py:11-42` (5 dataclass field-existence), `test_models.py:8-53` (6 ID-gen tautologies). Tighten `test_mix_allocator.py:40-50` bounds (650<a<950 is wide but not currently flaky). size: **trivial**
- Make protocol event dataclasses `frozen=True` (after grep confirms no field-rewriting, e.g. `dataclasses.replace` in `_stamp_*`). size: **trivial** if clean, **medium** if rewrites exist
- Add `pending_start` staleness check in `SegmentRecorder` — no timeout today. size: **trivial**
- A7 two-Schedulers (`practice.py:45` vs `session_manager.get_scheduler`) — verifier downgraded: Scheduler is stateless view-builder, not currently bug-causing. size: **nearby**
- A1/A11 SessionManager glue + scattered mode-transition enforcement — already mostly addressed by the recent encapsulation pass; remaining glue is genuine orchestration. size: **nearby**
- Check `db reset` CLI command — CLAUDE.md references it but VERIFY didn't find it in `cli.py`. Either docs are stale or it moved. size: **trivial**

## Picked this session
- **F23 fix** → handed off to: inline edit
- **Delete dead config knobs** (`ra_game_basename`, `ra_core_subdir`) → handed off to: inline edit
- **Delete trivial protocol/models tests** + tighten mix-allocator bounds → handed off to: inline edit
- **Make protocol events frozen=True** (pending grep) → handed off to: inline edit
- **CF1 (type event boundary)** → handed off to: brainstorming → writing-plans
- **CF4 (structured errors)** → handed off to: brainstorming → writing-plans
- **CF8 (observability charter)** → handed off to: brainstorming → writing-plans

## Dropped during critique
- **A4 / R2 — split or delete RAClient** — verifier confirmed RAClient is 480 lines of substantive mtime-polling / slot-resolution / retry-timing logic; not a thin shim. Andrew's "split when tests land" stance stands.
- **N1 — delete protocol events entirely** — events cross no wire (VERIFY: in-process only) but they're convenient typed payloads; deletion replaces typed dispatch with arg-soup.
- **C1 / C9 — flaky poller / NCI tests** — debunked: tests pair sleep with explicit stop, not retry-loop.
- **M1 — Mesen/Lua residue** — debunked: zero hits across `python/spinlab/` and `frontend/src/`.
- **K6 / E1 — Mode enum vs Literal duplication** — they ARE truly separate definitions (V10 confirmed), but the codegen pipeline justifies the structure; the cleanup (api_schemas re-exporting the Enum) folds into CF1 rather than standing alone.
- **A14 — cold_fill/fill_gap "near-identical" spawn observation** — verifier: moderate overlap, not near-identical; premature DRY.
- **A15 — SMW-specific fields on SpawnEvent** — single-game codebase; YAGNI to abstract.
- **N4 — ship Kalman, delete SM-2** — out of scope for /improve.
- **B5 — mode mutated in handlers (race)** — asyncio single-threaded; mutations don't span awaits in practice. Skeptic correct.
- **B6 — cold_fill cascade in `finalize_run`** — designed feature (cold capture auto-start); not a surprise side-effect.
- **A12 — DB 8-mixin over-composition** — mixin-per-table is fine; per-feature repositories would be a net-negative for a solo dev.

## New finds from VERIFY pass
- **`poller.py:122` `self._deps.on_event(event)` has no try-except** — a buggy handler crashes the 60Hz tick. Folds under CF4.
- **`db reset` command referenced in CLAUDE.md but missing from `cli.py`** — either docs are stale or it moved. Quick check needed.
- **No `@dataclass(frozen=True)` on protocol events** (M14 from skeptic) — confirmed. Mutation surface exists via `dataclasses.replace` in poller stampers.
- **`pending_start` has no staleness check** (M8 from skeptic) — confirmed. If a spawn never confirms, the field could leak across runs.

## Full merged list (collapsed)

### Architectural (Lens 1) — A1-A15
- A1 `session_manager.py:28-102` — SessionManager 400-line god-object (DOWNGRADED nearby)
- A2 `session_manager.py:60-95` — `get_scheduler()` lock scope (DEBUNKED race claim, V3)
- A3 `routes/practice.py:28` — route calls `_handle_attempt_invalidated` directly
- A4 `retroarch/raclient.py` — 480-line wrapper (DROPPED, V5: substantive logic)
- A5 `retroarch/orchestrator.py:96-122` — hand-rolled command dispatch
- A6 `capture/recorder.py:39-150+` — SegmentRecorder mixed concerns
- A7 `practice.py:45` — two Schedulers per game (VERIFIED but stateless, DOWNGRADED nearby)
- A8 `frontend/src/model.ts:1-100+` — Model tab overload
- A9 ReferenceController push vs PracticeSession pull (DOWNGRADED stylistic per skeptic O4)
- A10 `session_manager.py:258-310` — handler-guard asymmetry (DEBUNKED, V4: Bundle 3 closed it)
- A11 `models.py:22-30` — `_LEGAL_TRANSITIONS` scattered enforcement
- A12 `db/__init__.py` — 8-mixin over-composition (DROPPED, O5)
- A13 `frontend/src/api.ts` + `model-api.ts` split (DROPPED, K8)
- A14 ColdFillController + FillGapController spawn overlap (DROPPED, V17: moderate)
- A15 `protocol.py:52-60` SMW fields on SpawnEvent (DROPPED, single-game)

### Coupling (Lens 2) — B1-B11
- B1-B3 `wiring.py:121` / `movies.py:60-64` / `dashboard.py:65` — post-hoc callback rebinding (folds into CF2 but lower priority)
- B4 `session_manager.py:444-445` — on_attempt + SSE bundle
- B5 `session_manager.py:269-301` — mode mutated in handlers (DEBUNKED, V2)
- B6 `session_manager.py:392-410` — finalize cascade to cold_fill (DROPPED, designed)
- B7 `capture/cold_fill.py:31-44` — ColdFillController owns queue
- B8 `retroarch/movies.py:118-155` — synchronous event emission
- B9 `state_builder.py` — non-atomic reads
- B10 `practice.py` — _result_event split
- B11 `retroarch/poller.py:34` — `on_event: Callable[[Any], None]` (FOLDS into CF1)

### Tests (Lens 3) — C1-C9
- C1 `test_poller.py:53,84,116` — async sleeps (DEBUNKED, V23: not flaky)
- C2 `test_db_capture_sessions.py:123` — `time.sleep(0.01)` for ordering
- C3 `test_practice_coverage.py` + `test_speed_run_mode.py` async sleeps
- C4 `test_dashboard_references.py:95-135` — 4 mock-only route tests (DOWNGRADED per K9)
- C5 `test_speed_run_route.py:39-60` — 2 mock-only tests (DOWNGRADED)
- C6 `test_mix_allocator.py:40-50` — wide statistical bounds (PICKED for tightening)
- C7 `test_protocol.py:11-42` — 5 trivial dataclass tests (PICKED for deletion)
- C8 `test_models.py:8-53` — 6 ID-gen tests (PICKED for deletion)
- C9 `test_nci.py:97-111` — fire-and-forget race

### Dead code (Lens 4) — D1-D4
- D1 `session_manager.py:74` — invalidate_combo (deferred per Andrew)
- D2 `config.py:24` — ra_game_basename (PICKED for deletion)
- D3 `config.py:26` — ra_core_subdir (PICKED for deletion)
- D4 `raclient.py:477` — Mesen-era docstring (DROPPED, K3 nit)

### Types (Lens 5) — E1-E13
- E1 Mode enum vs Literal duplication (FOLDS into CF1 as api_schemas re-export)
- E2-E3, E8-E10 — untyped event/stamper/decode/state_builder (FOLDS into CF1)
- E4 `db/model_state.py:69,80,95` — `dict(zip)+# type: ignore` cast (unlocked by CF1)
- E5-E7, E11-E12 — estimator priors/state untyped (could fold into a separate "Estimator types V2" item, not picked this session)
- E13 `db/attempts.py:96` — same TypedDict ignore (unlocked by CF1)

### Observability (Lens 6) — F1-F26
- F1, F2, F11 poller silent failures (FOLDS into CF4)
- F3 state_builder silent parse (FOLDS into CF4)
- F4 dashboard generic error (FOLDS into CF8)
- F5-F7, F12-F15 context-free warnings (FOLDS into CF4)
- F8 ActionError no-args (CF4 root cause)
- F9-F10 connection diagnostics (FOLDS into CF8)
- F16 SSE broadcast (FOLDS into CF4)
- F17 invariant assert no state dump (FOLDS into CF8)
- F18-F22, F24-F25 silent except sites (FOLDS into CF4)
- F23 `_on_practice_done` (PICKED, must-fix)
- F26 missing diagrams (FOLDS into CF8)

## Critique reports

### Skeptic
KILL list: K1 (RAClient split deferred), K2 (invalidate_combo dead-known), K3 (Mesen-era docstring nit), K4-K5 (protocol/models tests as regression-guards — but VERIFY judged them trivial enough to drop), K6 (Mode duplication by design — but PARTIALLY DEBUNKED, V10 confirms two real definitions), K7 (poller `on_event` Any unavoidable — DEBUNKED by CF1), K8 (api.ts/model-api.ts split unverified), K9 (route mock tests are pyramid-correct), K10 (DIAGRAMS.md just shipped — fair point).
OVERSTATED: O1 SessionManager line count not god-object (kept partial), O2 scheduler lock no real concurrency (CONFIRMED V3), O3 two-Schedulers might be intentional (PARTIALLY: V1 confirms stateless), O4-O5 stylistic, O6 cold_fill/fill_gap rule-of-three (CONFIRMED V17), O7 SMW fields acceptable, O8 mode-mutation race (DEBUNKED V2), O9 cold_fill cascade designed, O10-O11 not-yet-flaky (CONFIRMED V23), O12 type-debt 12→1 item (FOLDED into CF1), O13 observability 25→5 themes (FOLDED into CF4+CF8), O14 task.exception nit (DEBUNKED V19 — F23 actually correct).
MISSED: M1 Mesen/Lua grep (DEBUNKED V31 — done), M5 frame-overwrite test (deferred), M6-M7 retroarch.cfg drift checks (real but out-of-scope), M8 pending_start timeout (PICKED, V29 confirms gap), M14 frozen dataclasses (PICKED, V28 confirms gap), M16 db reset confirmation (V30: command not found — docs-or-code gap).

### Dreamer
REFRAME: R1 delete SessionManager (DROPPED — not yet earned), R2 delete RAClient wrapper (DEBUNKED V5/V32), R3 one global Scheduler (DROPPED — currently stateless, V1), R4 codegen Python from one schema (FOLDS into CF1), R5 delete bottom half of tests (partially picked), R6 structured logging adoption (FOLDS into CF4), R7 event bus (DROPPED — overkill for single-process), R8 vulture sweep (could do later).
NEW BIGGER: N1 delete protocol events (DROPPED V33), N2 commit to SMW or extract GameAdapter (DROPPED), N3 per-feature repositories (DROPPED), N4 ship Kalman (out of scope), N5 eliminate asyncio.sleep from tests (partially via CF1), N6 update DIAGRAMS.md (FOLDS into CF8), N7 drop reference/ (user policy), N8 cross-language fixtures (out of scope), N9 No-Game-state-shouldn't-exist (Andrew already flagged separately), N10 delete xfail tests (out of scope).

### Convergence hunter
CF1 typed events — PICKED.
CF2 explicit construction wiring (B1-B4 + A7) — DEFERRED (lower priority than CF1/CF4/CF8).
CF3 recording state machine ownership (A10/A11/B5/B6/A9/B10) — MOSTLY DEBUNKED (V4 says asymmetry gone; V2 says no race; B6 designed). Remaining bits fold into nearby.
CF4 structured errors — PICKED.
CF5 encapsulation continuation — Andrew's explicit backlog item; not picked this session per "no upfront over-engineering" preference.
CF6 test cleanup — partially picked (trivial-test deletes + bound tightening).
CF7 SMW separation — DROPPED single-game.
CF8 observability charter — PICKED.

### Verify
V1 high (two Schedulers but stateless), V2 high-but-debunked (no race), V3 high (lock guards init only), V4 DEBUNKED (no asymmetry), V5 high (RAClient 480 lines substantive), V6-V12 all high confirms, V13 dead confirmed, V14 dead confirmed, V15-V22 confirmed, V23 DEBUNKED (sleeps not flaky), V24 wiring-only confirmed, V25-V27 trivial confirmed, V28-V29 gaps confirmed, V30 db reset CLI not found, V31 DEBUNKED (no Mesen/Lua residue), V32 orchestrator goes through RAClient, V33 events in-process only, V34 no structured logging at all, V35 Bundle 3 migration complete.
