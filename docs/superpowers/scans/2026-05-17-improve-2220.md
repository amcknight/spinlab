---
date: 2026-05-17
focus: "tests"
git_head: fd932187ac1255453da49c2a8777627a32981ea1 (at scan start)
lenses_run: [architect, control-inversion, test-skeptic, dead-code, types, observability]
critiques_run: [skeptic, convergence]
raw_findings: ~33
merged_findings: 31
picked: 8 (CF-2 6 items + CF-3-obs 2 items, on `improve/test-infra-helpers-and-typed-fixtures`)
status: full_scan
---

## Top wins

### must-fix
*(none)*

### high-leverage
- **CF-1** — Extract `dashboard_harness` helper + shared typed `wait_for(predicate, *, name, timeout)` utility for the integration test infra. Absorbs A1 (`tests/integration/conftest.py:188-258` + `:431-546` duplicated dashboard-startup), A3 (no unit-level analogue), A6 (`tests/unit/integration/` reaches into 19 private conftest fns), OB1 (`run_scenario` timeout drops scenario name), OB4 (`_wait_for_dashboard_state` timeout drops predicate name), OB8 (`_wait_for_replay_mode` can't disambiguate mode-stuck from frame-stuck). Shrinks the 871-line conftest naturally. — size: **medium** — handed off to writing-plans this session.
- **CF-4** — Constructor-inject Scheduler / ConditionRegistry. Absorbs CI1 (`practice.py:47` self-Scheduler with DB IO in ctor), CI8 (`reference.py:100` self-ConditionRegistry), CI3 (`test_session_manager.py` mutates `sm.capture.recorder.*` private fields), CI4 (hand-builds MagicMock sessions on `sm.practice_session`), A5 (`test_dashboard_integration.py:74` TODO(Task 8) becomes resolvable once tests can swap dependencies through a clean seam). — size: **medium** — NOT picked this session; queued as next medium after CF-1.

### convergent win
- **CF-2** — Typed test-fixture vocabulary. Absorbs TY1 (sent_commands list elt), TY2 (send_command cmd type), TY3 (make_seg_with_state hint), TY4 (estimator fixture hints), TY5 (make_capture_session hint), TY6/M2 (test_cold_fill local emu MagicMock duplicated mock_emu), CI7 (test_practice_coverage _make_tcp duplicates conftest mock_emu — flagged but NOT included in this commit to keep the batch narrow). — size: **trivial** — shipped in commit `57f3043`.
- **CF-3-obs** — Surface silent attempt-loop exits with one log line each. Absorbs OB2 (`practice.py:228-237` segment timeout silently drops `_result_data=None`), OB3 (`speed_run.py:208-214` queue-get timeout spin without progress signal). — size: **trivial** — shipped in commit `09edb56`.

### nearby cleanup
- **TY7** `tests/integration/conftest.py:544` — `replay_ra_dashboard` yields untyped tuple `(base_url, db, tmp_path)`. Adding TypedDict would over-engineer a 2-tuple boundary; defer until a consumer destructures differently. — size: trivial (deferred)
- **TY13** `state_paths.py:39` — `segment_id_for_event(event: Any)` → union of `LevelEntranceEvent | CheckpointEvent | SpawnEvent` — size: trivial
- **TY14** `timing.py:80,106,114` — `_end_type: str` → `Literal["checkpoint","goal"]`; same root as carry-over **M5** EndpointType propagation — size: trivial
- **OB5** `dashboard.py:35-41` — event-loop `logger.exception("Error in event loop")` lacks event_type / mode / game_id context — size: trivial
- **OB6** `orchestrator.py:183-186` — `disconnect()` swallows RAClient cleanup at `logger.debug`; keep level but include exc class name — size: trivial
- **OB7** `raclient.py:310-313` — `cur_game = "<get_status failed>"` placeholder hides exc type/message — size: trivial
- **CI6** `wiring.py:96-122` — `on_event=lambda ev: None` placeholder rebound after orchestrator construction (verified-high). Fragile but works; no concrete failure mode. — size: trivial
- **TS1** `tests/unit/db/test_db_capture_sessions.py:123` `time.sleep(0.01)` for distinct created_at — singleton, low flake risk; skeptic killed — size: trivial
- **A4** `tests/unit/test_session_manager.py:1-10` vague docstring — skeptic killed — size: trivial

### Carry-overs (already backlogged / declined)
- **CF-β** typed AppState + DB rows — in flight plan; absorbs CI5, TY8-11, TY12
- **CF3** exception-context helpers — in flight; partially overlaps OB5 framing but disjoint line scope
- **CI2** Poller default detector — VERIFY confirmed Bundle 2 already shipped detector DI on 2026-05-12
- **M4** skipif audit (skeptic-MISSED) — VERIFY confirmed clean; only 4 valid Windows-platform skips in the entire tree
- **M6** `pytest_runtest_makereport` setup-phase coverage — VERIFY confirmed the RAHarnessLaunchError special-case at `:854` is the intended coverage; other setup-phase failures fall through to pytest's normal reporting (acceptable)
- **poller.py:42-44** gravestone — user declined repeatedly

## Picked this session
- **CF-2** (trivial, convergent) → inline edit on branch `improve/test-infra-helpers-and-typed-fixtures`, commit `57f3043` "tests: type fixture vocabulary + collapse duplicate emu fixture"
- **CF-3-obs** (trivial, convergent) → inline edit on same branch, commit `09edb56` "practice/speed_run: surface silent attempt-loop exits"
- **CF-1** (medium, high-leverage) → handed off to `superpowers:writing-plans` on the same branch

## Dropped during critique
- **K1 — TS1** (`time.sleep(0.01)` for ordering) — self-debunking ("low flake risk"); the sleep is the correct primitive at sqlite's created_at resolution; alternative (mock clocks) is strictly worse
- **K2 — A4** (test_session_manager docstring vague) — finding admits "not necessarily wrong"; lowest-stakes cleanup imaginable
- **K3 — CI6** (wiring.py placeholder lambda) — verified-high but acceptable late-bind pattern; no concrete failure mode named
- **K4 — CI2** (Poller default detector) — Bundle 2 commit `2a94a61` on 2026-05-12 already added detector DI; the fallback is API choice, not coupling smell
- **K5 — CI5** (StateBuilder reads SM private fields) — CF-β scope, in-flight plan
- **K6 — TY12** (allocators duck-typed row.get) — CF-β scope, in-flight plan

## New finds from VERIFY pass
- **N1** `tests/conftest.py:137-143` (`make_seg_with_state`) is called for side-effects only at `test_session_manager.py:295` (return value not captured) — fixture is fine, just noting one call site doesn't need the return
- **N2** `tests/integration/conftest.py:606-628` dashboard-startup failure formatter omits `base_url` and the predicate name — same pattern as OB4; if multiple dashboard fixtures fail in parallel the error is ambiguous (folded into CF-1)
- **N3** `tests/integration/conftest.py:418-428` `run_scenario` 30s `wait_for` is hardcoded with no per-test override AND loses scenario identity in the exception (folded into CF-1 / OB1)
- **N4** `wiring.py:99` — placeholder `on_event=lambda ev: None` silently drops events if rebinding at `:121` is skipped or fails (CI6 follow-up; not picked)
- **EndpointType StrEnum interaction** — typing `start_type: str` on `make_seg_with_state` exposed 4 latent pyright errors in the body's calls to `Waypoint.make(endpoint_type=...)` and `Segment(start_type=...)`. Reverting those two params to untyped restored baseline. A real fix would migrate the helper + its 11 callers to `EndpointType`; that's the **M5** carry-over.

## Patterns observed by lenses
- **Architect** (and Convergence-hunter): the 871-line integration conftest is a symptom, not the disease. Two duplicated dashboard fixtures + a unit-test directory that imports 19 private fns from it create the bloat. Extracting `dashboard_harness` + a non-private path-resolver module (per CF-1) shrinks the file naturally and breaks the leaky-import pattern.
- **Control-inversion**: `test_session_manager.py` is the canary for self-construction pain — CI3 (private-field mutation) and CI4 (hand-built MagicMock sessions) both exist because `PracticeSession.__init__` constructs its own Scheduler and pulls DB. Once CF-4 lands, the test cleanups become trivial.
- **Test-skeptic**: returned only 1 finding (TS1) for a tests-focused scan — confirms the prior 2026-05-17-1649 conclusion that the test suite has absorbed most ceremony-removal critiques already. Genuine gap is hostile audit of new test infra (the CF-1 helpers) BEFORE they bloat.
- **Dead-code**: zero findings; recent sweeps have kept hygiene tight.
- **Types**: the boundary between test fixtures and production types remains the highest-density gap. CF-2 chipped at the test side; CF-β + M5 will close the production side.
- **Observability**: timeout-without-identity is the recurring shape. CF-1's typed `wait_for(predicate, *, name, ...)` is the structural fix; CF-3-obs landed the two highest-value point-fixes ahead of the structural one.

## Full merged list (collapsed)

Architect (6): A1 dashboard-fixture dup, A2 871-line conftest, A3 no unit-level dashboard fixture, A4 test_sm docstring (KILLED), A5 TODO(Task 8) in seeded_db, A6 unit/integration imports 19 private fns.

Control-inversion (8): CI1 PracticeSession self-Scheduler, CI2 Poller detector default (DEBUNKED — Bundle 2 done), CI3 test_sm mutates recorder internals, CI4 test_sm hand-builds mock sessions, CI5 StateBuilder reads SM private (in CF-β), CI6 wiring placeholder lambda (verified, kept defensible), CI7 test_practice_coverage _make_tcp dup of mock_emu, CI8 ReferenceController self-ConditionRegistry.

Test-skeptic (1): TS1 time.sleep(0.01) (KILLED — self-debunking).

Dead-code (0).

Types (10): TY1-TY7 test-fixture types (PICKED as CF-2 batch — TY7 deferred), TY12 allocators duck-typed (in CF-β), TY13 state_paths.Any, TY14 timing._end_type Literal.

Observability (8): OB1 run_scenario timeout (in CF-1), OB2 practice silent exit (PICKED), OB3 speed_run silent loop (PICKED), OB4 _wait_for_dashboard_state predicate (in CF-1), OB5 dashboard event-loop context, OB6 orch.disconnect debug, OB7 raclient get_status placeholder, OB8 _wait_for_replay_mode disambiguation (in CF-1).

## Critique reports

**Skeptic verdict**: KILL 6 (TS1, A4, CI6, CI2, CI5, TY12). OVERSTATE 6 (A2 — pytest conftests normally mix concerns; A6 — coupling between thing and its tests is the point; A3 — severity needs call-site count; CI3+CI4 — same complaint; OB6 — debug is correct level for shutdown disconnect; OB7 — nested-diagnostic context). MISSED 8 (M1 test redundancy, M2 EmuBackend Protocol typing for fakes, M3 factory call-site audit, M4 skipif audit, M5 OB4+OB8 same shape, M6 makereport setup coverage, M7 ADDR_MAP dead entries, M8 full sleep inventory).

**Convergence-hunter verdict**: 5 fixes — **CF-1 dashboard_harness + wait_for (PICKED — writing-plans)**, **CF-2 typed test-fixture vocabulary (PICKED — shipped)**, **CF-3-obs structured timeout logs (PICKED — shipped, scoped to OB2+OB3)**, **CF-4 constructor injection (queued)**, **CF-5 typed event/row vocabulary (subsumed by CF-β)**.

**VERIFY verdict**: all 22 anchors confirmed high-confidence including TY7 line-shift confirmation. K4 (CI2) directly confirmed via commit history. M4 (skipif audit) returned cleanly — only 4 valid Windows skips. M6 makereport coverage acceptable. N4 wiring placeholder is a real latent risk but no observed failure mode.
