# /improve scan — 2026-05-27

Run while Andrew was AFK, right after the death-distribution arc (is_hot +
bootstrap-resample + hazard plot) landed on local main. 6 lenses → 1 focused
verify pass (formal 2-agent critique compressed into the verifier given the
AFK context). Branch: `improve/contract-and-type-tightening`.

## Top wins

### must-fix
*(none — the one candidate correctness bug, "two Scheduler instances", was debunked in VERIFY.)*

### high-leverage
- **V5 — `require_game()` leaks FastAPI into the domain layer** — `session_manager.py:172`. Raised `HTTPException(409)` directly, bypassing the existing `ActionError`→`create_app` boundary handler (`errors.py`, `dashboard.py:146`). Fix: `NoGameLoadedError(ActionError, 409)` + raise it. — size: **trivial-small** — SHIPPED 934f4c1.

### nearby cleanup (verified, trivial)
- **V8a** — `session_manager.py:158` `get_scheduler()` missing `-> Scheduler`. — SHIPPED 934f4c1.
- **V8b** — `state_builder.py:135` `_build_practice_state` untyped `sched` param → `sched: Scheduler`. — SHIPPED 934f4c1.
- **PRE — `detect_finish` return `str | None` → `LevelExitGoal | None`** — `retroarch/predicates.py:78`. Last genuine production pyright error outside the vendored shim noise. — SHIPPED 11dbe28 (made before the scan, folded into this branch).

### needs-your-call (NOT auto-shipped — design/risk judgment)
- **V2 — estimator `from_dict` uses `.get(key, default)` on contract fields** — `death_aware_rolling.py:67`, and identically in `bootstrap_resample.py:175`, `exp_decay.py:104`, `kalman.py:78`, `rolling_mean.py:28`. This *looks* like the "no defensive-`.get` on contract fields" anti-pattern, BUT it is a **pervasive deliberate convention** across all 5 estimators, and kalman/exp_decay default ~25 fields to *meaningful* non-zero values — i.e. tolerant deserialization for schema evolution at a persistence boundary. `load_mature_states` already catches `KeyError` and logs "skipped corrupt estimator state"; the `deserialize` path (scheduler.py:264) does not. Decision needed: keep tolerant convention, or move to strict-index + a documented "corrupt state" surfacing. Not safe to flip unilaterally (could break loading of existing persisted rows). — size: **medium** (touches 5 files + the deserialize error path).
- **V7 — `NetworkConfig.port` dead from Python's POV** — `config.py:13`. Written to `.spinlab-ports` as `tcp_port`, no Python reader. CONTRADICTION: the 2026-05-19 scan claimed the AHK launcher reads only `dashboard_port`/`vite_port`; this run's verifier said the launcher *does* read `tcp_port`. Must confirm `scripts/spinlab.ahk` before removal — not safe unattended. — size: trivial-if-confirmed.
- **V8c — `SegmentCommand.end_type: str` → `Literal`** — `models.py:230`. Part of the known-deferred `EndpointType` narrowing family (call-site ripple); left with that batch rather than piecemeal.

### Carry-overs (plans exist, NOT executed — confirmed still open)
- **Session-lifecycle facade** — routes mutate `session.mode`/`session.scheduler` + call private `_clear_ref_and_idle` (`routes/system.py:79,91,96`); `StateBuilder` reaches into `session.capture.is_recording`/`active_run_id`. Plan: `docs/superpowers/plans/2026-05-23-session-lifecycle-facade.md`. Re-surfaced by 3 lenses again.
- **Save-state error path → durable failure record** — `capture/reference.py` + `cold_fill.py`. Plan: `docs/superpowers/plans/2026-05-23-save-state-error-path.md`.
- **Mode 3 RA crash quarantine** — plan: `docs/superpowers/plans/2026-05-19-mode3-cascade-quarantine.md`.
- **Scheduler/ConditionRegistry constructor injection** — `practice.py:58` builds own `Scheduler`; `start_practice` reads `death_penalty_ms` from `capture.condition_registry`. Recurring "next medium."
- **EndpointType narrowing at DB row boundary** — recurring; absorbs V8c.

## Picked this session
- **V5** require_game → NoGameLoadedError → inline → commit 934f4c1
- **V8a/V8b** type annotations → inline → commit 934f4c1
- **PRE** detect_finish LevelExitGoal → inline → commit 11dbe28

(All trivial/small. No medium/big picked → no writing-plans handoff this session.
Branch left for review; not merged while AFK.)

## Dropped during VERIFY (debunked / hallucinated)
- **DEBUNKED — "two Scheduler instances cause stale weight/estimator mid-session."** `scheduler.pick_next()` calls `_sync_config_from_db()` every pick (scheduler.py:146 / :131-138), so a running PracticeSession DOES pick up dashboard weight/estimator changes immediately. No bug.
- **HALLUCINATED — `_events_from_rows`/`_attempts_from_rows` duplicated in estimators/__init__.py and scheduler.py.** Only defined in `scheduler.py`; `routes/model.py:25` imports them. No duplication.
- **DEBUNKED — `_snapshot_expected_times` silently skips missing state files.** It logs once per segment via `log.warn` + dedupes through `self._missing_state_paths` (practice.py:117-123). Already correct.
- **DROPPED (house rule) — `scheduler.py:156` silent `None` when picked segment absent.** Possible only with a buggy allocator (allocators pick from the passed list); guarding a can't-happen path violates "don't add handling for scenarios that can't happen."
- **PARTIAL — trivial tests.** `test_transition_state.py:5-18` reset() test is removable-but-harmless; `test_factories.py` (is_hot default) and `test_attempts_conditions.py` (invalidated flag persist) are real regression coverage — keep. Net: not worth a removal commit.

## Lens patterns (archival)
- **Routes as mutators, not askers.** Multiple lenses independently flagged routes assigning `session.mode`/`session.scheduler` and calling private session methods. This is the session-lifecycle-facade carry-over; it keeps re-surfacing because it's real and unaddressed.
- **Lazy-init layering.** SessionManager double-checked-locks Scheduler; PracticeSession builds its own; StateBuilder pulls via `get_scheduler()` in the request thread. Looks alarming but the DB-resync-per-pick makes it behaviorally safe (see debunk above). Eager construction at game-load would still simplify reasoning.
- **`-> dict` at internal boundaries.** `get_state()`, `get_priors()`, `set_allocator_weights(body: dict)`, `build() -> dict` underspecify shapes that are deterministic. Real but low-urgency; the AppState/api_schemas source-of-truth already exists, so tightening is mechanical when someone's in the area.
- **Clean migration & dead-code hygiene.** Dead-code lens found essentially one item (`NetworkConfig.port`) — the Mesen→RA migration was executed cleanly. Codebase is in good shape; this scan's honest verdict is "mostly fine, a few sharp edges."
