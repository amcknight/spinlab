---
date: 2026-05-15
focus: "the improve skill itself; investigate the skill's usage history (permission calls etc.)"
git_head: fa7cd3e705b3b45b1ae043075cf4653f73234045
target: C:/Users/thedo/.claude/skills/improve/ (4 files, ~20 KB total)
lenses_run: [inline — see note]
critiques_run: [skeptic, convergence — inline]
raw_findings: 18
merged_findings: 13 (after dedup against scan-history evidence)
picked: 3 high-leverage/convergent + 4 nearby cleanup, all trivial
status: full_scan_adapted
---

## Note on methodology

Target was 4 markdown files (SKILL.md, references/lenses.md, references/critiques.md, references/ranking.md). Mechanically dispatching 6 parallel Explore lenses over ~20 KB of prose would have been process theater. Applied the 6-lens framework **inline** against three evidence sources:

1. The skill's own text (read directly).
2. The 7 prior scan output files (`docs/superpowers/scans/2026-05-13..15-*.md`) — the skill's "test data."
3. The session transcript investigation (one general-purpose agent dispatched against the 9 `.jsonl` sessions that ran `/improve`).

Critique (skeptic + convergence) and verify were also inline. Verify reduced to re-reading the skill text against claim anchors, since the target is in the orchestrator's own context.

## Top wins

### must-fix
*(none — skill works; this is iteration)*

### high-leverage

- **HL-1** [`references/lenses.md`](C:/Users/thedo/.claude/skills/improve/references/lenses.md) — Anti-hallucination clause for lens prompts. ≥4 hallucinated debunks across 7 scans (V31 Mesen residue, C10.3 bare-except, A13 memoization race, V14 StateBuilder async). SHIPPED. — size: trivial
- **HL-2** SKILL.md PREP — Carry-over/declined registry. `poller.py:42-44` gravestone surfaced 4 consecutive scans; CF1'/CF8' carry-over markers ad-hoc. — size: medium. **NOT PICKED** (Andrew: "I don't mind the repeats yet").

### convergent win

- **CV-1** [`SKILL.md` Phase 4](C:/Users/thedo/.claude/skills/improve/SKILL.md) — Resilience stanza: poll-budget + inline-fallback if dispatched agents vanish; surface to user before grinding through repeated 5xx. Absorbs: critique-agents-vanished (session `0c4fb292`), 529 storm (session `3558ea3b`). SHIPPED. — size: trivial
- **CV-2** [`references/ranking.md`](C:/Users/thedo/.claude/skills/improve/references/ranking.md) — Scan template tightening. Replaces vague `findings_count` with `raw_findings` / `merged_findings` / `picked`. Forbids saving scans with `Picked this session: TBD` (seen in 2026-05-14-improve-1735.md). Adds Patterns-observed-by-lenses section so lens-mandated "## Patterns" output has a home. Clarifies that Top Wins bullets stay terse (no multi-paragraph convergent-fix synthesis). Absorbs: format drift, TBD persistence, branch-slug ambiguity, present-vs-rank bleed. SHIPPED. — size: trivial

### nearby cleanup

- [`references/critiques.md:10`](C:/Users/thedo/.claude/skills/improve/references/critiques.md) — Dreamer gravestone paragraph. Kept as honest history (per `feedback_terse_intermediates` — keep the "why we dropped X" record).
- [`SKILL.md:15`](C:/Users/thedo/.claude/skills/improve/SKILL.md) — Simplified MEMORY.md fallback paragraph from 3 sentences of path-encoding trivia to one line. SHIPPED.
- [`references/lenses.md`](C:/Users/thedo/.claude/skills/improve/references/lenses.md) — Added "few findings is fine" line so lenses stop padding. SHIPPED.
- [`references/lenses.md`](C:/Users/thedo/.claude/skills/improve/references/lenses.md) — Clarified that "## Patterns I noticed" output goes to scan-file archival, not the merged findings list. SHIPPED.
- [`SKILL.md:107`](C:/Users/thedo/.claude/skills/improve/SKILL.md) — Medium handoff to writing-plans now points at scan-file Top Wins entry instead of duplicating the spec inline. SHIPPED.
- [`SKILL.md:102`](C:/Users/thedo/.claude/skills/improve/SKILL.md) — Branch slug rule rewritten: derive from dominant theme across picks (with concrete example), not literal first-picked-item. SHIPPED.

## Picked this session

- **HL-1, CV-1, CV-2, and all SHIPPED nearby-cleanup items** → handed off to: **inline edits** on the skill files at `C:/Users/thedo/.claude/skills/improve/`. No branch (skill dir is not under version control — verified `Test-Path .git` returned false at both the skill dir and `~/.claude/` root).
- **HL-2** (carry-over registry) → **deferred** at Andrew's call. Re-eligible next scan.

## Dropped during critique

- **S6 — focus arg propagation weak**. Scan history shows `F38 "out of scope: touches python/spinlab/"` got dropped during critique. Skill self-corrects. Downgraded; no action needed.
- **S15 — verify bonus pass adds work**. Empirically earns its keep across all 7 scans by catching hallucinations and finding new issues. Not a bug.
- **S9 — Dreamer gravestone in critiques.md**. Honest history (the paragraph documents why an experiment was dropped). Keep.
- **S16 — skill is project-conventions-coupled** (`docs/superpowers/scans/`, "Andrew" implicit). Solo dev tool; not a real concern.
- **S18 — branch-switch observability gap**. Orchestrator reports the branch; minor.

## New finds from VERIFY pass

None beyond the inline lens findings — the surface is small and was directly verifiable against the open files.

## Patterns observed by lenses

- **Lens hallucinations cluster around behavioral claims** (race conditions, swallowed exceptions, unused fields) more than structural claims. The anti-hallucination clause should target *those* specifically — but a general rule is cheaper than a typed taxonomy.
- **Carry-overs reveal what /improve undervalues**: nearby-cleanup items with no advocate inside the picker design. Today's design says "do them when you're in the area" but the picker never goes to that area on a /improve session. The natural fix is the carry-over registry (HL-2) — deferred.
- **Critique-pass debunks are the most useful signal** for tuning lens prompts. Future improvement: pipe debunks back into a lens-prompt feedback loop (out of scope now).
- **Phase 9 handoff trees get deep**: /improve → writing-plans → subagent-driven-development → finishing-a-development-branch in some sessions. Each Skill invocation re-injects multi-KB skill bodies into context. Not directly fixable here, but worth noting that the rerunnable-skill principle matters more as chains lengthen.

## Full merged list (collapsed)

18 raw findings → 13 after dedup against scan-history evidence:

- S1 carry-over registry (→ HL-2, deferred)
- S2 critique-agent fragility (→ CV-1)
- S3 5xx storm has no fallback (→ CV-1)
- S4 lens hallucinations (→ HL-1)
- S5 picker capacity vs trivial batch (→ CV-2 + verified already documented)
- S6 focus arg propagation weak (DROPPED — self-corrects)
- S7 branch naming for multi-pick (→ CV-2 + SKILL.md:102 edit)
- S8 scan-format drift (→ CV-2)
- S9 Dreamer gravestone (DROPPED — keep)
- S10 PRESENT-vs-RANK bleed (→ CV-2)
- S11 TBD persistence (→ CV-2)
- S12 MEMORY.md fallback prose (→ nearby cleanup, SHIPPED)
- S13 lenses don't pad-stop signal (→ nearby cleanup via lenses.md edit)
- S14 MERGE ignores Patterns sections (→ nearby cleanup via lenses.md edit)
- S15 verify bonus pass adds work (DROPPED — earns keep)
- S16 project-conventions coupling (DROPPED — solo tool)
- S17 writing-plans context duplication (→ nearby cleanup, SHIPPED)
- S18 branch-switch observability (DROPPED — minor)

## Critique reports

Skeptic and convergence ran inline against the 18-item merged list. Key calls:

**Skeptic KILLs:** S6 (lenses self-correct via critique), S9 (intentional gravestone), S15 (empirically valuable), S16 (YAGNI single-user), S18 (orchestrator reports already).

**Skeptic OVERSTATEMENTS:** S12 (mild noise but documented for unusual case — kept, simplified).

**Skeptic MISSED:** nothing new beyond the 18 (target is small; nothing got past).

**Convergence:**
- CV-1 (resilience stanza) absorbs S2 + S3 — single SKILL.md paragraph, two friction modes.
- CV-2 (template tightening) absorbs S5 + S7 + S8 + S10 + S11 — single ranking.md template edit + one SKILL.md branch-slug edit.

**Anti-convergence:** HL-1 (anti-hallucination) is independent of CV-1/CV-2 — pure lens-prompt change. HL-2 (carry-over registry) is independent of everything (data structure introduction).

**Verify:** inline against open files; all 13 surviving claims confirmed verifiable from open editor state.
