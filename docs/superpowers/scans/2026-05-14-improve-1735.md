---
date: 2026-05-14
focus: "test usefulness for agent development"
git_head: 1c2451676f7776c483fc80d86da4776e5129932b
lenses_run: [architect, control-inversion, test-skeptic, dead-code, types, observability]
critiques_run: [skeptic, convergence]
findings_count: 56
status: full_scan
---

## Top wins

### must-fix

- **Emulator integration skips violate CLAUDE.md** — `tests/integration/conftest.py:85-88` (`skip_no_love_yourself` skipif on missing `SPINLAB_TEST_ROM`) + `pytest.skip()` at lines 289, 303, 329, 343. CLAUDE.md explicitly forbids the env-var-missing skip pattern; the rule clause "an env var the harness needs (e.g. `SPINLAB_TEST_ROM`) — is a failure, not a skip" applies directly. Andrew has corrected this pattern twice. — size: **medium** (design choice: hard-fail vs bootstrap-env vs Andrew-accepted markers).

### high-leverage

- **Diagnostic hook only fires for `replay_ra_dashboard`** — `tests/integration/conftest.py:536` literally `for fixture_name in ("replay_ra_dashboard",)`. Every `run_scenario` test (transitions, crash-recovery — bulk of emulator coverage) gets an empty diagnostic block on failure. Verifier confirmed: run_scenario yields a callable, replay_ra_dashboard yields `(base_url, db, tmp_path)` — shapes don't match, so fix is reshape-or-branch, not one-liner. Also missing from the block: poller stats (poll_count, `_read_failing`), state_version, orchestrator task health, cold-fill queue state, timing state, NCI consecutive-timeout count, RA process status. — size: **medium**.
- **Untyped `/api/state` and paused-run envelopes** — `state_builder.py:30` returns bare `dict`; `AppState` already exists in `api_schemas.py:122+`. `capture/reference.py:183` `get_paused_state() -> dict | None` shapes a `PausedRunState` that doesn't exist as a model yet. Annotation + one new model. Pyright catches no key typo today. — size: **medium**.

### convergent win

- **CF-DOCSTRING-SWEEP — test-archaeology + missing invariant docstring** — single trivial pass: replace "Task N / Phase E / 4928f1c" archaeology in test docstrings with one-line concrete context (or delete); clarify "Legacy fallback" naming → "regression guard"; strip BSV+SAVE_STATE / C99 commentary from `tests/fixtures/love_yourself/one_level.json:2`; rename `_make_tcp()` post-migration → `_make_mock_emu()`; add a docstring to `python/spinlab/capture/recorder.py` `SegmentRecorder.clear()` recording the per-session-in-memory invariant currently only in `tests/unit/capture/test_recorder.py:89-91` comments. Absorbs: B1, B2, B3, B4, B5, B6, B7, M1. Anchors: `tests/unit/retroarch/test_movies.py:137`, `tests/unit/capture/test_finalizer.py:3-5`, `tests/unit/test_dashboard_integration.py:70-74`, `tests/unit/routes/test_dashboard_references.py:191,235,249`, `tests/integration/test_replay_fixture.py:7`, `tests/fixtures/love_yourself/one_level.json:2`, `tests/unit/test_practice_coverage.py:17-22`, `python/spinlab/capture/recorder.py:66-74`. — size: **trivial**.

### nearby cleanup

- A1 (`_make_seg_with_state` duplicated `tests/conftest.py:137-165` vs `tests/unit/test_segment_with_model.py:11-31` with divergent cold/hot variant logic). size: trivial when next touched.
- A2 (`_make_sm()` inlined in 3 files: `tests/unit/test_state_builder.py:13`, `tests/unit/test_replay.py:34`, `tests/unit/test_session_manager_conditions.py:24`). size: trivial.
- A4 (`FakeEmuBackend` lives in `tests/conftest.py:88-135` while sibling fakes are in `tests/fakes/`; verifier confirms no docstring justifies the conftest location). size: trivial.
- A5 (no medium-scale "1 game + segments, no attempts" DB fixture between empty and fully-seeded). size: trivial when next needed.
- A7 (`tests/unit/capture/conftest.py:39` calls private `ctl._enter_recording()` instead of `start_reference()`). size: trivial.
- I1 (`session_manager.py:93-97` eagerly constructs 5 controllers in `__init__` — verifier confirmed present). size: medium if pursued, but no demonstrated bug.
- I3 (`practice.py:62` `self._result_event = asyncio.Event()` binds to current event loop at construction). size: trivial-medium, theoretical risk.
- F1-F5 mock-call assertions / private-method tests — skeptic correctly noted these are appropriate at unit-test layer; only F5 segment_label exact-match is borderline.
- E1/E2 bare-assert sweep in `tests/integration/test_transitions.py` — skeptic correctly noted test names carry scenario context; busywork.
- H1/H2/H3 session-scoped fixtures `fake_dashboard_server` / `fake_game_loaded` / `replay_ra_dashboard` — by design for speed.
- L1 wide stat bound `tests/unit/allocators/test_mix_allocator.py:40-51` (~6.3σ at 1000 picks 80/20) — carry-over from prior scan, low value.
- N1-N7 production logging gaps — largely shipped by recent CF4 / Bucket A/B / poller sweeps.
- G1-G3 event-ordering tests in detector — low priority absent a real bug.

## Picked this session

- **C1-C3 (must-fix, medium)** — Hard-fail emulator skip path (raise RuntimeError instead of `pytest.skip` / `skipif`) → plan saved to `docs/superpowers/plans/2026-05-14-rom-keyed-harness-hard-fail-skips.md`. Scope grew during plan-writing: also introduces a ROM-keyed factory with `Toothpaste.smc` + `Love Yourself.smc` as the two registered ROMs (Andrew's call: "develop for at least two ROMs to keep the muscle", 2-5 expected long-run).
- **D1+D2 (high-leverage, medium)** — Diagnostic hook expansion for `run_scenario` tests + richer field set (mode, state_version, poller poll_count / _read_failing, RA proc + NCI status) → plan saved to `docs/superpowers/plans/2026-05-14-diagnostic-hook-expansion.md`.
- **CF-DOCSTRING-SWEEP (convergent, trivial)** — **Dropped this session.** Inline commit was gated on a clean pytest baseline (Andrew's `feedback_red_baseline_habit` rule); baseline ran 855 passed + 11 skipped because the fragile auto-ROM-picker fed `'the.smc` (deep-freezes) into `ra_harness`. Cleaning skips requires the C1-C3 plan to execute first. The sweep items (B1-B7 + M1) remain captured above in `### convergent win`; a future /improve cycle (or a follow-on commit after C1-C3 lands) can pick them back up.
- Branch: `improve/loud-fail-emulator-skips`

## Dropped during critique

- A4 framing as "asymmetry without good reason" — verifier confirmed asymmetry is real (FakeEmuBackend in conftest, siblings in `tests/fakes/`) but downgraded to nearby cleanup; no docstring justifies the location either way.
- K1 (no test for missing-state-file behavior) — debunked: `tests/unit/test_scheduler_kalman.py:200-235` `pick_next_skips_missing_state_files` exists and is end-to-end.
- K2 (no save_state TimeoutError test) — skeptic cited `tests/unit/retroarch/test_raclient.py:160` `StateSaveTimeoutError` test; that covers SAVE_STATE only. K2's intended LOAD_STATE-missing-file gap survives but has no concrete anchor — left as low-priority residue.
- J9 (condition decode vs `event.conditions` shape boundary) — already resolved by commits `cfc5d72` + `aa77fbf`: decode returns typed `dict[str, str | bool]`, events carry `dict[str, int]`; boundary clarified.
- I4 / I5 — skeptic correct: two-phase callback binding via `wiring.py:96-122` IS the fix (placeholders then post-construction rebind), not the bug; `get_scheduler()` at `session_manager.py:159-171` is correctly memoized.
- Most of I1-I7 (DI items) — verifier confirmed I1 + I3 still present but no demonstrated bug; I4/I5 already addressed; others rejected by 2026-05-14 scan as YAGNI-single-game.
- A6/A11/B3/B4 parallel-session-hierarchies family — already downgraded by 2026-05-14 as YAGNI until a third session type lands.
- L1 (wide stat bounds) — skeptic's "0.006% false-fail rate, catches 0%/100% bugs" defense holds.

## New finds from VERIFY pass

- **`SegmentRecorder.clear()` has zero docstring** (`python/spinlab/capture/recorder.py:66-74`). Confirms M1: the per-session-in-memory invariant lives only in the test comment at `tests/unit/capture/test_recorder.py:89-91`. Folded into CF-DOCSTRING-SWEEP.
- **`PausedRunState` does NOT yet exist in `api_schemas.py`** — J3's typing fix requires adding the model, not just an annotation. Adjusts J3 handoff slightly upward but still medium.
- **`run_scenario` and `replay_ra_dashboard` fixtures yield incompatible shapes** — `run_scenario` yields a callable; `replay_ra_dashboard` yields `(base_url, db, tmp_path)`. CV2 (D1+D2 fix) cannot be a one-line extension of the existing diagnostic hook — either run_scenario must be reshaped to yield diagnostic-collectable state, or the hook must branch on fixture type. Bumps CV2 from "trivial" (convergence's claim) to genuine medium.

## Full merged list (collapsed by lens)

### Lens 1 — Principal architect
- A1 `_make_seg_with_state` dup (conftest vs test_segment_with_model)
- A2 `_make_sm` helper inlined across 3 files
- A3 `mock_emu` (MagicMock) vs `fake_emu` (FakeEmuBackend) naming asymmetry
- A4 FakeEmuBackend location asymmetry vs `tests/fakes/`
- A5 No "medium-scale" DB fixture
- A6 `practice_db._test_seg_id` underscore attrs used by tests
- A7 `reference_controller_recording` calls private `_enter_recording()`

### Lens 2 — Control inversion
- I1 SessionManager eager controller construction
- I2 PracticeSession constructs its own Scheduler
- I3 PracticeSession `asyncio.Event()` in `__init__`
- I4 Two-phase callback binding for MovieController / Poller (verified addressed)
- I5 `get_scheduler()` lazy-init (verified memoized correctly)
- I6 `start_practice` baked-in lambda couples PracticeSession to SSE
- I7 `create_app()` hardcoded composition

### Lens 3 — Test skeptic
- C1-C3 auto-skip masquerading as pass
- E1-E2 bare asserts in integration tests
- F1-F5 mock-call-only assertions, private-method coupling
- G1-G3 event ordering / weak collection asserts
- K1-K3 missing error-path coverage
- L1 statistical bounds too wide
- Mixed-concern tests; `_make_tcp()` naming

### Lens 4 — Dead code
- B1 `test_movies.py:137` "Task 1 refactor"
- B2 `test_finalizer.py:3-5` "Task 1.1 / 1.3"
- B3 `test_dashboard_integration.py:73-74` `TODO(Task 8)`
- B4 `test_dashboard_references.py:191,235,249` "Legacy fallback"
- B5 `test_replay_fixture.py:7` "Phase E option a"
- B6 `one_level.json:2` BSV / C99 commentary
- B7 `test_practice_coverage.py:17-22` `_make_tcp()` post-migration naming

### Lens 5 — Types & contracts
- J1 `SetConditionsCmd.definitions: list[dict]`
- J2 `state_builder.py:30` `build() -> dict`
- J3 `capture/reference.py:183` `get_paused_state() -> dict | None`
- J4 Estimator params bare `dict | None`
- J5 `# type: ignore[return-value]` cluster in db mixins
- J6 `routes/segments.py:31-41` `list[dict]` API response
- J7 `estimator_name` fixture no return type
- J8 FakeEmuBackend.sent_commands untyped element
- J9 condition raw vs decoded shapes (resolved)

### Lens 6 — Observability
- D1 Diagnostic hook scope (one fixture only)
- D2 Diagnostic block missing fields
- N1 poller read-failure no flake counter
- N2 orchestrator tick exception no state context
- N3 save_state retry no per-attempt log
- N4 `_find_current_replay_slot` returns None ambiguously
- N5 cold_fill save_state failure granularity
- N6 `_resolve_replay_path` None ambiguous
- N7 DIAGRAMS.md missing state-machine diagrams
- M1 Test-only invariant on `SegmentRecorder.clear()`

## Critique reports

Skeptic, convergence, and verify reports are preserved in the agent task transcripts at:
- `C:\Users\thedo\AppData\Local\Temp\claude\c--Users-thedo-git-spinlab\2e66c09c-fdb1-4b07-8cd5-acd926052236\tasks\a7c13bc22dcf7971a.output` (skeptic)
- `C:\Users\thedo\AppData\Local\Temp\claude\c--Users-thedo-git-spinlab\2e66c09c-fdb1-4b07-8cd5-acd926052236\tasks\a4a123b472030cb6f.output` (convergence)
- Verifier output inline in the orchestrator transcript.
