---
date: 2026-05-28
focus: "full sweep"
git_head: 29ced12
lenses_run: [architect, control-inversion, test-skeptic, dead-code, types, observability]
critiques_run: [skeptic, convergence]
raw_findings: ~26
merged_findings: 21
picked: 1
status: full_scan
---

Run while Andrew was AFK, right after the death-distribution arc (is_hot +
bootstrap-resample + hazard plot) and the cold-state Segments-tab indicator
landed on local main. The freshest surface (cold_distribution.py,
bootstrap_resample.py, hazard fields, has_cold_state) was the scan target; the
RA layer / session_manager have been scanned ~10 times and are stable.

Both critique agents (skeptic, convergence) were Explore agents with codebase
access, so their KILL/OVERSTATED verdicts came pre-anchored; a focused
self-VERIFY (direct reads of the pick-relevant files) confirmed them.

## Top wins

### must-fix
*(none)*

### high-leverage
- **F1 — `has_cold_state` silently false on DB/disk disagreement** — `routes/segments.py:43-45`. When a `cold` save-state row exists in the DB but its `state_path` file is gone on disk, `has_cold_state` returns False with no log. A moved/cleaned `spinlab_state_dir` makes the whole Cold column go ✗ with nothing in `spinlab.log`. Violates the project "no silent failures / honest data" principle. Fix: log a `warning` when DB has a path but `os.path.exists` is False; `has_cold_state` value unchanged. — size: **trivial** — SHIPPED this session (branch `improve/cold-state-disk-mismatch-log`).

### convergent win
*(none survived verify — the proposed "instrument the cold/bootstrap null path" (F2/F3/F5/F6) collapsed to marginal debug logs on documented nullable-output states; only F1 is a real DB/disk *disagreement* worth surfacing.)*

### nearby cleanup (verified real, low value — do when in the area)
- **A2** — `cold_distribution.py:31` re-declares `EFFECTIVE_WINDOW_HALFLIVES=5` (also in `death_aware_rolling.py:47`). A cross-reference comment already exists; `bootstrap_resample.py:21` *imports* it. Could import in cold_distribution too, but that couples a viz module to an estimator module — the comment may be the right call. — trivial.
- **A3** — `cold_distribution._compute_attempt_weights` (`:50`) mirrors `_episode_helpers._compute_weights`. Documented intentional attempt-vs-episode split (comment at `:56`). Could unify with a `kind` param; semantic-clarity argument is legit. — trivial.
- **C3** — `test_cold_distribution.py:186` hazard assert uses `abs(x-0.5)<1e-3`; `pytest.approx(0.5, abs=1e-3)` reads cleaner. — trivial.
- **C8** — `test_segment_history_cold_distribution.py:40-85` asserts `hazard > 0` without pinning a value; add one pinned hazard assertion. — trivial.
- **A4** — `db/attempts.py:_split_episode_into_events` doesn't thread `is_hot` (always False). Near-zero risk (test-fixture synthesis only); a one-line LIMITATIONS doc comment would suffice. — trivial.
- **E4** — `api_schemas.py:221` `EstimatorParamsRequest.params: dict[str, float]` but `_resolve_n_samples` coerces `int(raw)`. Deliberate: all params are float for the generic slider UI, int-coerced where needed (`declared_params` declares n_samples as float too). A clarifying comment is the most that's warranted. — trivial.
- **F2/F3/F5/F6** — `_empty_output()` / `hazard=None` / cold-filter-drop paths could carry debug logs. Marginal — these are documented nullable-output states, not failures; CLAUDE.md discourages unearned log noise. — trivial.

## Picked this session
- **F1** has_cold_state DB/disk-disagreement warning → inline edit + regression test → branch `improve/cold-state-disk-mismatch-log`.

## Dropped during critique / verify (debunked or intentional design)
- **A1 — KILLED (hallucinated duplication).** Architect lens claimed bootstrap's `_filter_to_cold_episodes` duplicates a cold-filter in `death_aware_rolling`. VERIFY: `death_aware_rolling` has NO cold filter (operates on all episodes); `bootstrap_resample.py:19-28` *imports* the shared `EFFECTIVE_WINDOW_HALFLIVES` + floor helpers FROM it. Cold-filtering is bootstrap-only by design (`:52-65` docstring). No duplication.
- **A5 — KILLED (by design).** "Cold panel uses death_aware_rolling halflife not the active estimator's." VERIFY: `routes/model.py:221-222` comment states this is intentional — the cold panel tracks one shared smoothing knob (DAR + bootstrap declare the same `halflife`). Not an asymmetry.
- **B2 — DROPPED (documented contract).** "Inline is_hot filter implicitly coupled to compute_cold_distribution." VERIFY: `cold_distribution.py:75-77` docstring explicitly states the caller-filters contract; the math layer correctly doesn't re-filter. Clean separation.
- **C2/C4/C5 — DROPPED (ground-truth-by-construction).** Test-skeptic flagged bootstrap/binning tests as "self-consistency not correctness." VERIFY: the expected values are arithmetic derivations in the test comments (`13200 = 3000+7000+3200`; `15900 = (5×7000+5×24800)/10`), not magic numbers. Tests are fine.
- **C1/C6/C-extra — DROPPED (cheap contract guards).** Skeptic defended `test_schema_imports`, `TestModuleExports`, `test_bin_count_constants_are_sane` as import/field/constant-relationship guards in a FastAPI→codegen pipeline. Not pure ceremony; leave them.
- **C7 — DEFERRED (matches project philosophy).** `hazard-render.test.ts` mocks Chart.js. The frontend unit suite is deliberately Vitest + happy-dom pure-logic; rendering isn't unit-tested by design.
- **B1/B3/B4/B5 (control-inversion) — KNOWN CARRY-OVERS.** Scheduler dual-instantiation, session-lifecycle facade, mode-based event routing, cold-fill async lifecycle. Plans exist / deferred repeatedly; not re-picked.

## New finds from VERIFY pass
- None beyond the skeptic's MISSED list (M1/M2 = the F3/F6 debug-log items, already in nearby-cleanup; M3/M4/M5 were trivial doc/dedup notes not worth a line).

## Patterns observed by lenses (archival)
- **Architect:** Decay-weight horizon (`EFFECTIVE_WINDOW_HALFLIVES`) and per-item exponential-decay weight computation now appear in ~3 places (estimators, cold_distribution) — natural consequence of adding a viz module; consolidate before it becomes 5. `is_hot` threading is careful but the "only bootstrap cares about is_hot" convention is undocumented. Routes layer is thin and well-delegating.
- **Control-inversion:** Re-surfaced the known cluster (dual Scheduler, mode-gated event routing, async-arm-before-complete lifecycle). No new levers beyond the backlogged ones.
- **Test-skeptic:** New statistical tests favor aggregate self-consistency; the few that matter already pin ground-truth in comments. Filtering tests (is_hot/cold) are load-bearing.
- **Dead-code:** Zero findings. Explicitly noted the `_compute_attempt_weights` vs `_compute_weights` split is intentional/documented (directly contradicting the architect lens's A3 framing — verify sided with dead-code: real but intentional).
- **Types:** No removal candidates; ColdBin/ColdDistribution schemas have clean dataclass↔schema correspondence; is_hot bool↔int conversion is explicit at both DB boundaries.
- **Observability:** Fallback-to-None logging is sparse but mostly intentional (nullable = "not enough data"). The standout is the filesystem check in segments.py (F1) — the first place SpinLab reaches outside the DB to answer a data-presence question, and the one genuine 2am gap.

## Verdict
Consistent with the last several scans: codebase is in good shape, the fresh cold/hazard/bootstrap surface is solid, and the lenses' big findings were misreads of well-commented intentional design. One real operability sharp edge (F1), shipped. Everything else is genuine-but-trivial nearby cleanup.
