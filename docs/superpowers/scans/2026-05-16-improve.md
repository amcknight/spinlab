---
date: 2026-05-16
focus: "full sweep"
git_head: fa7cd3e705b3b45b1ae043075cf4653f73234045
lenses_run: [architect, control-inversion, test-skeptic, dead-code, types, observability]
critiques_run: [skeptic, convergence]
raw_findings: 21
merged_findings: 21
picked: 1
status: full_scan
---

## Top wins

### must-fix
*(none)*

### high-leverage

- **M5 — `EndpointType` propagation across schemas/db/allocators**. `EndpointType` enum exists at [`python/spinlab/models.py:43-46`](python/spinlab/models.py#L43) but is bypassed by bare `str` annotations in `api_schemas.py` (12 sites: L59,61,77,79,163,165,230,232,283,285,328,330), `db/segments.py:15,17,32,34` (SegmentRow + MissingColdRow TypedDicts), `allocators/__init__.py:30,32` (SegmentWithModel), and `models.py:98` (Segment.make_id parameters). Codegen pipeline propagates Python Literal → TS string-union, so tightening here also tightens frontend types. — size: **medium** (~20 sites)
- **M4 — `transition_mode()` dead validator** at [`python/spinlab/models.py:36-40`](python/spinlab/models.py#L36). Validates against `_LEGAL_TRANSITIONS` but the SessionManager mode setter at `session_manager.py:122-126` directly assigns without calling it. Decision: wire through ~20 `self.mode = X` sites in session_manager.py, or delete the function as aspirational dead code. — size: **medium**

### convergent win

- **CF3 — Exception context helpers**. Absorbs **6 findings**:
  - **M9** [`condition_registry.py:90-113`](python/spinlab/condition_registry.py#L90) — `from_yaml` errors don't include the yaml path
  - **M10** [`speed_run.py:268-271`](python/spinlab/speed_run.py#L268) silently passes ConnectionError; `practice.py:275-280` logs it — inconsistent
  - **M11** [`retroarch/orchestrator.py:304-305`](python/spinlab/retroarch/orchestrator.py#L304) — tick_loop exception lacks state-machine context
  - **M12** [`retroarch/nci.py:76-91`](python/spinlab/retroarch/nci.py#L76) — `_drain_socket` swallows exceptions silently
  - **M14** [`sse.py:41-51`](python/spinlab/sse.py#L41) — dropped subscriber not logged
  - **M15** [`estimators/__init__.py:86-89`](python/spinlab/estimators/__init__.py#L86) — JSONDecodeError silently skips estimator state without logging
  - VERIFY DEBUNKED **M13** (`capture/reference.py:472-492` — already logs seg_id), so 6 not 7.
  - — size: **medium**

### nearby cleanup

- **M3** — Rename `_clear_ref_and_idle()` at [`session_manager.py:178-180`](python/spinlab/session_manager.py#L178) → public (e.g., `reset_reference_state`); called externally at `routes/system.py:75`. Leading underscore is misleading. size: **trivial**
- **M6** — Loose event signatures: `event: object` at [`timing.py:121,295`](python/spinlab/timing.py#L121); `event: Any` at [`state_paths.py:39,82`](python/spinlab/state_paths.py#L39). Tighten to a union of known event types. size: **trivial**
- **M16** — `time.sleep(0.05)` at [`tests/unit/retroarch/test_nci.py:105`](tests/unit/retroarch/test_nci.py#L105). Lens claimed 8 sites; VERIFY found 1. size: **trivial**

### Carry-overs not surfaced (already backlogged)

- **M1 / CF-2** routes-through-SessionManager facade (`routes/*` reaching DB direct, multiple files) — known pending
- **M2** Dual Scheduler at `practice.py:47` vs `session_manager.py:169` — known pending
- **M7 / Cluster J** Estimator dict-param shapes — known pending
- **M8 / HL-A** Practice loop observability gaps — known pending (CF3 helper from this session is the natural home for it)

## Picked this session

- **CF3 — Exception context helpers** (medium, convergent win) → handed off to `superpowers:writing-plans` on branch `improve/exception-context-helpers`.

## Dropped during critique

- **M10 (KILLED then reinstated)** — Skeptic argued speed_run.py:268-271 mirrors practice.py:275-280 — VERIFY confirmed speed_run silently passes but practice logs. Inconsistent. Kept inside CF3.
- **M13** — DEBUNKED by VERIFY: capture/reference.py:472-477 and 487-492 already include `seg_id` in log context.
- **M16 size** — DOWNGRADED by VERIFY: lens claimed `× 8` sites, only 1 found. Kept as nearby cleanup at correct size.
- **M17** — `test_socket_reused_across_calls` brittle internal socket identity check. Couldn't verify within VERIFY's read window; defer.
- **M18** — `test_mix_allocator.py:40-51`: skeptic math correct (σ ≈ 12.6, bounds 720-880 = 6.3σ → negligible flake). Test is sound.
- **M19** — `format.test.ts:84-94`: skeptic correct — regex spans 3 values, fixed offset subtracted from Date.now() yields ms-scale drift only.
- **M20** — `test_db_capture_sessions.py:123 time.sleep(0.01)`: needed for `created_at` ordering between two `create_capture_run` calls; mocking the system clock here is worse.
- **M21** — `tests/integration/conftest.py:275-285`: 10s = 40 × 0.25s window is appropriate for uvicorn startup latency. Already-improved area.

## New finds from VERIFY pass

- **N1** — `speed_run.py:268-271` vs `practice.py:275-280` **inconsistency**: identical try/except pattern, but speed_run silently passes while practice logs. Subsumed by CF3.
- **N2** — M5 size adjustment: VERIFY found 20+ sites spanning api_schemas, db/segments, allocators, models. Originally "medium"; bumped to "medium-large" for handoff planning.
- **N3** — M4 / CF1 size: VERIFY confirmed ~20 direct `self.mode = X` sites in session_manager.py (lines 180, 193, 288, 300, 332, 340, 358, 401, 409, 413, 454, 464, 475, 479, 506, 516, 527, 531+). Wiring is medium-large; deletion is trivial. Decision belongs in the picker, not here.
- **N4 (MM1 from skeptic)** — `routes/reference.py:150,156,162` call `db.rename_capture_run / delete_capture_run / set_active_capture_run` directly — already in M1 / CF-2 known carry-over.

## Patterns observed by lenses

- **Architect**: Routes layer reaches into SessionManager internals (data_dir, scheduler, private helpers) and Database direct — five patterns clustered around the same boundary. CF-2 is the right convergent fix; not picked this session.
- **Control-inversion**: PracticeSession and SessionManager each construct their own Scheduler; the SessionManager one is route-visible, the PracticeSession one is invisible to routes. Two views of the same logical object.
- **Test-skeptic**: Sleep-as-synchronization for UDP / DB timestamp ordering. Most are defensible; only one needed flagging.
- **Dead-code**: Codebase is in good hygiene shape post-Phase G. Only one real find: `transition_mode()` aspirational validator.
- **Types**: Single-sourcing intent is strong (recent Literal/Enum pass) but `start_type`/`end_type` leaked through intermediate layers. Codegen-to-TS amplifies the win for fixing it.
- **Observability**: Six places where exceptions are caught and either swallowed or logged without context. Common shape → CF3 absorbs all.

## Full merged list (collapsed)

Architect (7):
- A1 routes mutate db/sm privates (→ M1)
- A2 dual scheduler (→ M2)
- A3 `_clear_ref_and_idle` private leak (→ M3)
- A4 replay path duplication in routes (→ M1)
- A5 reset route duplicates shutdown cleanup (→ M1)
- A6 routes mutate capture_runs directly (→ M1)
- A7 mode mutations scattered, validator unused (→ M4)

Control-inversion (5):
- I1 PracticeSession self-constructs Scheduler (→ M2)
- I2 routes call `db.set_attempt_invalidated` direct (→ M1)
- I3 routes call db CRUD directly (→ M1)
- I4 ReferenceController.finalize_run silent if scheduler=None (→ adjacent observability)
- I5 dashboard.route_event push-pull tangle (informational)

Test-skeptic (7): M16, M17, M18, M19, M20, M21, plus dashboard fixture polling design (informational).

Dead-code (1): M4 `transition_mode()`.

Types (4): M5, M6, M7, plus speed_run `row["start_type"]` access (subsumed by M5).

Observability (8): M9, M10, M11, M12, M13 (DEBUNKED), M14, M15, plus capture/reference.py YAML context noise.

## Critique reports

**Skeptic verdict**: KILL M10/M18/M19/M20/M21, OVERSTATE M2/M3/M4/M8 (carry-overs already), MISS 7 items (mostly extensions of M1/M5/M6).

**Convergence-hunter verdict**: 7 candidate convergent fixes:
- CF1 wire transition_mode through setter (M4, M1 partial) — medium-large
- CF2 EndpointType propagation (M5, M6 partial) — medium
- **CF3 exception context helpers (M9-M15) — medium**
- CF4 rename `_clear_ref_and_idle` (M3) — trivial
- CF5 estimator ParamDict TypedDicts (M7, carry-over) — medium
- CF6 dual scheduler consolidation (M2) — big
- CF7 event-type union (M6) — trivial

CF3 picked because it ships observability uniformly across 6 sites and lays the foundation for HL-A (practice-loop obs carry-over).

**VERIFY verdict**: All M1-M15 anchors confirmed except M13 (debunked — already logs seg_id) and M16 size (1 site, not 8). M5 and M4 sizes confirmed as medium-large. CF3 confirmed as the cleanest convergent absorption.
