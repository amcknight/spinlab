---
date: 2026-05-19
focus: "test flake"
git_head: 36c5536 (baseline) → 375c90b (after picks, on improve/mode3-cascade-quarantine)
lenses_run: [architect, control-inversion, test-skeptic, dead-code, types, observability]
critiques_run: [skeptic, convergence]
raw_findings: ~30
merged_findings: 19 (A1-A6, OB1-OB6, CI2-CI7, TY1-TY4, TS2, DC1-DC4)
picked: 2 (CF-7-narrow shipped + CF-1 plan written; trivial-batch CF-3/CF-4/M6/TS2 deferred after re-verification raised scope)
status: full_scan
---

## Top wins

### must-fix
*(none)*

### high-leverage
- **CF-1** — `tests/integration/_harness_factory.py:82-90` + `tests/integration/ra_poke_engine.py:104` — Mode 3 RA crash cascade quarantine. Health-gate the session-scoped harness cache via a new `RAHarness.is_alive()` probe, capture `proc.returncode` at death detection (survives teardown reap), raise typed `RABackendDied(exit_code=...)` so the test that triggers the discovery fails with a clear "RA died with 0xC0000005" report instead of opaque NCITimeouts. `pytest_runtest_makereport` proactively probes is_alive() on NCITimeout/NCIProtocolError test failures so even the first cascading test captures the exit code before teardown loses it. Absorbs A1, A3, A5, OB4, OB5 (5 confirmed items). Directly addresses the 5-10% measured Mode 3 flake rate. — size: **medium** — plan written at `docs/superpowers/plans/2026-05-19-mode3-cascade-quarantine.md` this session.

- **CF-5** — `python/spinlab/retroarch/poller.py:140-146` + `detector.py:48` — Detector reset hook. Collapse `state_version`-gated `resync_after_state_load` + missing `_frame_counter` reset behind one named `Poller._on_state_reload()` that every reload path calls (state-bump, LOAD_STATE_SLOT, reconnect). Absorbs A2, A4. — size: medium. NOT picked this session.

### convergent win
- **CF-3** — `python/spinlab/models.py:47-50` (EndpointType StrEnum already exists) + `models.py:102-104, :229` + `python/spinlab/timing.py:82,119` — `EndpointType` end-to-end annotation swap. Absorbs TY2, TY3, and the M5 carry-over from 2026-05-17. — size: trivial in surface but call-site ripple: `Segment.make_id` callers (~6-8 sites) currently pass raw `str` literals; tightening to `EndpointType` requires updating those too (or using `EndpointType | str` for soft acceptance, which loses the safety). Deferred from this session's trivial batch — needs its own micro-pass.

### nearby cleanup
- **CF-4** — `python/spinlab/config.py:13` `port: int = 15482` + `python/spinlab/cli.py:142-145` `_write_ports_file(.., config.network.port, ..)` — kill dead `NetworkConfig.port`. Verified: AHK launcher (`scripts/spinlab.ahk:27, :97`) reads only `dashboard_port` and `vite_port` from `.spinlab-ports`, never `tcp_port`. Touches test_config.py (2 asserts), test_cli.py (1 assert + 1 fixture), tests/conftest.py:22 (`port=overrides.get("port", 59999)` pass-through), tests/integration/conftest.py:236 (already documented as inert), tests/integration/_dashboard_harness.py:86-88, :91, :96 (`fake_tcp_port=_free_port()` pattern). Trivial but not single-file — defer to its own commit.
- **CF-7-narrow** — `tests/integration/_dashboard_harness.py:40` — `session: object` → `session: SessionManager` (under TYPE_CHECKING). Shipped this session.
- **TY4** — `tests/conftest.py:30-41` — `mock_emu()/mock_db()` return `MagicMock` untyped. Adding return type hints would help static analysis. Deferred.
- **TS2** — `tests/unit/capture/test_cold_fill_integration.py:20-27` — mocks `emu` when `FakeEmuBackend` exists at `tests/conftest.py:88`. Conversion to `fake_emu` fixture is straightforward but the test's assertion shape (`emu.send_command.call_args[0][0]`) changes to `emu.sent_commands[0]`. Deferred.
- **M6 (new)** — no `pytest_sessionstart` hook validates `SPINLAB_TEST_ROM` is set; emulator tests each hard-fail individually instead of one loud session-start error. Trivial. Deferred.

### Carry-overs (already backlogged / declined)
- **CF-4-DI** — Scheduler / ConditionRegistry constructor injection in `PracticeSession.__init__` (`python/spinlab/practice/practice.py:47`) and `ReferenceController.__init__` (`python/spinlab/reference.py:100`). Queued as next medium; same name as a prior carry-over.
- **CF-β** — typed AppState + DB rows (`docs/superpowers/plans/2026-05-17-typed-boundaries-cf-beta.md`) in flight.
- **261 pyright errors** — known cleanup. Andrew explicitly pushed back on "tracked over time" framing 2026-05-18.

## Picked this session
- **CF-7-narrow** (nearby, trivial) → inline edit on branch `improve/mode3-cascade-quarantine`, commit `375c90b`. `DashboardContext.session: object` → `SessionManager` via `TYPE_CHECKING` import. Pyright now type-checks `ctx.session.X` callers.
- **CF-1** (high-leverage, medium) → plan written to `docs/superpowers/plans/2026-05-19-mode3-cascade-quarantine.md`. Seven tasks (RABackendDied exception, RAHarness.is_alive(), HarnessFactory health-gate, diagnostic surfacing, makereport probe, CLAUDE.md note, 15-iter stress validation). Same branch.

## Dropped during critique
- **OB2** (orchestrator silent NCI dispatch) — DEBUNKED by VERIFY: `python/spinlab/retroarch/orchestrator.py:212-220` is dispatch-table lookup with `logger.warning()` on miss, not NCI swallow. Lens misread.
- **OB6** (LOAD_STATE_SLOT drain logging) — Skeptic kill: speculative; no measured incidence. Mode 3 fires after `press key=RESET`, not after LOAD_STATE_SLOT.
- **CI2** (`receive_result` mode-check swallow) — DEBUNKED by VERIFY: `python/spinlab/practice.py:161-167` has no mode guard; the guard lives at `SessionManager._handle_attempt_result:336-337` (CI5 territory). Lens misanchored.
- **A6** (`_force_next_entrance` stale `_exit_this_frame`) — DEBUNKED by VERIFY: `python/spinlab/retroarch/detector.py:134` overwrites `_exit_this_frame` fresh each call before the entrance check at :163 reads it. No stale state.
- **DC3** (conftest docstring references deleted `replay_dashboard`) — DEBUNKED by VERIFY: no such docstring reference exists at `tests/integration/conftest.py:201` — the fixture name is `replay_ra_dashboard`, the only "replay_dashboard" mention in the body is at :200 ("Mirrors replay_dashboard but uses the RA backend...") which is a historical comparison comment, not a fixture reference; not stale enough to surface. Lens overreach.
- **CI6** (separate `_result_event` + `_result_data` slots) — Skeptic kill: one writer, one reader, stylistic preference. CI2's mode-check shape was the load-bearing concern, and that one debunked.
- **TY4** (mock_emu/mock_db untyped MagicMock) — Skeptic overstate → demoted to nearby cleanup. Generic mock-skepticism without a specific bug instance.
- **CI7** (FakeEmuBackend.recv_event always None) — Skeptic overstate: this is the intentional unit-vs-integration tradeoff. Real event-loop delivery is exercised by `test_replay_fixture.py`.
- **M1 (CRLF in CI)** — DEBUNKED: no `.github/workflows/` exists in this repo at all. The "skeptic missed CI guard" framing presumed a CI surface that isn't there.
- **M2 (log rotation 1MB too small)** — Skeptic propose, VERIFY: confirmed `RotatingFileHandler(maxBytes=1_000_000, backupCount=3)` at `python/spinlab/cli.py:104-105`. Marginal — keep on deferred list if a post-mortem ever loses history; not flake.
- **M3 (pyright baseline)** — DEBUNKED: `pyproject.toml:54-58` already has `[tool.pyright]`. Skeptic guessed wrong.
- **M8 (POST_LOAD_SETTLE_FRAMES magic number)** — DEBUNKED: `tests/integration/ra_poke_engine.py:51-55` has a 5-line comment explaining the value (snes9x SPC re-init). Not a CLAUDE.md violation.

## New finds from VERIFY pass
- **N1** — `python/spinlab/models.py:47-50` `EndpointType(StrEnum)` already exists with ENTRANCE/CHECKPOINT/GOAL. CF-3 is therefore a pure annotation swap, not "introduce a new enum." BUT the call-site ripple (~6-8 `Segment.make_id(..., "entrance", ..., "goal", ...)` raw-string sites) means it's not free. Folded into the CF-3 line in nearby cleanup; not picked.
- **N2** — `tests/integration/conftest.py:236` `port=15482, # unused — RA backend uses NCI, not TCP` is the *only* documentation that NetworkConfig.port is dead. The CF-4 cleanup should leave a docstring on `NetworkConfig` explaining why the field was removed, or future-me will rediscover it from git blame.
- **M6 (new)** — no `pytest_sessionstart` checks `SPINLAB_TEST_ROM`; trivial single-place add. Folded into nearby cleanup.
- **VERIFY anchor count**: 33 anchors checked; 22 verified high, 5 debunked at the anchor, 3 not fully verifiable from the read window (M4 migration ordering test, M5 FAST_FORWARD off-after-replay test, M7 replay_total persistence — see Open Questions below).

## Open questions deferred to next scan
- **M4** — does a migration-ordering test exist? Grep returned no obvious match. If absent, a single test asserting `sorted(glob('NNNN_*.sql')) == applied_migrations` would catch a typo'd filename silently shipping the wrong subset.
- **M5** — is there any test that asserts `FAST_FORWARD` is OFF after replay end? If not, a missed BSV-end event would leave FF on for subsequent tests and timing assumptions would silently break.
- **M7** — Bundle 3 moved `replay_total` to `ReferenceController`. Is it re-derived on RA restart, or stale? Concrete narrow question.

## Patterns observed by lenses
- **Architect**: Mode 3 cascade is the dominant test-flake architecture issue. The session-scoped harness cache is correct for performance (avoiding the 30s relaunch per test), but lacks a health contract. The CF-1 plan adds the contract without changing the cache pattern.
- **Control-inversion**: The "two mutable slots for one signal" pattern (CI6) and the "construct dependencies in __init__" pattern (CI1 → already-queued CF-4-DI) are recurring smells. CF-4-DI is queued; CI6 was killed by skeptic.
- **Test-skeptic**: Returned 2 actionable items (TS2 + TY4-via-skeptic-promotion), neither flake-related. The recent CF-1/CF-2/CF-3 sweeps have absorbed most ceremony critiques — confirms the 2026-05-17/18 pattern that test-skeptic lens output is converging to ≤1 actionable per scan.
- **Dead-code**: CF-4 (NetworkConfig.port) is the only real find. The 2026-05-15-1432 VERIFY at the time said "actually used" because the `_write_ports_file` call site exists; subsequent inspection of the AHK launcher confirms the `tcp_port=` line written to `.spinlab-ports` has no reader.
- **Types**: CF-3 is the headline (EndpointType exists, callers don't use it). The CF-7-narrow shipped fix is a one-line analogue.
- **Observability**: The Mode 3 cascade has been the recurring story — proc.poll() loses the exit code after teardown reaps. CF-1's `last_returncode` field is the structural fix; CF-3-obs and CF-1 share the "name the failure mode at the decision point" pattern.

## Verification verdict summary
| Cluster | Verdict | Evidence |
|---------|---------|----------|
| A1 | VALID | tests/integration/ra_poke_engine.py:104 — fresh detector per scenario; conftest.py:135-144 — session-scoped harness fixture. Cascade vector confirmed. |
| A2 | VALID | python/spinlab/retroarch/poller.py:140 — guard `cur_ver != self._last_seen_state_version`. |
| A3 | VALID | tests/integration/_harness_factory.py:82-90 — teardown_all wraps each in try/except. |
| A4 | VALID | python/spinlab/retroarch/detector.py:48 init + comment at :75 confirms `_frame_counter` not reset. |
| A5 | VALID | tests/integration/conftest.py:57, :147 — both fixtures `scope="session"`. |
| A6 | DEBUNKED | detector.py:134 sets `_exit_this_frame` before :163 reads it. |
| OB1 | VALID | python/spinlab/retroarch/poller.py:125-131 except with `log.warn()` and continue. |
| OB2 | DEBUNKED | orchestrator.py:212-220 is dispatch-table lookup with warn-on-miss, not NCI swallow. |
| OB4 | VALID | tests/integration/ra_harness.py:414-425 — `_kill()` polls `proc.poll()` and returns; no exit-code log. |
| OB5 | VALID | tests/integration/_diagnostics.py:180-193 — reads `harness.proc.poll()` at test-end; cascade tests see `None`. |
| OB6 | DEBUNKED (speculative) | python/spinlab/retroarch/nci.py:188-210 — drain is silent but no measured incidence of "hung not post-crash" confusion. |
| TY2/TY3 | VALID | python/spinlab/models.py:102-104, :229 — bare `str` parameters. |
| M5 carry | VALID | python/spinlab/timing.py:82 — `_end_type: str` initialized. |
| EndpointType | VALID exists | python/spinlab/models.py:47-50 — `EndpointType(StrEnum)` with ENTRANCE/CHECKPOINT/GOAL. |
| DC1 | VALID | python/spinlab/cli.py:144 — `_write_ports_file(.., config.network.port, ..)`. |
| DC4 | VALID dead | python/spinlab/config.py:13. AHK reads only `dashboard_port` + `vite_port`. |
| DC3 | DEBUNKED | conftest.py:200-201 is a body comment about historical RA-vs-Mesen migration, not a stale fixture reference. |
| CI2 | DEBUNKED (misanchored) | practice.py:161-167 has no mode guard; the guard is at session_manager.py:336-337 (CI5). |
| CI5 | VALID | session_manager.py:335-340 — `if self.mode != Mode.PRACTICE: return`. |
| CI6 | DEFER (style) | practice.py:73-74 — two slots; one writer one reader. |
| TY1 | VALID | tests/integration/_dashboard_harness.py:40 — `session: object`. SHIPPED. |
| TY4 | DEFER (style) | tests/conftest.py:31-41 — MagicMock untyped. |
| TS2 | VALID | tests/unit/capture/test_cold_fill_integration.py:20-27 — mocks emu. |
| CI7 | DEFER (tradeoff) | tests/conftest.py:120-122 — `FakeEmuBackend.recv_event()` returns `None`. Intentional. |
| M1 | DEBUNKED (no CI) | no `.github/workflows/`. |
| M2 | DEFER | cli.py:104-105 — 1MB rotation. Marginal. |
| M3 | DEBUNKED | pyproject.toml:54-58 — `[tool.pyright]` exists. |
| M6 | VALID (new) | no `pytest_sessionstart` for SPINLAB_TEST_ROM. |
| M8 | DEBUNKED | ra_poke_engine.py:51-55 has the rationale comment. |
| M4/M5/M7 | UNVERIFIED | not fully readable from the verifier's window; open questions. |

## Baseline note

Baseline `python -m pytest` at HEAD `36c5536`: 929 passed, 1 failed (`tests/integration/test_replay_fixture.py::TestReplayFixture::test_replay_produces_segments`). This is the known Mode 2/Mode 3 flake documented in `project_test_reliability_known_issues` — the exact target of the CF-1 plan. Per CLAUDE.md, surfaced; not silently moved past. CF-1 plan landing is the resolution path.

Post-edit (commit `375c90b`) fast-tests pass at 915/915. The CF-7-narrow annotation change is pure typing and cannot have caused the baseline failure.
