---
date: 2026-05-15
focus: "peripherals (scripts, tests, docs, hidden files)"
git_head: e59034d76af16a1ab6f3ebd1cf953eb65816e01e
lenses_run: [architect, control-inversion, test-skeptic, dead-code, types, observability]
critiques_run: [skeptic, convergence]
findings_count: 40
status: full_scan
---

## Top wins

### must-fix
*(none — nothing is broken-now; flaky integration tests have a path forward via CF-C below)*

### high-leverage / convergent win

- **CF-A — Dead-artifact deletion batch**. Absorbs: F4, F5, F6, F7, F25-bucket, F36. Confirmed dead by VERIFY:
  - `scripts/launch.sh`, `scripts/launch.bat` — reference Mesen exe paths and `lua_script` config; Mesen deleted in Phase G
  - `scripts/probe_bsv_record_with_saves.py`, `probe_cp_entrance.py`, `probe_input_dispatch.py`, `probe_save_and_nrp.py`, `probe_save_state.py`, `spike_retroarch.py`, `smoke_nci_client.py`, `count_replay_frames.py` — zero imports across `tests/` and `python/spinlab/`
  - `tests/screenshots/` — 10 PNGs, zero references in any test
  - `scripts/dedicated_coverage.py:41` — `--ignore=tests/playwright` (directory renamed to `tests/screenshots/` weeks ago; the filter is a silent no-op)
  - `tests/integration/test_replay_fixture.py:141` — comment mislabels the current fallback as a "legacy path"
  - size: **trivial** (one `git rm` commit + 2 string edits)

- **CF-C — Failure-context generalization**. Absorbs: F2, F3, F18, F31, F32, F35. Carry-over from 2026-05-13 and 2026-05-14-1735 (still pending across 2 scans). VERIFY confirmed:
  - `tests/integration/conftest.py:629` hook hard-codes `("replay_ra_dashboard",)` — every `run_scenario` failure gets an empty diagnostic block
  - `tests/integration/ra_harness.py:179-182` launches RA with `stdout=DEVNULL, stderr=DEVNULL` — RA crashes are invisible
  - `tests/integration/conftest.py:358-360` wraps `RAHarnessLaunchError` as `RuntimeError`, losing pid + startup duration
  - `tests/integration/conftest.py:567-576` `pause_toggle()` exception swallowed at warning level; later test fails with unrelated "core not paused"
  - `tests/integration/conftest.py:282` dashboard timeout message has no probe context (port, last health-check status)
  - `_collect_diagnostics` never calls `.proc.poll()` — can't distinguish "RA died" from "NCI port wrong"
  - size: **medium** (plan: stdout/stderr → per-launch logfile in harness; generalize diagnostic loop over funcargs; structured `RAHarnessLaunchError` with context fields)

- **CF-B — Test-helper hoist + integration-fixture shape standardization**. Absorbs: F1, F10, F22, F27, F29. VERIFY confirmed:
  - `_make_seg_with_state` duplicated in 3 files (`tests/conftest.py:137`, `tests/unit/test_scheduler_kalman.py:12`, `tests/unit/test_segment_with_model.py:11`)
  - `_make_sm` duplicated in 3 files with divergent signatures (`test_replay.py:34` takes `tmp_path`, `test_session_manager_conditions.py:24` takes `**kwargs`, `test_state_builder.py:13` minimal)
  - `tests/integration/conftest.py:231` does `from tests.conftest import FakeEmuBackend` **inside** the `fake_dashboard_server` body — a function-local cross-conftest import
  - Four fixture return shapes in the same file: tuple `(base_url, db, session)`, scalar `game_id`, callable `_run`, tuple `(base_url, db, tmp_path)`
  - size: **medium** (new `tests/helpers/` package; one `DashboardContext` dataclass; rewrite of 5 fixture sites)

### nearby cleanup

- **CF-F — pytest markers cleanup** (`pyproject.toml`, `scripts/dedicated_coverage.py:42`). Only `emulator` is declared; `slow` and `frontend` are not declared anywhere and not used by any test; the dedicated-coverage script filters with `-m "not (emulator or slow or frontend)"`. Either register them (+ retag tests) or strip them from the filter. **size: trivial**
- **CF-D — TypedDicts / dataclasses for scenarios + ROM_REGISTRY entries**. `parse_poke -> dict`, `run_scenario(scenario: dict)`, `FakeRAClient.nci -> Any`, bare `ROM_REGISTRY: dict[str, str]` with three-part contract enforced only by docstring. **size: medium**
- **CF-E — `scripts/_paths.py` shared helper**. `smoke_nci_client.py:298` hardcodes `C:/RetroArch-Win64/states/Snes9x`; `make_fresh_boot_state.py:28-33` imports private `_resolve_ra_paths` from `tests.integration.conftest`; `config.yaml` vs `config.example.yaml` diverges on `savestate_dir`; `bootstrap-sandbox.sh:29` + `plan-preflight.sh:50-73` swallow stderr; `dedicated_coverage.py:41` references a renamed dir. **size: medium**
- **F12** — `time.sleep(0.05)` × 2 at `tests/unit/retroarch/test_nci.py:104,110`; in-process sync server → magic delay. **size: trivial**
- **F20** — Link `docs/retroarch-migration/` from `docs/ARCHITECTURE.md` (migration docs orphaned post-Phase G). **size: trivial**
- **F16** — Frontend SSE fallback poll silent on repeated failures (`frontend/src/api.ts:59-75`). **size: trivial-medium**
- **F26** — `.spinlab-ports` purpose undocumented; 1-line in README. **size: trivial**
- **M9** — `docs/superpowers/scans/2026-05-14-improve-flaky-tests.md` is untracked yesterday's scan; commit or delete. **size: trivial**

## Picked this session

- **CF-A** → inline trivial batch on branch `improve/peripherals-cleanup-and-diagnostics`
- **CF-C** → handed off to `superpowers:writing-plans` (medium, plan-then-execute)

## Dropped during critique

- **F38** — out of scope (touches `python/spinlab/`)
- **F40** — debunked by VERIFY: `_make_tcp` exists in `tests/unit/test_practice_coverage.py`
- **F25** — `.playwright-mcp/` is a gitignored MCP runtime artifact in the working dir, not a repo file
- **F17** — frontend `api.test.ts` mocking fetch is the correct unit-test pattern; contract drift is caught by `npm run gen-types`
- **F24** — within-file ModelSegment copy-paste in `model-logic.test.ts` is not worth a factory
- **F26** — also borderline (skeptic: `.spinlab-ports` is runtime artifact; 1-line README is the only fix)
- **F15** — debunked by VERIFY: `mock_emu` IS used in `tests/unit/test_state_builder.py`; `mock_db` may still be unused but the conftest comment makes the template intent explicit
- **F30** — `_ring` buffer module-scoped leak only matters under in-process re-runs; pytest doesn't do those normally
- **F33, F34, F39** — shell scripts swallowing stderr is the bash idiom; downgraded
- **F22, F27** — `_HarnessFactory` / `RAHarness` constructing sub-objects internally — these are test infrastructure, not production targets for DI; downgraded
- **F12 inside CF-C** — actually kept as nearby-cleanup (small enough to batch separately)
- **coverage.sh:6 "Mesen2"** (originally F9) — debunked by VERIFY: the comment doesn't actually say "Mesen2"; the lens misread

## New finds from VERIFY pass

- **M5 / process gap** — No `.pre-commit-config.yaml` or `.husky/` in the repo despite `.gitattributes` warning about CRLF leakage in sandboxes. Today's only defense is "Claude remembers to run `git ls-files --eol`".
- **M7** — `frontend/src/api-types.ts` IS gitignored (line 33) — consistent with codegen pipeline.
- **M9** — `docs/superpowers/scans/2026-05-14-improve-flaky-tests.md` is untracked (from yesterday).
- **bootstrap-sandbox.sh:35** — `command -v uv` has no stderr redirect; minor inconsistency with the rest of the file.
- **`tests/integration/conftest.py` is 700+ lines** — splitting into `conftest/fixtures_dashboard.py`, `fixtures_ra.py`, `diagnostics.py`, `rom_registry.py` would be the real structural improvement F1/F11/F22/F27/F30/F35 are dancing around. Defer until CF-B + CF-C land.

## Full merged list (collapsed)

40 findings across 6 lenses (architect / control-inversion / test-skeptic / dead-code / types / observability). Key clusters:

- **Architect (F11, F22, F23, F27, F29, F37, F39)**: leaky test/script boundaries, three-part ROM_REGISTRY contract not enforced, no shared test-helpers module, scripts importing test conftest, probes reimplementing NCI logic.
- **Control inversion (F1, F22, F27)**: fixture shape asymmetry, factory constructs sub-objects internally, callable-vs-tuple yield mismatch.
- **Test skeptic (F1, F6, F10, F12, F15, F17, F24, F28, F30, F36, F40)**: fixture asymmetry, screenshots dead, helper duplication, sleep-based tests, mock-template siren song, frontend mock pattern, orphan fixtures.
- **Dead code (F4, F5, F6, F7, F8, F9, F21, F25, F26, F36, F40)**: Mesen-era launch scripts, probe/spike archaeology, screenshots, stale ignore-globs, pyproject marker drift, working-dir cruft.
- **Types (F11, F13, F14, F15)**: bare dicts for scenarios + config + ROM registry, `Any` escape hatches in fakes.
- **Observability (F2, F3, F16, F18, F19, F20, F31, F32, F33, F34, F35)**: diagnostic hook only on one fixture, DEVNULL'd subprocess, silent SSE fallback, swallowed exceptions, missing process-state checks, hardcoded paths in scripts, unlinked migration docs.

## Critique reports

See conversation transcript for full skeptic / convergence-hunter / verify outputs. Key verdicts above in `Dropped during critique` and `New finds from VERIFY`.
