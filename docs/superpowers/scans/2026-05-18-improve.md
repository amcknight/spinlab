---
date: 2026-05-18
focus: "crosstest contamination and flake"
git_head: 3f2ab041f2da8f3e12c1f8a7004fa9144175cbb6 (at scan start)
lenses_run: [architect, control-inversion, test-skeptic, dead-code, types, observability]
critiques_run: [skeptic, convergence]
raw_findings: ~35
merged_findings: 19 (A-S)
picked: 3 (F + G + I on `improve/script-imports-and-port-helper-dedupe`)
status: full_scan
---

## Top wins

### must-fix
- **F** — `scripts/make_fresh_boot_state.py:28-32` — broken imports after the conftest split. Script tries to `from tests.integration.conftest import ROM_REGISTRY, _free_udp_port, _resolve_ra_paths`, but all three moved (`ROM_REGISTRY` → `_rom_paths`, `_free_udp_port` → `_harness_factory`, `_resolve_ra_paths` → `_rom_paths` as `resolve_ra_paths`). Script crashes on import; nothing in CI catches it because the script is invoked manually to regenerate per-ROM fresh-boot savestates. — size: **trivial** — shipped this session.

### high-leverage
*(none — the recent test-infra refactor (CF-1 + per-ROM fresh-boot mechanism + RA sram/savestate isolation) has cleaned out the substantive flake risks; the lens swarm proposed several large clusters but VERIFY debunked them.)*

### convergent win
*(F+E would have been a natural pair, but skeptic + VERIFY agreed that the `ROM_REGISTRY["toothpaste"]` entry and `TOOTHPASTE_ROM_NAME` re-export are intentional infrastructure prep, not flake risk. F shipped as a stand-alone fix.)*

### nearby cleanup
- **G** — `_free_port()` duplicated in `tests/integration/conftest.py:53-57` and `tests/integration/_dashboard_harness.py:27-31`. Conftest now imports the dashboard-harness version. — size: trivial (shipped this session)
- **I** — `tests/integration/ra_harness.py:437` — `tempfile.mkdtemp(prefix="spinlab_ra_failed_")` is created on failed-launch path to preserve the RA log past the original `tmp_dir`'s `rmtree`, but the new directory is never cleaned up. Added `pytest_sessionfinish` hook in conftest.py to globs+rmtree any `spinlab_ra_failed_*` leftovers in the system temp root. — size: trivial (shipped this session)
- **M** — `tests/integration/_diagnostics.py:129-134` — `collect_diagnostics` duck-types fixtures via `isinstance(fixture_val, tuple) and len >= 2 and isinstance(fixture_val[0], str) and fixture_val[0].startswith("http")`. Fragile but no observed misfire today (only two fixture shapes start with "http"). Deferred until a non-dashboard URL-tuple fixture appears.
- **E** — `tests/integration/_rom_paths.py:18,37` `TOOTHPASTE_ROM_NAME` + `ROM_REGISTRY["toothpaste"]`. Registered but no consumer fixture; intentional framework prep per [[project_test_rom_declaration_design]] memory. Conftest re-export at line 39 also unused, but removing only one in isolation creates inconsistency with the other 5 re-exports. Leave.

### Carry-overs (already backlogged / declined)
- **A** (page fixture scope) — DEBUNKED by VERIFY: pytest-asyncio's `loop_scope="session"` is orthogonal to fixture scope; `pyproject.toml:29` confirms `asyncio_default_fixture_loop_scope = "session"` and the page fixture defaults to `scope="function"`, yielding a fresh `errors` list per test. Lens conflated event-loop scope with fixture scope.
- **B** (session-scoped DB shared) — only 1 replay test today; mark for re-eval if a 2nd replay test lands without cleanup.
- **C** (ring buffer + session-fixture log bleed) — DEBUNKED: `pytest_runtest_setup`'s `ring.clear()` fires before the test body, so session-fixture noise is dropped.
- **D** (HarnessFactory inter-test reset) — DEBUNKED: per-scenario `LOAD_STATE_SLOT` IS the inter-test boundary; ROM_REGISTRY's docstring documents this.
- **H** (replay uses user `savestate_dir`) — Intentional: RA needs the user's dir to locate staged `.replay` files; poke tests need isolation for fresh-boot state.
- **J** (pause-toggle sleep) — load-bearing; NCI has no request-reply primitive. Prior-scan triaged.
- **K** (`StatusInfo.state` + test `mode` stringly-typed) — M5 EndpointType propagation carry-over; not flake.
- **L** (`HarnessFactory.__call__(rom_key: str)` → `Literal`) — type-time win only; runtime already hard-fails on unknown key via `resolve_ra_paths`. Not flake.
- **N** (`fake_dashboard_server` yields `(url, db, session)` vs `replay_ra_dashboard` yields `(url, db, tmp_path)`) — single consumer each; named DashboardContext exists for context-manager path. Style only.
- **O** (RA teardown silent except + no test-name ring marker + missing rom_key/pid in teardown log) — observability hygiene; not flake. Could batch with CF-3 follow-ups if future scan picks it up.
- **P** (CF-4: PracticeSession self-Scheduler / ReferenceController self-ConditionRegistry) — already queued as next medium.
- **Q** (`port=15482` hardcoded in `replay_ra_dashboard`) — explicitly marked `# unused — RA backend uses NCI, not TCP`; verified no consumer reads `NetworkConfig.port` from the replay fixture's config path.
- **R** (`DashboardHarness._teardown` no double-close guard) — speculative; requires context-manager misuse.
- **S** (`SessionManager.get_scheduler()` cache reset only via `switch_game`) — latent low-risk; segments are seeded at game-load and not mutated mid-session today.

## Picked this session
- **F** (must-fix, trivial) → inline edit on branch `improve/script-imports-and-port-helper-dedupe`. Fixed imports in `scripts/make_fresh_boot_state.py` (point at `_rom_paths` + `_harness_factory`), renamed `_resolve_ra_paths` → `resolve_ra_paths`, updated docstring to point at `_rom_paths.py:ROM_REGISTRY`, and replaced stale `'default'` example with `'vanilla_smw'`.
- **G** (nearby, trivial) → conftest now `from tests.integration._dashboard_harness import _free_port`; conftest's own copy deleted along with its now-unused `import socket`.
- **I** (nearby, trivial) → added `pytest_sessionfinish` hook in `tests/integration/conftest.py` that globs `<tempdir>/spinlab_ra_failed_*` and `rmtree`s leftovers. Cleans up both this-session orphans and any from prior crashed sessions.

## Dropped during critique
- **A** (page fixture scope) — Lens claim that `loop_scope="session"` makes the fixture session-scoped is wrong; pytest-asyncio's `loop_scope` controls event-loop reuse, not fixture instance reuse. Skeptic right; VERIFY confirmed by reading pyproject + the fixture body.
- **D** (HarnessFactory inter-test reset) — Per-scenario `LOAD_STATE_SLOT` IS the boundary; lens framed it as missing.
- **E** (toothpaste dead) — Memory + docstring both confirm intentional prep. Keep for future fixture.
- **H** (replay savestate dir asymmetry) — intentional by RA-replay-file-resolution design.
- **Q** (port 15482 hardcoded) — actually inert; lens worried about speculative future refactor.
- **R** (double-close guard) — requires context-manager misuse.

## New finds from VERIFY pass
- **N1** — `tests/integration/conftest.py:34-39` has 5 other `# noqa: F401 — re-exported for downstream consumers` lines (CLEAN_SMW_ROM_NAME, INTEGRATION_STATES_DIR, LOVE_YOURSELF_ROM_NAME, PROJECT_ROOT, TOOTHPASTE_ROM_NAME) where only LOVE_YOURSELF_GAME_ID has an actual downstream consumer (`test_replay_fixture.py:19`). The others are dead, but uniformly removing them would also require migrating `test_replay_fixture.py`'s import to `tests.integration._rom_paths` directly. Out of scope this scan; flag for future cleanup.
- **N2** — `pyproject.toml:29` `asyncio_default_fixture_loop_scope = "session"` is the reason `loop_scope="session"` shows up on most pytest-asyncio fixtures. This setting plus the orthogonality of `loop_scope` vs `scope` is non-obvious to readers; a sentence in the integration testing docs (or in `_dashboard_harness.py` docstring) would prevent the lens's misread from recurring.
- **N3** — `scripts/make_fresh_boot_state.py` is not exercised by any test or CI step; the broken-import regression survived a week unnoticed. Worth a single smoke test that just `import scripts.make_fresh_boot_state` to catch future moves. Not picked this scan — file-import smoke is a separate small win.

## Patterns observed by lenses
- **Architect**: Most "session-scoped fixture mutating shared state" findings turned out to be already-correct designs (per-scenario state-load, session-fixture log clearing in setup). The 871→367-line conftest split (CF-1) addressed the bulk of the structural concerns; what's left is genuine maintenance debt, not flake risk.
- **Control-inversion**: CF-4 remains the biggest open lever (PracticeSession self-Scheduler, ReferenceController self-ConditionRegistry), already queued. No new control-inversion insights from this scan beyond what's tracked.
- **Test-skeptic**: Returned 2 findings in flake scope; VERIFY debunked both. The test-skeptic lens has now produced ≤1 actionable item across the last 3 tests-focused scans — the suite has truly absorbed the obvious ceremony critiques.
- **Dead-code**: 3 findings, 1 confirmed broken (F), 2 intentional prep (E + script-pinned-to-vanilla docstring). Dead-code hygiene remains tight.
- **Types**: Best findings (L, M, N) are real but not flake — they're style/safety improvements. The recent CF-2 typed-fixture-vocabulary work closed the highest-density gap.
- **Observability**: Findings cluster around teardown silent excepts + missing test-name markers in the ring. Not flake per se, but a future CF-3-style structured-logging pass could absorb cluster O if a flaky run actually bites.

## Verification verdict summary
| Cluster | Verdict | Evidence |
|---------|---------|----------|
| A | DEBUNKED | `pyproject.toml:29` + pytest-asyncio docs: `loop_scope` ≠ fixture scope; fresh errors list per test. |
| B | DEFER | 1 replay test today; pattern is fragile but not bitten. |
| C | DEBUNKED | `ring.clear()` in `pytest_runtest_setup` runs before test body — session-fixture noise dropped. |
| D | DEBUNKED | Per-scenario `LOAD_STATE_SLOT` IS the boundary. |
| E | DEFER | Intentional framework prep; memory confirms. |
| **F** | **VALID — SHIPPED** | Confirmed import error via direct Python invocation. |
| **G** | **VALID — SHIPPED** | Duplicate functions confirmed in two files. |
| H | DEBUNKED | Intentional asymmetry; RA needs user dir for replay-file resolution. |
| **I** | **VALID — SHIPPED** | No cleanup hook anywhere for `spinlab_ra_failed_*` dirs; added `pytest_sessionfinish`. |
| J | DEFER | Load-bearing per prior scan. |
| K | DEFER | M5 carry-over. |
| L | DEFER | Style/safety win, not flake. |
| M | DEFER | Fragile but unbitten. |
| N | DEFER | Single consumer per fixture today. |
| O | DEFER | Observability hygiene, not flake. |
| P | DEFER | CF-4 queued. |
| Q | DEBUNKED | Port inert; no downstream consumer. |
| R | DEBUNKED | Speculative; requires misuse. |
| S | DEFER | Low-risk latent issue. |
