---
date: 2026-05-19
focus: "tests reliability and/or speed"
git_head: 9c7ace5 (baseline) → 49b6a28 (after trivials) → +plan-write on improve/scenario-diagnostics-and-loud-fails
lenses_run: [architect, control-inversion, test-skeptic, dead-code, types, observability]
critiques_run: [skeptic, convergence]
raw_findings: ~30
merged_findings: 12 (M1-M12, of which M1/M6/M8/M9 killed by skeptic, M4 skipped as in-flight)
picked: 4 (N2 + N4 + M12 shipped as trivials; CF-B plan written for medium)
status: full_scan
---

## Top wins

### must-fix
*(none)*

### high-leverage (convergent)
- **CF-B — `scenario_runner` instrumentation** — `tests/integration/ra_poke_engine.py:74-134` + `tests/integration/conftest.py:186-191, :287-295` + `tests/integration/ra_harness.py:313-320`. Per-phase logging in `RAPokeEngine.run_scenario` (fresh_boot_load / addr_map_zero / frame_advance_loop_start / loop_end), `proc.poll()` capture on `run_scenario` timeout, replace unconditional `time.sleep(0.3)` after `pause_toggle()` with `wait_for(name="ra_playing_after_pause_toggle", timeout_s=2.0, interval_s=0.05)`, surface swallowed `pause_toggle` exceptions via `RAHarness._log_pause_toggle_recoverable`. Absorbs M2 + M5 + M10 + OB5. — size: **medium** — plan written at `docs/superpowers/plans/2026-05-19-scenario-runner-instrumentation.md`.

### nearby cleanup
- **N2 (shipped)** — `tests/integration/conftest.py:312-329` — `_convert_auto_skipped_emulator_to_failure` converts emulator-marker test SKIPPED outcomes (without explicit `@pytest.mark.skipif`) into failures. Mechanizes CLAUDE.md "skips count as failures." — size: trivial — shipped at `49b6a28`.
- **N4 (shipped)** — `tests/integration/test_replay_fixture.py:32-38, :192-205` — `FAST_FORWARD_REGRESSION_CAP_S = 30.0` + second assertion in `test_replay_produces_segments`. Catches FF being silently disabled (native rate is ~38s, FF is ~3s). — size: trivial — shipped at `49b6a28`.
- **M12 (shipped)** — `tests/integration/test_transitions.py:22-117` — added f-string context to 31/33 bare assertions. Detector misfires now show the offending event list, not just the bare comparison. — size: trivial — shipped at `49b6a28`.
- **M3 (dropped on re-verification)** — All 4 candidate mock assertions are load-bearing: `test_raclient.py:165` catches retry count, `:237` catches taps=1→0 sends AND taps=1→2 sends (timing covers neither), `:248` catches taps=2→3 sends double-fire (timing only asserts >= gap), `test_practice.py:41` catches double-send (DB assertions cover the logger, not the emu send). Skeptic's full kill stands.
- **M11** — `_seed_paused_run_with_hot_segments` (test_session_manager.py:282-299) could live in `tests/factories.py`. Style only, single-file use today. Defer.

### Carry-overs (already backlogged / declined)
- **CF-1 Mode 3 cascade quarantine** — plan written `docs/superpowers/plans/2026-05-19-mode3-cascade-quarantine.md`, NOT executed.
- **CF-4-DI** — Scheduler / ConditionRegistry constructor injection — next medium after CF-1.
- **CF-3 EndpointType** — `tests/factories.py:76-77` etc. stringly-typed; deferred per prior scan due to call-site ripple.
- **CF-β** — typed AppState + DB rows; in-flight plan.
- **261 pyright errors** — known cleanup; Andrew pushed back on "tracked over time" framing 2026-05-18.

## Picked this session
- **N2** (nearby, trivial) → inline edit on branch `improve/scenario-diagnostics-and-loud-fails`, commit `49b6a28`. Hook in `pytest_runtest_makereport` converts auto-skipped emulator tests to failures.
- **N4** (nearby, trivial) → same commit. `FAST_FORWARD_REGRESSION_CAP_S = 30.0` module constant + assertion in `test_replay_produces_segments`.
- **M12** (nearby, trivial) → same commit. 31 bare assertions in `test_transitions.py` got f-string context.
- **CF-B** (high-leverage, medium) → plan written to `docs/superpowers/plans/2026-05-19-scenario-runner-instrumentation.md`. Five tasks: per-phase logger in RAPokeEngine, proc.poll() on run_scenario timeout, wait_for pause-settle, log swallowed pause_toggle, 15-iter stress validation.

## Dropped during critique
- **M1** (test-fixture Database dynamic attrs across 8 files / 41 sites) — Skeptic KILL: "pyright noise dressed up as reliability." VERIFY adjusted: actually 5 files / 33 sites, not 41/8. Not a flake driver; defers to a focused pyright cleanup pass (CF-β scope or its own session). Out of "tests reliability and/or speed" focus.
- **M3** (brittle mock ceremony assertions) — Skeptic KILL on `test_raclient.py:165` (load-bearing — verifies retry count). Re-VERIFY extended kill to :237 (count = 1 distinct from timing), :248 (count = 2 distinct from elapsed >= gap), `test_practice.py:41` (catches double-send, DB doesn't), `test_dashboard_references.py:100,107,...` (mocked session, only verification surface).
- **M6** (FakeEmuBackend public attrs as test API) — Skeptic KILL: "encapsulation theater" on a test double. `sent_commands[0]` is the cleanest possible assertion idiom; no reliability impact.
- **M8** (FakeEmuBackend.on_disconnect protocol mismatch) — VERIFY DEBUNKED: `python/spinlab/emu_backend.py:21` defines `on_disconnect: Callable | None`, FakeEmuBackend at `tests/conftest.py:103` sets `on_disconnect = None` — matches protocol exactly.
- **M9** (reference_controller_recording bypasses async start_reference()) — Skeptic KILL: proper unit/integration boundary. `start_reference()` is exercised in `test_session_manager.py:142,155,179`.
- **M11** (move helper to factories.py) — Skeptic KILL: explicit style preference, no second consumer. Out of focus.
- **N6** (skeptic-MISSED: claims transition state-leak unresolved) — VERIFY DEBUNKED: `tests/integration/ra_poke_engine.py:80-85` confirms per-scenario LOAD_STATE_SLOT, `_harness_factory.py:57` caches by `(rom_key, use_fresh_state)`. The `project_transition_state_leak.md` memory is accurate; skeptic guessed wrong on this one.

## New finds from VERIFY pass
- **N7** — `tests/integration/conftest.py:107-116` `ra_harness_factory` is keyed by `(rom_key, use_fresh_state)`, not per-test or per-scenario. This is correct (the per-scenario reset is inside `run_scenario` via LOAD_STATE_SLOT). Documented here so a future scan doesn't relitigate.
- **N8** — The verifier confused line 235 (a different test, `test_press_single_tap_no_gap`) with line 247 (`test_press_reset_double_tap_observes_min_gap`) when assessing M3 ceremony. The corrected reading is in this scan's "Dropped during critique" section.
- **N9** — `_diagnostics.install_log_handler()` at `_diagnostics.py:69-75` sets the `spinlab` logger to INFO and attaches the ring handler. Any child logger (`spinlab.test.scenario`, `spinlab.replay_fixture_diag`) inherits this and lands in the ring. CF-B Task 1 relies on this; spelled out in the plan's "Notes for the executing agent."

## Open questions deferred to next scan
- **OQ1** — N3 (RA process leak): teardown audit in `tests/integration/ra_harness.py:393-425` lacks a `psutil.process_iter()` orphan-retroarch.exe check. Speculative — no observed symptom in current run; defer until a leak is suspected.

## Patterns observed by lenses
- **Architect**: The recent test-infra refactor (CF-1/CF-2/conftest split) has tightened the structural surface. What's left is observability inside the harness (CF-B) and pyright-noise cleanup (M1, out of focus). The "fixtures attach undeclared attrs to Database" pattern (M1) is real but it's a type-cleanliness concern, not a flake driver.
- **Control-inversion**: CF-4-DI remains the biggest open lever. No new control-inversion finds this scan beyond what's queued.
- **Test-skeptic**: Returned 6 items; 0 survived critique + verify. Reinforces the prior pattern (last 4 tests-focused scans) that the suite has absorbed most ceremony critiques. Each new sweep finds increasingly nitpicky items — converging.
- **Dead-code**: 0 findings. Recent sweeps have kept hygiene tight; this lens earns its keep mostly by reassurance now.
- **Types**: CF-3 EndpointType propagation + M1 FixtureContext are the two big clusters; both already documented in prior scans. No new types-driven flake finds.
- **Observability**: The dominant high-leverage finding (CF-B) is observability inside the test infra itself — exactly the place that's been most opaque during the 2026-05-18 flake hunt. The `pytest_runtest_makereport` diagnostic block exists; what it has to chew on is the gap CF-B closes.

## Verification verdict summary

| Cluster | Verdict | Evidence |
|---------|---------|----------|
| M1 (Database dynamic attrs) | VALID but out-of-focus | conftest.py:183-184, test_cold_fill.py:76-81 confirmed; 33 hits/5 files (not 41/8). Pyright noise, not flake. |
| M2 (0.3s pause-settle sleep) | VALID | conftest.py:293 confirmed; fixture is session-scoped so net win is small but real. CF-B absorbs. |
| M3-165 (call_count == SAVE_RETRY_ATTEMPTS) | VALID, LOAD-BEARING | test_raclient.py:165 verifies retries actually retry. Keep. |
| M3-237 (assert_called_once on single-tap) | VALID, LOAD-BEARING | test_raclient.py:237 catches taps=1→0 AND taps=1→2 sends. Keep. |
| M3-248 (call_count == 2) | VALID, LOAD-BEARING | test_raclient.py:248 catches taps=2→3 sends (double-fire). Timing only validates >= gap. Keep. |
| M3-practice (assert_called_once) | VALID, LOAD-BEARING | test_practice.py:41 catches double-send; DB asserts cover logger path, not emu send. Keep. |
| M3-routes (assert_called_once_with) | VALID, LOAD-BEARING | test_dashboard_references.py — session is a mock, no other verification surface. Keep. |
| M5 (run_scenario silence) | VALID | ra_poke_engine.py:74-134 has zero logger calls; conftest.py:186-191 timeout has no proc.poll(). CF-B absorbs. |
| M5 (ra_harness.py:317 swallow) | VALID | ra_harness.py:315-320 `except Exception: pass`. CF-B absorbs. |
| M6 (FakeEmuBackend public attrs) | VALID but out-of-focus | conftest.py:88-129; 8 callers. Skeptic's "encapsulation theater on test double" kill stands. |
| M7 (fixture cleanup on partial-setup crash) | LOW (speculative) | _dashboard_harness.py:115-149 has no try/except in __enter__; no observed symptom. Defer. |
| M8 (FakeEmuBackend.on_disconnect protocol) | DEBUNKED | emu_backend.py:21 `on_disconnect: Callable | None` matches `None`. |
| M9 (reference_controller_recording bypass) | DEFER (intentional) | tests/unit/capture/conftest.py:39 calls `_enter_recording`; start_reference covered in test_session_manager.py:142+. Unit/integration boundary, not a defect. |
| M10 (orchestrator_ready polling) | VALID, small win | conftest.py:277-283 polls every 0.25s. CF-B's wait_for-with-50ms-interval addresses the same shape downstream. |
| M11 (move helper to factories.py) | DEFER (style) | test_session_manager.py:282-299; single-file use. |
| M12 (test_transitions bare asserts) | VALID, SHIPPED | 31/33 bare; commit 49b6a28 added f-string context. |
| N1 (per-scenario duration logging) | DEFER | Worthy improvement, but folded into CF-B's "loop_end elapsed_total=..." line. |
| N2 (loud-fail SKIPPED hook) | VALID, SHIPPED | conftest.py:312-329 hook lands at 49b6a28. |
| N3 (RA process leak audit) | LOW | ra_harness.py:393-425 no psutil orphan check; speculative. Defer. |
| N4 (FAST_FORWARD regression cap) | VALID, SHIPPED | test_replay_fixture.py:35-38, :199-205 lands at 49b6a28. |
| N5 (frontend smoke build precondition) | DEFER | Worthy, but CLAUDE.md already documents the precondition; manual workflow. |
| N6 (transition state-leak unresolved) | DEBUNKED | ra_poke_engine.py:80-85 confirms per-scenario LOAD_STATE_SLOT. Memory `project_transition_state_leak` is accurate. |

## Baseline note

Baseline `python -m pytest -m "not emulator"` at HEAD `9c7ace5`: 915 passed (clean fast suite). Full suite baseline not re-run this session — the merge from `worktree-segments-v07-phase0` was just before this scan, and the prior 2026-05-19 scan documented a 1-failure full-suite baseline (the known Mode 2/Mode 3 flake target of CF-1 plan). After the trivials commit at `49b6a28`: 915 passed (clean fast suite preserved). Emulator-suite validation of CF-B is in the plan as Task 5 (15-iteration stress run after implementation).

## Sequence note

`improve/scenario-diagnostics-and-loud-fails` overlaps with `improve/mode3-cascade-quarantine` in that both touch `tests/integration/conftest.py`'s `pytest_runtest_makereport` hook. CF-B Task 2 (proc.poll() in `run_scenario` timeout) is complementary — it surfaces RA exit status at scenario timeout. CF-1's makereport probe surfaces it on NCITimeout/NCIProtocolError. They don't conflict; sequencing should pick one to land first and rebase the other.
