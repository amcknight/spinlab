---
date: 2026-05-14
focus: "messy flaky test situation"
git_head: 27860df
lenses_run: [architect, control-inversion, test-skeptic, dead-code, types, observability]
critiques_run: [skeptic, convergence]
findings_count: ~75 raw lens → 15 clusters → 5 surviving items after critique/verify
status: full_scan
---

## Top wins

### must-fix
- **Transition tests fail 3-5/run on main** — `tests/integration/test_transitions.py` (whole file). Two interleaved bugs: (a) session-scoped `ra_harness` leaks SPC700/CPU state between scenarios — `test_key_exit`/`test_orb_exit` see `goal == "abort"` after `test_entrance_goal` runs first ([[project_transition_state_leak]]); (b) `ROM_REGISTRY["default"]` temp-points at Love Yourself.smc because the FRAMEADVANCE probe rejects vanilla SMW + Toothpaste, so scenarios that hard-code vanilla level numbers (e.g. `entrance_goal.poke`) misfire ([[project_framadvance_probe_too_aggressive]]). Per CLAUDE.md "pre-existing failures are still failures" — current main has a red baseline. — **size: medium** (followup plan already exists at `docs/superpowers/plans/2026-05-14-transition-state-leak-followup.md`, option B = per-ROM fresh-boot savestate + `LOAD_STATE_SLOT` per scenario; collapses with the probe issue — see CV1).

### high-leverage
- **Diagnostic hook only fires for one fixture** — `tests/integration/conftest.py:572` literal `for fixture_name in ("replay_ra_dashboard",):`. Verified: every `run_scenario` test gets an empty diagnostic block on failure. Critical because the next round of transition flakes will need observability *at failure time*, not via reproducer. Includes shape asymmetry (callable vs tuple yield) and missing fields (poll_count, `_read_failing`, state_version, cold-fill state). — **size: medium** (CV2 below).

### convergent win
- **CV1 — Per-scenario fresh-boot savestate** (the must-fix above). Absorbs: state-leak (C1.1-C1.4), FRAMEADVANCE probe rejecting valid ROMs (C3.1), `WRAM_SANITY_*` dishonest naming (C3.2-C3.4), implicit ROM declaration (C4.1, C4.4). Mechanism: a baseline savestate is *by construction* a live frame, so the warm-up probe becomes obsolete and the rom_key parameter stops being optional. Anchor: `tests/integration/ra_poke_engine.py:56` (insert `reset_to_baseline()` per V18 plan; rom_key→fresh_state_path mapping lives in conftest factory layer, not the engine). — **size: medium** (plan exists).
- **CV2 — Diagnostic hook generalization** — Absorbs C2.1, C2.2, C2.3, C2.4, C2.5, C7.2, C15.1. Skeptic flagged C2.3/C2.5 (DB sanity, poller stats, orchestrator task health) as wishlist scope-creep — fair; do the shape unification first (extend hook to `run_scenario` failures with `<unavailable: reason>` labels), then add fields opportunistically when one is needed in a post-mortem. Anchor: `tests/integration/conftest.py:572`. — **size: medium**.

### nearby cleanup
- `python/spinlab/retroarch/poller.py:42-44` — gravestone docstring referencing deleted `mark_state_loaded` flag (verified; carry-over D6 from prior scans, never picked). size: trivial.
- `python/spinlab/state_builder.py:30,106,135` + `python/spinlab/capture/reference.py:183` + `python/spinlab/capture/cold_fill.py:127` — `dict` returns where `AppState` / `PausedRunState` / `ColdFillState` exist. **J3 carry-over from 2026-05-14-improve-1735**. size: medium when picked. Off-topic for "flaky tests" focus, but listed for continuity.
- `tests/integration/conftest.py:50-51` — dead `TOOTHPASTE_ROM_NAME` and `CLEAN_SMW_ROM_NAME` constants. Skeptic right: these come back to life when CV1 lands. **Don't delete now.**
- `tests/unit/test_dashboard_integration.py:73-74` — `TODO(Task 8)` comment about missing `add_save_state` call. Off-topic for flaky-tests; leave as roadmap marker.
- `tests/integration/conftest.py:62-69` — comment block claiming "three transition tests currently FAIL"; this is correct documentation that CV1 will delete. Don't paper over with `xfail`.

## Picked this session

- **CV1 (must-fix, medium)** — auto-included per skill rules (must-fix items skip the picker). Followup plan already exists. Verifier confirmed plan puts savestate load in `RAPokeEngine.run_scenario` and rom_key→state-path in conftest factory layer — both convergence-hunter and skeptic align with this layering.
- **CV2 (high-leverage, medium)** — picker question below.

## Dropped during critique / verify

- **V7 debunked** — Critique-of-critique flagged "bare except swallows detector.step exceptions" as worth fixing. **VERIFY proves no bare except exists** at `tests/integration/ra_poke_engine.py:86-87`. C10.3 dropped.
- **C6.1 (replay_ra_dashboard pytest.skip on missing savestate_dir)** — Skeptic right: this is a configuration-absence skip, not a `ra_harness launch failed` skip; the C1-C3 sweep targeted the latter. Different beast; leave it.
- **C6.2 (class-level skipif on FIXTURE_REPLAY.exists())** — Verified `FIXTURE_REPLAY` IS checked into the repo at `tests/fixtures/love_yourself/one_level.replay`. The skipif is a defense against the file going missing in unusual checkouts. Leave.
- **C3.2-C3.4 (`WRAM_SANITY_*` naming + magic numbers)** — Subsumed by CV1 (the probe gets deleted entirely once savestates are the boot mechanism). No need to rename a thing scheduled for deletion.
- **C5.1-C5.4 (helper duplication: polling loops, `_load_config`, factory two-layer, `_api()`)** — Skeptic right on all four. Verified `_api()` is exactly 2 lines. Verified polling loops are ~6 lines each. Factory class+function landed deliberately one week ago in C1-C3. All ceremony-grade.
- **C7.x (poke DSL types: `parse_poke -> dict`, `pokes: list[dict]`)** — Off-topic for flaky-tests. Real win only if combined with CV1's `PokeScenario` dataclass introduction; convergence-hunter folded it into CV3 (poke-scenario-as-typed-value-object) but it's cosmetic without CV1 motivating it. Defer.
- **C9.x (raclient/nci/cold_fill_detector/poller observability gaps)** — Off-topic; Bucket A/B logging pass already swept this layer. Skeptic right: lens-dump.
- **C13.x (SessionManager constructs 5 controllers in `__init__`; `_clear_ref_and_idle` side effects)** — Off-topic AND contradicts the just-landed encapsulation pass ([[project_encapsulation_pass_2026_05_11]]). Skeptic right: re-opening days after Bundle 1-3 landed is churn.
- **C10.1 (bare assert lacks breadcrumb)** — Skeptic right: pytest already auto-prints `exits[0].goal` and `len(exits)` on failure. Verified the assertion shape. CV2 (diagnostic hook) is where the breadcrumb actually goes.
- **C10.4 (polling intervals brittle under xdist)** — `[[project_pytest_xdist_experiment_2026_05_11]]` deferred xdist; YAGNI.
- **C11.1, C11.2, C11.3 (thin smoke / FakeEmuBackend in crash recovery / harness-isolation tests test infra)** — Skeptic right: smoke is scoped right, FakeEmuBackend in unit-style tests is fine, harness-isolation tests *are* the unit under test. C11.2 is the only borderline one (real RA-driven crash test would catch real bugs Fake won't) — leave as a separate future item, not absorbed here.
- **C12.x (test_poke_parser asserts dict shape)** — Skeptic right: parser unit tests assert parser contract; verified end-to-end coverage exists via `test_transitions.py` running scenarios through `engine.run_scenario`.
- **C14.2 (one_level.json:3 outdated comment)** — Skeptic right: the comment IS the policy (determinism test self-rewrites). Verifier flagged "medium" confidence on the rewrite mechanism but the comment text matches a self-healing pattern.
- **C5.3 unit-test coupling to factory** — Verifier saw `tests/unit/integration/test_ra_harness_factory.py` deliberately tests both class + factory function (just landed C1-C3). Coupling is intentional, not a code smell.

## New finds from VERIFY pass

- **No NEW issues beyond the merged list.** Verifier confirmed every load-bearing claim with high confidence; bonus pass turned up zero unflagged finds. The state_builder.py untyped return (V14) was already J3 carry-over.

## Synergies

- **CV1 swallows the probe (C3) AND the implicit-ROM (C4) clusters.** Both are downstream symptoms of "no notion of clean per-scenario boot state." Once `LOAD_STATE_SLOT` is the boot mechanism, the warm-up retry probe is dead code (`WRAM_SANITY_*` constants gone), and the rom_key parameter becomes load-bearing (no more `default` → Love Yourself silent fallback). This is the highest-leverage single move on the board.
- **CV2 stays separate from CV1** — Different layer (test plumbing vs scenario isolation). Anti-convergence flagged correctly by hunter. But CV2 becomes more valuable AFTER CV1 lands because the next round of transition surprises won't be the state-leak — they'll be new things, and the diagnostic block needs to actually contain something.

## Full merged list (collapsed by lens, before critique)

### Lens 1 — Principal architect
- A1 conftest.py:572 diagnostic hook hardcoded fixture name
- A2 ra_poke_engine.py:56-99 run_scenario conflates 3 concerns
- A3 conftest.py:385-398 fixture-shape asymmetry
- A4 conftest.py:384-399 run_scenario function-scoped on session-scoped harness, no test handle
- A5 test_transitions.py — tests assume Love Yourself's level numbering (silent fallback)
- A6 conftest.py:76-79 ROM_REGISTRY no schema
- A7 ra_harness.py:250-266 FRAMEADVANCE probe too narrow
- A8 conftest.py:209-284 fake_dashboard_server / replay_ra_dashboard polling dup
- A9 conftest.py:147-154 _load_config dual caller
- A10 test_replay_fixture.py:34-35 _api helper duplicates request wrapping
- A11 conftest.py:314-354 two-layer factory
- A12 conftest.py:62-79 ROM_REGISTRY admits "temporary state"
- A13 ra_poke_engine.py:1-26 RAPokeEngine no rom_key/scenario context
- A14 conftest.py:561-565 _ring module-level global
- A15 test_replay_fixture.py:120-125 fixture-failure not handled in autouse setup

### Lens 2 — Control inversion
- I1 conftest.py:384 fixture-scope mismatch
- I2 ra_poke_engine.py:79 frame_advance/write_ram ordering — within-frame fix doesn't address scenario-boundary leak
- I3 conftest.py:314 session-wide caching but tests assume hermetic state
- I4 conftest.py:509 fixture mutates production emu state via pause_toggle
- I5 wiring.py:99,110 placeholder lambda + late binding (already verified addressed)
- I6 orchestrator.py:80,83 set_event_callback rebinding (already verified addressed)
- I7 session_manager.py:93-96 controllers pull deps
- I8 dashboard.py:27-38 event_loop bidirectional coupling
- I9 conftest.py:572 hook hardcoded
- I10 session_manager.py:179 _clear_ref_and_idle side-effect orchestration
- I11 conftest.py:384-398 run_scenario closure mutates harness

### Lens 3 — Test skeptic
- S1 test_transitions.py:22-83 session-scope state leak
- S2 conftest.py:572 diagnostic hook
- S3 ra_harness.py:251-266 FRAMEADVANCE probe rejects valid ROMs
- S4 conftest.py:76-79 ROM_REGISTRY ambiguous mapping
- S5 conftest.py:425-428 replay fixture pytest.skip
- S6 test_poke_parser.py:18-77 assert dict shape only
- S7 test_harness_isolation.py:18 tests test infra
- S8 test_retroarch_practice_smoke.py:16-51 too thin
- S9 test_crash_recovery.py:37-73 FakeEmuBackend + in-mem SQLite never exercises real crash
- S10 test_replay_fixture.py:109-112 class-level skipif
- S11 conftest.py:265-275 fake_dashboard polling brittle
- S12 conftest.py:492-504 replay polling brittle
- S13 test_replay_fixture.py:38-52,55-106 _wait_* helpers don't emit timeline
- S14 test_transitions.py:19 session-loop scope mixing
- S15 conftest.py:570-608 diagnostic best-effort silently empty
- S16 unit/capture/conftest.py:14-40 manual FakeEmuBackend may drift from protocol
- S17 ra_harness.py:250 magic numbers undocumented
- S18 test_poke_parser.py:55-58 comment-handling never goes through engine

### Lens 4 — Dead code
- D1 poller.py:43 mark_state_loaded gravestone
- D2 conftest.py:50-51 unused TOOTHPASTE/CLEAN constants
- D3 conftest.py:62-69 "three tests currently FAIL" comment block
- D4 ra_harness.py:37-48 dishonest WRAM_SANITY_* names
- D5 unit/test_dashboard_integration.py:73-74 TODO(Task 8)
- D6 conftest.py:43-49 stale comment refs
- D7 conftest.py:572-597 diagnostic hook hardcoded fixture
- D8 fixtures/love_yourself/one_level.json:3 outdated comment

### Lens 5 — Types & contracts
- T1 ra_poke_engine.py:56 run_scenario(scenario: dict) -> list
- T2 conftest.py:388 run_scenario fixture closure list return
- T3 state_builder.py:30 build() -> dict (J3 carry-over)
- T4 state_builder.py:106,135 untyped helpers (J3)
- T5 state_builder.py:156,183 ad-hoc current_seg/model_outputs dicts
- T6 conftest.py:573 fixture_val Any unpack
- T7 conftest.py:147 _load_config -> dict
- T8 poke_parser.py:30 parse_poke -> dict
- T9 poke_parser.py:38 pokes: list[dict]
- T10 capture/reference.py:183 get_paused_state -> dict | None
- T11 capture/cold_fill.py:118 get_state -> dict | None

### Lens 6 — Observability
- O1 test_transitions.py:70-76 bare assert no breadcrumb
- O2 conftest.py:572 hook one-fixture
- O3 conftest.py:392 parse error no file context
- O4 ra_poke_engine.py:79-86 no log on poke fail / io_port=0
- O5 raclient.py:350-363 file-move retry logs only final
- O6 conftest.py:388-396 wait_for timeout no phase visibility
- O7 nci.py:157-162 read_ram -1 doesn't log request
- O8 ra_poke_engine.py:56-99 silent except on detector.step (DEBUNKED by VERIFY)
- O9 cold_fill_detector.py:50-70 resync silent except phantom case
- O10 poller.py:113-122 _read_failing no consecutive-count
- O11 conftest.py:567-608 _collect_diagnostics no DB sanity
- O12 ra_harness.py:244-266 probe failure doesn't log frozen bytes
